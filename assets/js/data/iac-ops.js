/* Terraform & IaC — running it for real: adopting existing infrastructure,
   detecting drift, environments, policy gates and the pipeline that should be
   the only thing allowed to apply to production.                            */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

{ name:'terraform import', track:'iac', cat:'import', level:'advanced',
  cert:['tf-associate:state'],
  sum:'Bring existing infrastructure under management without recreating it.',
  syntax:'terraform import ADDRESS ID   ·   import { to = ADDRESS  id = "…" }',
  flags:[['ADDRESS ID','The CLI form: state-only, and the configuration must already exist'],
         ['import { to = … id = … }','The block form: planned, reviewable, and it can generate configuration'],
         ['-generate-config-out=gen.tf','With import blocks, write a starting configuration for you'],
         ['(id format varies)','Each resource type documents its own; a VPC takes vpc-…, an IAM policy takes an ARN']],
  ex:[['import { to = aws_instance.web  id = "i-0a1b2c3d" }','The modern form — shows up in plan before it touches state'],
      ['terraform plan -generate-config-out=generated.tf','Draft configuration from what actually exists'],
      ['terraform import \'aws_s3_bucket.logs\' my-logs-bucket','The CLI form, one resource at a time']],
  tip:'Prefer **import blocks** over the CLI command: they appear in the plan so you can see what will be adopted before anything is written, they are reviewed like any other change, and `-generate-config-out` writes a first draft of the configuration for you. The trap with either form is that import records reality in state but does not write your configuration — so if the code says one thing and the imported resource is another, the very next plan proposes to change the live resource to match. Always run a plan straight after an import and expect it to be empty; if it is not, the configuration is wrong, not the infrastructure.',
  related:['terraform state rm','terraform plan','moved'] },

{ name:'terraform plan -refresh-only', track:'iac', cat:'drift', level:'intermediate',
  cert:['tf-associate:state'],
  sum:'Show how reality has diverged from state, proposing no configuration changes.',
  syntax:'terraform plan -refresh-only   ·   terraform apply -refresh-only',
  flags:[['-refresh-only','Compare state against the real world and stop there'],
         ['apply -refresh-only','Accept reality into state without changing any infrastructure'],
         ['-refresh=false','The opposite: plan from state alone, faster and blind to drift']],
  ex:[['terraform plan -refresh-only','What changed outside Terraform'],
      ['terraform apply -refresh-only','Record reality in state, changing nothing live'],
      ['terraform plan -refresh-only -detailed-exitcode','Exit 2 means drift — a scheduled drift alarm in one line']],
  tip:'This separates the two questions a normal plan mixes together: "what has changed outside Terraform" and "what would my code change". Running it on a schedule and alerting on exit code 2 turns drift from something discovered during an incident into a notification. When drift is found, decide deliberately which side is right — `apply -refresh-only` accepts reality into state, while a normal apply pushes the code back over reality — and the wrong choice here either enshrines an unreviewed console change or reverts an emergency fix somebody made at 3am for a reason.',
  related:['terraform plan','terraform import','terraform state show'] },

{ name:'terraform workspace', track:'iac', cat:'workspace', level:'intermediate',
  cert:['tf-associate:cli','tf-associate:state'],
  sum:'Multiple states from one configuration — useful, and not an environment strategy.',
  syntax:'terraform workspace list | new NAME | select NAME | show',
  flags:[['show','Which workspace you are in. Run it before anything destructive'],
         ['list','All of them; the asterisk marks the current one'],
         ['new / select','Create or switch — each has its own state, same backend'],
         ['terraform.workspace','Reference the name inside the configuration']],
  ex:[['terraform workspace show','The two seconds that prevent the classic mistake'],
      ['terraform workspace select prod && terraform plan','Switch deliberately, then plan'],
      ['locals { instance_count = terraform.workspace == "prod" ? 6 : 1 }','Sizing by workspace — workable, and it hides differences in expressions']],
  tip:'Workspaces share one backend and one configuration, so they suit short-lived parallel copies — a feature branch, a test environment — far better than production separation. For real environments most teams prefer a directory per environment with its own backend key and its own variables: it gives separate credentials, separate blast radius, and a diff you can read, whereas workspace-conditional expressions scatter environment differences through the code. Whatever the choice, `workspace show` before a destructive command is the habit that stops the wrong-environment story.',
  related:['terraform destroy','backend "s3"','terraform plan'] },

{ name:'terraform apply -replace', track:'iac', cat:'drift', level:'intermediate',
  cert:['tf-associate:cli'],
  sum:'Force one resource to be destroyed and recreated. Replaces the old `taint`.',
  syntax:'terraform apply -replace=ADDRESS   ·   terraform plan -replace=ADDRESS -out=tfplan',
  flags:[['-replace=ADDRESS','Mark it for replacement as part of a normal plan'],
         ['plan -replace first','You see the consequences before agreeing to them — which taint never gave you'],
         ['terraform taint','Deprecated: it wrote to state immediately with no plan']],
  ex:[['terraform plan -replace=aws_instance.web -out=tfplan','See what replacing it drags along'],
      ['terraform apply -replace=\'aws_instance.web["api"]\'','Recreate one for_each instance'],
      ['terraform apply -replace=module.db.aws_db_instance.main','Replacing a database — read that plan very carefully']],
  tip:'This is the right way to recreate a resource that has gone bad without editing configuration to force a change and then editing it back. The reason it superseded `taint` is that `taint` mutated state immediately, so you found out what replacement meant during the apply; `-replace` runs through plan, so the destroy-and-recreate and everything downstream of it is visible first. Read that plan properly — replacing a resource other things depend on frequently cascades further than expected.',
  related:['terraform plan','terraform apply','lifecycle'] },

{ name:'tfsec', track:'iac', cat:'policy', level:'intermediate',
  cert:['aws-sec:infrastructure-security'],
  sum:'Static analysis of Terraform for insecure configuration, before anything is created.',
  syntax:'tfsec .   ·   checkov -d .   ·   trivy config .',
  flags:[['--soft-fail','Report without failing the build — the right first setting'],
         ['#tfsec:ignore:aws-s3-enable-bucket-logging','Inline exception, and it should carry a reason and an expiry'],
         ['--format json | sarif','SARIF uploads into GitHub code scanning'],
         ['checkov -d . --framework terraform','A broader rule set, more findings, more noise']],
  ex:[['tfsec . --format json > findings.json','Machine-readable for a pipeline gate'],
      ['checkov -d . --compact --quiet','The alternative scanner; the two disagree usefully'],
      ['tfsec . --soft-fail','Introduce it without blocking anyone on day one']],
  tip:'These catch the everyday findings — a security group open to the world, an unencrypted bucket, a public database, plaintext secrets — before a resource exists, which is the cheapest possible moment. Introduce with `--soft-fail` and a baseline, because turning a scanner on hard against an existing estate produces hundreds of findings and the team learns to ignore the tool. The inline ignore comment is fine when it carries a reason and a review date, and corrosive when it becomes the standard response to a finding.',
  related:['terraform validate','terraform show'] },

{ name:'terraform plan in CI', track:'iac', cat:'ci', level:'advanced',
  cert:['tf-associate:cli'],
  sum:'Plan on the pull request, apply on merge, and never from a laptop.',
  syntax:'plan -out=tfplan → upload artifact → review → apply tfplan',
  flags:[['-out=tfplan then apply tfplan','Apply exactly what was reviewed; no re-plan window'],
         ['-detailed-exitcode','Exit 2 means changes, which drives whether to gate'],
         ['-lock-timeout=5m','Wait rather than failing when another job holds the lock'],
         ['-no-color','Readable in a log or a pull request comment'],
         ['OIDC to assume a role','No long-lived cloud keys in the CI system at all']],
  ex:[['terraform plan -no-color -out=tfplan -detailed-exitcode || [ $? -eq 2 ]','Plan, tolerating the changes-pending exit code'],
      ['terraform show -no-color tfplan > plan.txt','Post it as a pull request comment for review'],
      ['terraform apply -input=false -auto-approve tfplan','On merge: apply the artifact, not a fresh plan']],
  tip:'The shape that works is plan on the pull request with the output posted for review, then apply the **saved plan artifact** on merge — `-auto-approve` is fine there precisely because a human approved that exact plan. Two things to get right: authenticate with OIDC and a short-lived assumed role rather than storing access keys, and remember a plan file **contains state values, secrets included**, so the artifact needs the same protection as state. Then make the pipeline the only identity with write access to production, so applying from a laptop is impossible rather than merely discouraged.',
  related:['terraform apply','terraform show','backend "s3"'] },

{ name:'TF_VAR_ and secrets', track:'iac', cat:'secrets', level:'intermediate',
  cert:['aws-sec:data-protection'],
  sum:'Getting secrets into Terraform without leaving them anywhere.',
  syntax:'export TF_VAR_db_password=…   ·   data "aws_secretsmanager_secret_version" …',
  flags:[['TF_VAR_name','Environment variable for a variable; keeps it out of files'],
         ['data "aws_secretsmanager_secret_version"','Fetch at plan time — no secret in the repository'],
         ['ephemeral / write-only arguments','Newer Terraform: values that are not persisted to state'],
         ['(state is not encrypted by Terraform)','Whatever route you use, the value lands in state unless the resource supports write-only']],
  ex:[['export TF_VAR_db_password="$(aws secretsmanager get-secret-value --secret-id prod/db --query SecretString --output text | jq -r .password)"','Fetch and pass without writing a file'],
      ['data "aws_secretsmanager_secret_version" "db" { secret_id = "prod/db" }','Let the configuration fetch it'],
      ['resource "aws_db_instance" "main" { manage_master_user_password = true }','Best of all: let the provider own the password so it never reaches state']],
  tip:'The rule that governs everything here is that **state holds every value in plain text**, so a secret passed into a resource is a secret in the state file — which is why state is encrypted, access-controlled and never committed. The strongest pattern is not passing the secret at all: `manage_master_user_password` hands the credential to Secrets Manager and Terraform never sees it. Failing that, fetch at runtime from a secret store rather than putting it in a `.tfvars` file, and keep `*.tfvars` out of the repository so nobody commits one "temporarily".',
  related:['variable','backend "s3"','terraform output'] },

{ name:'terraform fmt', track:'iac', cat:'test', level:'beginner',
  cert:['tf-associate:cli'],
  sum:'Canonical formatting, so reviews are about behaviour rather than whitespace.',
  syntax:'terraform fmt [-recursive] [-check] [-diff]',
  flags:[['-recursive','Include subdirectories; without it only the current one'],
         ['-check','Exit non-zero if anything is unformatted — the CI form'],
         ['-diff','Show what would change'],
         ['(pre-commit)','A hook makes it invisible rather than a review comment']],
  ex:[['terraform fmt -recursive','Format the tree'],
      ['terraform fmt -check -recursive -diff','The CI gate, showing what is wrong'],
      ['terraform fmt -check -recursive || (terraform fmt -recursive && git diff)','Fix and show, locally']],
  tip:'Cheap and worth enforcing, because unformatted code turns every review into a whitespace argument and hides the two lines that mattered. Put `fmt -check` and `validate` at the front of the pipeline where they cost seconds, and a pre-commit hook locally so nobody meets them as a CI failure. The related habit is a `.gitignore` that excludes `.terraform/`, `*.tfstate*`, `*.tfvars` and `tfplan` — committed state and committed plan files are both real leaks, and both happen most often to people who are new to the repository.',
  related:['terraform validate','terraform plan in CI'] },

{ name:'terraform test', track:'iac', cat:'test', level:'advanced',
  cert:['tf-associate:modules'],
  sum:'Native module tests — assertions against a plan or a real apply.',
  syntax:'terraform test   ·   tests/*.tftest.hcl',
  flags:[['command = plan','Assert against the plan without creating anything — fast, and most module bugs are here'],
         ['command = apply','Actually create, assert, then destroy. Slow, real, and it costs money'],
         ['run "name" { assert { condition = … error_message = … } }','The unit of a test'],
         ['variables { }','Per-run inputs, so one module is tested across several configurations']],
  ex:[['terraform test','Run every .tftest.hcl in tests/'],
      ['run "defaults" { command = plan  assert { condition = aws_instance.web.instance_type == "t3.micro"  error_message = "default size changed" } }','Pin a default so a module bump cannot move it silently'],
      ['run "validation" { command = plan  variables { env = "nope" }  expect_failures = [var.env] }','Assert that bad input is rejected']],
  tip:'Plan-mode tests are the ones to write first: they run in seconds, need no cloud credentials, and catch the majority of module regressions — a changed default, a broken conditional, a validation that stopped rejecting bad input. Apply-mode tests create real infrastructure and cost real money, so keep them few and scoped to the module\'s core promise. The failure this prevents is the Phase-4 classic: a module version bump that changes a default nobody noticed, discovered as a plan diff in production.',
  related:['terraform validate','module','tfsec'] },

{ name:'terraform graph', track:'iac', cat:'test', level:'advanced',
  cert:['tf-associate:cli'],
  sum:'Render the dependency graph — for when an ordering or cycle error makes no sense.',
  syntax:'terraform graph [-type=plan] | dot -Tsvg > graph.svg',
  flags:[['-type=plan','The graph for a planned change rather than the whole configuration'],
         ['| dot -Tsvg','Needs graphviz; the raw output is DOT'],
         ['(cycle errors)','Terraform names the cycle in the error, and this shows you the shape of it']],
  ex:[['terraform graph | dot -Tsvg > graph.svg','The whole configuration'],
      ['terraform graph -type=plan | dot -Tpng > plan.png','Just what this change touches'],
      ['terraform graph | grep -A3 aws_security_group','Text search when rendering is overkill']],
  tip:'Reach for this on two occasions: a cycle error you cannot see, and an apply ordering that surprises you. A cycle almost always means two resources reference each other — commonly a pair of security groups each allowing the other — and the fix is to break the reference out into separate rule resources rather than to add `depends_on`. For everyday work the graph is too dense to read, so it is a debugging tool rather than documentation.',
  related:['depends_on','terraform plan'] }

);
