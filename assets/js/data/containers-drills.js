/* Containers track — open-ended drills, hazard questions and recall items.
   Answer the drills out loud before expanding: speaking it is the skill. */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };
LX.drills = LX.drills || [];
LX.quiz = LX.quiz || [];
LX.dangerQs = LX.dangerQs || [];

/* ── Drills ─────────────────────────────────────────────────── */
LX.drills.push(

{ track:'containers', cat:'runtime', level:'beginner', cert:['cka:workloads-and-scheduling'],
  q:'What actually is a container? Explain it without using the word "lightweight".',
  a:'A normal Linux process that the kernel has been told to show a restricted view of the system. Namespaces give it its own network stack, mount table, PID numbering, hostname, IPC and user mapping; cgroups cap what it can consume. There is no virtual machine and no guest kernel — `ps aux` on the host shows the process, and `nsenter -t PID -n` steps into its network view. That is why containers start in milliseconds and why a container escape is a host compromise.',
  points:['Namespaces isolate what it can see; cgroups limit what it can use',
    'Same kernel as the host — no guest OS, which is the whole performance story',
    'The container image is layers plus metadata; the running container adds a writable layer',
    'Security consequence: root in the container is root on the host unless user namespaces or a sandbox runtime say otherwise'] },

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:troubleshooting'],
  q:'A pod is not running. Walk me through how you find out why, in order.',
  a:'Read the STATUS first because it routes the whole investigation. Pending means the scheduler could not place it, so `describe pod` and read the FailedScheduling event. ImagePullBackOff means it was scheduled but the image could not be fetched, and the event carries the registry error. CrashLoopBackOff means it ran and died, so `kubectl logs --previous` for the instance that died and `describe` for the exit code. Running but 0/1 Ready means readiness is failing and the probe message says which. I only start changing things once the state has told me which of those four I am in.',
  points:['STATUS is a routing table, not a verdict',
    'Pending → scheduler event; ImagePull → registry error; CrashLoop → logs --previous plus exit code',
    'Exit 137 with OOMKilled is the memory limit, not the application',
    '0/1 Running is readiness, which is deliberate: Kubernetes is keeping traffic away'] },

{ track:'containers', cat:'svc', level:'beginner', cert:['cka:services-and-networking'],
  q:'A Service is not routing traffic although the pods are healthy. What are the possible causes?',
  a:'There are only two reasons a Service has no endpoints: the selector does not match the pod labels, or every pod is failing its readiness probe. `kubectl get endpoints` shows it in one line and `describe svc` shows the selector to compare against `get pods --show-labels`. If endpoints *are* populated and traffic still fails, the remaining candidates are targetPort pointing at a port nothing serves, a NetworkPolicy denying the client, or externalTrafficPolicy: Local on a node with no local pod.',
  points:['Empty endpoints = selector mismatch or readiness failing. Nothing else',
    'Selectors are exact string matches with no warning when they miss',
    'port is what clients use, targetPort is the container port',
    'Port-forward to the pod first to prove the application is not the problem'] },

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:workloads-and-scheduling'],
  q:'Explain requests and limits, and what happens when a container exceeds each.',
  a:'A request is a reservation the scheduler uses to decide placement; a limit is a ceiling the kernel enforces at runtime. Exceeding a **memory** limit gets the container killed immediately — SIGKILL, exit 137, Reason OOMKilled — because memory cannot be throttled. Exceeding a **CPU** limit gets it throttled instead, which shows up as latency rather than a restart. Requests set too high waste the cluster, because a node can be unschedulable at 10% real usage; limits set too low kill healthy processes.',
  points:['Requests decide scheduling; limits decide killing and throttling',
    'Memory over limit = OOMKilled. CPU over limit = throttled, not killed',
    'A CPU limit is often the wrong default — throttling a latency-sensitive service hurts more than it protects',
    'A memory limit is almost always right, because unbounded memory takes the node down'] },

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:workloads-and-scheduling'],
  q:'Liveness, readiness and startup probes — what does each one do, and what goes wrong when you confuse them?',
  a:'Readiness controls **traffic**: failing removes the pod from Service endpoints but leaves it running. Liveness controls **life**: failing restarts the container. Startup holds the other two off while a slow application boots. The classic mistake is using liveness where you meant readiness — a pod that is briefly unable to serve gets restarted instead of drained, and if a shared dependency is slow, every replica restarts at once and you have turned a degradation into an outage.',
  points:['Readiness routes, liveness restarts, startup waits',
    'A liveness probe against a slow startup path is an infinite restart loop that looks like a crash',
    'Probe the process, not its dependencies — otherwise one slow database restarts your whole fleet',
    'No readiness probe means traffic goes to pods that have not finished starting, which quietly breaks zero-downtime deploys'] },

{ track:'containers', cat:'netns', level:'intermediate', cert:['cka:services-and-networking'],
  q:'Trace the path of a packet from a process inside a container to the internet.',
  a:'Process → eth0 inside the container, which is one end of a veth pair → the other end on a bridge on the host, docker0 or cni0 → the host routing table → iptables, where a MASQUERADE rule rewrites the source to the host address → the host NIC → the subnet route table → a NAT gateway or internet gateway. Every container network problem lives at one of those hops, and knowing the list is what turns "networking is broken" into four checkable places.',
  points:['veth pair is the virtual cable; the bridge is the virtual switch',
    'SNAT/MASQUERADE is why replies can find their way back to a private pod address',
    'On the AWS VPC CNI there is no overlay — pods get real VPC IPs and security groups apply directly',
    'net.ipv4.ip_forward and net.bridge.bridge-nf-call-iptables must both be 1 or this silently fails'] },

{ track:'containers', cat:'fw', level:'beginner', cert:['aws-saa:design-secure-architectures','sec+:security-architecture'],
  q:'Security group versus network ACL. Give me the difference and one practical consequence.',
  a:'A security group is stateful, allow-only, and attached to the ENI. A NACL is stateless, supports explicit deny, is attached to the subnet and evaluates numbered rules in order. The practical consequence: with a NACL you must allow the return traffic yourself, on ephemeral ports 1024-65535, because nothing tracks the connection. So the signature of a NACL problem is an outbound request that clearly leaves and a reply that never arrives. Security groups can also reference other security groups as a source, which is how you say "the app tier may reach the database tier" without hardcoding addresses.',
  points:['Stateful and allow-only versus stateless with deny',
    'ENI-level versus subnet-level',
    'Stateless means the reply needs its own inbound rule — the most common NACL mistake',
    'SG-referencing-SG expresses tiers; NACLs only understand CIDRs'] },

{ track:'containers', cat:'dns', level:'intermediate', cert:['cka:services-and-networking'],
  q:'Why is resolving an external hostname from inside a pod slower than you would expect?',
  a:'Because of `ndots:5` in the pod\'s resolv.conf. Any name with fewer than five dots is tried against every search-domain suffix first, so `api.example.com` becomes four failed cluster lookups before the real query. Fixes are a trailing dot to make the name absolute, a lower ndots in the pod\'s dnsConfig, or NodeLocal DNSCache so the failures are answered locally. It also matters at scale: thousands of short-lived UDP lookups put real pressure on conntrack and on CoreDNS.',
  points:['ndots:5 plus a three-entry search path means up to four wasted round trips',
    'A trailing dot makes a name fully qualified and skips the search path',
    'Test with the FQDN first — if that works and the short name does not, it is search path and not DNS',
    'NodeLocal DNSCache is the usual fix at scale'] },

{ track:'containers', cat:'images', level:'beginner', cert:['sec+:security-architecture'],
  q:'How would you reduce the size and the attack surface of a container image?',
  a:'Multi-stage build: compile in a full toolchain image, copy only the artefact into a minimal final stage. Use distroless or Alpine for that final stage so there is no shell and no package manager for an attacker to use. Run as a non-root UID with a read-only root filesystem and dropped capabilities. Order layers least-changing first so builds cache well, and clean package caches in the same RUN that created them — deleting a file in a later layer does not shrink the image, because the bytes are still in the earlier one. Add a `.dockerignore`, which is a security control as much as a speed one.',
  points:['Multi-stage: build tools never reach production',
    'USER non-root and readOnlyRootFilesystem, always',
    'Cleanup must happen in the same layer as the install',
    '.dockerignore stops .env and .aws/credentials being swept in by COPY . .',
    'Pin a digest rather than a tag so the tested image is the deployed image'] },

{ track:'containers', cat:'k8s', level:'intermediate', cert:['cka:workloads-and-scheduling'],
  q:'When would you use a StatefulSet instead of a Deployment?',
  a:'When identity or storage has to survive rescheduling. A StatefulSet gives stable ordinal names — api-0, api-1 — and a PersistentVolumeClaim per replica that follows that name, so a rescheduled pod reattaches its own volume. Deployments treat pods as interchangeable and give them random names. Use StatefulSet for databases, queues and anything with a quorum or a peer list. The cost is a strictly ordered rollout: one pod that never becomes ready blocks the entire update, silently and permanently.',
  points:['Stable network identity plus per-replica storage',
    'Ordered, one-at-a-time rollout — a feature and a trap',
    'Usually paired with a headless Service so each replica is individually addressable',
    'Its PVCs are deliberately not deleted with the StatefulSet'] },

{ track:'containers', cat:'k8s', level:'intermediate', cert:['cka:workloads-and-scheduling'],
  q:'You change a ConfigMap and nothing happens. Why, and what do you do?',
  a:'Because a pod that read its configuration from environment variables at startup keeps those values for its whole life — env is injected once, at container creation. The deployment stays healthy and serves stale configuration indefinitely, which is why this is such a persistent source of confusion. `kubectl rollout restart deploy/NAME` recreates the pods and picks up the change. ConfigMaps mounted as **files** do update in place, within a minute or so, if the application re-reads them.',
  points:['Environment variables are injected once; mounted files update in place',
    'rollout restart is the deliberate way to pick up a config change',
    'A checksum annotation on the pod template makes the rollout automatic — Helm does this',
    'The same applies to Secrets'] },

{ track:'containers', cat:'ops', level:'intermediate', cert:['aws-saa:design-resilient-architectures'],
  q:'ECS, EKS or AKS — how do you choose, and what do you own in each?',
  a:'Choose on operational burden rather than preference. ECS is AWS-native with task definitions instead of manifests and no control plane to run; on Fargate you own no nodes at all, which removes host patching and also removes daemonsets and node access. EKS and AKS give you a managed control plane but you still own nodes, the CNI, add-ons and version upgrades — real, recurring work. The case for Kubernetes is the ecosystem and portability: manifests move between clouds, ECS task definitions do not. For a small team with a handful of services, ECS on Fargate is genuinely the right answer more often than people admit.',
  points:['Fargate removes nodes, and with them daemonsets, node access and privileged agents',
    'EKS/AKS manage the control plane, not the nodes or the upgrades',
    'Portability is the real Kubernetes argument, not capability',
    'Name what you would own — "Kubernetes because it is standard" is the answer that gets probed'] },

{ track:'containers', cat:'storage', level:'intermediate', cert:['cka:storage'],
  q:'Explain the access modes on a PersistentVolume and how they constrain your design.',
  a:'ReadWriteOnce means one node at a time — that is what EBS and most block storage give you, and it is why two replicas cannot share a volume. ReadWriteMany means many nodes at once and needs a shared filesystem: EFS, Azure Files or NFS. ReadWriteOncePod is stricter still, one pod rather than one node. The consequence is architectural: a Deployment with three replicas and an RWO volume will not work, and the fix is either a StatefulSet with a volume each or genuinely shared storage.',
  points:['RWO is per node, not per pod — a common misreading',
    'RWX needs a filesystem, not a block device, and usually costs more',
    'Multi-Attach errors mean two pods want one RWO volume',
    'Block storage is zonal, so the volume and the node must be in the same zone'] },

{ track:'containers', cat:'fw', level:'intermediate', cert:['cka:services-and-networking','sec+:security-architecture'],
  q:'How do NetworkPolicies work, and what is the trap?',
  a:'They are additive and default-allow: with no policy selecting a pod, all traffic is permitted. The first policy that selects a pod flips it to deny-by-default for whichever directions appear in policyTypes, and from then on everything needs an explicit allow. Two traps. First, a default-deny egress policy blocks DNS on UDP 53 like anything else, so every name lookup in that namespace starts timing out while CoreDNS looks perfectly healthy. Second, they need a CNI that implements them — on flannel the manifests apply cleanly and do nothing at all, which is a security control that silently is not there.',
  points:['Default-allow until a policy selects the pod, then default-deny for that direction',
    'Always allow DNS to kube-system alongside any default-deny egress',
    'Verify the CNI enforces policy — flannel ignores it silently',
    'They are the cluster-internal equivalent of security groups, at pod granularity'] },

{ track:'containers', cat:'runtime', level:'intermediate', cert:['cka:troubleshooting'],
  q:'How do you debug a container built from a distroless image with no shell?',
  a:'`kubectl debug -it POD --image=nicolaka/netshoot --target=app` attaches an ephemeral container that shares the target\'s namespaces, so you get real tools looking at the real process. `--target` matters: without it the debug container has its own PID namespace and `ps` shows only itself. From the host, `nsenter -t PID -n` does the same thing using the node\'s tools, which is the reminder that a container is just a process in some namespaces. For anything serving traffic, `--copy-to` makes a copy of the pod instead of modifying the live one, because ephemeral containers cannot be removed.',
  points:['kubectl debug with --target shares the namespaces of a specific container',
    'nsenter from the host achieves the same and needs nothing installed in the image',
    'Ephemeral containers are permanent for the life of the pod — prefer --copy-to in production',
    'A distroless image is the right call; needing a debugging story is the cost'] },

{ track:'containers', cat:'ops', level:'intermediate', cert:['cka:cluster-architecture'],
  q:'How do you upgrade a Kubernetes cluster without an outage?',
  a:'Control plane first, one minor version at a time — you cannot skip versions, and the API server must never be older than the components talking to it. Then node groups: create new nodes on the new version, cordon and drain the old ones so workloads move gracefully, and let PodDisruptionBudgets hold evictions until replacements are ready. Check deprecated APIs before you start, because a removed API version turns a routine upgrade into a broken deploy an hour later. Upgrade add-ons — CNI, CoreDNS, CSI drivers — as their own step, since they have their own compatibility matrices.',
  points:['One minor version at a time, control plane before nodes',
    'Drain rather than terminate, and set PodDisruptionBudgets so it means something',
    'Audit removed API versions first — kubent or the API deprecation warnings',
    'Add-ons are separately versioned and are the usual source of surprises'] },

{ track:'containers', cat:'vpc', level:'intermediate', cert:['aws-saa:design-cost-optimized-architectures'],
  q:'Your EKS bill has a large NAT gateway line. What is causing it and what do you do?',
  a:'Every image pull, every S3 write and every call to an AWS API from a private subnet goes out through the NAT gateway, which charges per hour and per gigabyte. A gateway VPC endpoint for S3 and DynamoDB is free and removes most of it; interface endpoints handle ECR, CloudWatch and the rest at an hourly rate that is still usually cheaper than the data charge. Beyond that, one NAT gateway per AZ avoids cross-AZ data charges, and caching base images in ECR with a pull-through cache cuts the volume at source.',
  points:['Gateway endpoints for S3 and DynamoDB are free — do these first',
    'Interface endpoints for ECR keep image pulls off the NAT gateway',
    'One NAT gateway per AZ, or you pay cross-AZ on every packet',
    'A single NAT gateway is also a single-AZ dependency, which is a resilience question as well as a cost one'] },

{ track:'containers', cat:'k8s', level:'advanced', cert:['cka:cluster-architecture','sec+:security-architecture'],
  q:'How would you isolate two teams sharing one cluster?',
  a:'Namespaces plus RBAC for the API surface, ResourceQuota and LimitRange so neither can starve the other, and NetworkPolicy so pods cannot reach across. That covers the everyday case. It does **not** give you a security boundary: they still share a kernel, so a container escape crosses namespaces, and anyone with node access can enter any container. For genuine multi-tenancy you separate by node pool with taints, or by cluster. Being clear about which of those you are being asked for is most of the answer.',
  points:['Namespace + RBAC + quota + NetworkPolicy for everyday isolation',
    'A namespace is not a security boundary — same kernel, same nodes',
    'Node pools with taints, or separate clusters, for hostile tenants',
    'Ship a LimitRange before a ResourceQuota, or pods without requests are rejected outright'] },

{ track:'containers', cat:'images', level:'intermediate', cert:['sec+:security-architecture'],
  q:'How do you keep a supply chain of container images trustworthy?',
  a:'Pin base images by digest, not by tag, so what you tested is what ships. Scan on build and again on a schedule, because the image does not change but the CVE list does. Mirror external images into your own registry so you are not depending on someone else\'s uptime or rate limit. Sign images and verify signatures at admission with cosign and a policy controller. Generate an SBOM so that when the next widespread CVE lands you can answer "are we affected?" in minutes rather than days.',
  points:['Digests, not tags — tags are mutable pointers',
    'Rescanning matters more than scanning: the vulnerability list moves, the image does not',
    'Admission-time signature verification is what makes signing meaningful',
    'The SBOM question is asked during an incident, so build it before one'] },

{ track:'containers', cat:'runtime', level:'beginner', cert:['cka:troubleshooting'],
  q:'Why is logging to stdout a rule for containers rather than a preference?',
  a:'Because the runtime captures stdout and stderr and that is what `docker logs` and `kubectl logs` read. An application that writes to a file inside the container produces empty `kubectl logs`, and those logs die with the container — so on a crash loop the evidence is destroyed by the restart. Writing to stdout lets the node\'s log driver ship everything to CloudWatch, Loki or Splunk with no per-application configuration, and keeps the container filesystem ephemeral, which is what allows a pod to be replaced freely.',
  points:['The runtime only captures stdout and stderr',
    'Logs in the container die with the container — and a crash loop deletes them fastest',
    'kubectl logs --previous exists precisely because of that race',
    'It also keeps the writable layer small, which matters for node disk pressure'] }

);

/* ── Hazard questions: which of these would you not run here ── */
LX.dangerQs.push(

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:workloads-and-scheduling'],
  q:'Pods are being OOMKilled. An assistant suggests each of these. Which one would you never run in production?',
  choices:['kubectl delete pod --all -n production, to "clear the stuck state"',
    'kubectl top pod --containers to compare usage against the limit',
    'kubectl describe pod to read the exit code and Last State',
    'kubectl set resources deploy/app --limits=memory=1Gi after measuring'],
  a:0,
  why:'Deleting every pod in a namespace takes the whole service down at once, and it does not address a memory limit — the replacements are OOMKilled just as fast, with an outage added. It also destroys the evidence: the terminated container state and its logs go with the pods.' },

{ track:'containers', cat:'storage', level:'beginner', cert:['cka:storage'],
  q:'A build host is out of disk. Which of these is the dangerous one?',
  choices:['docker system prune -a --volumes -f',
    'docker system df, to see what is using the space',
    'docker builder prune, to clear the build cache',
    'docker image prune --filter "until=168h"'],
  a:0,
  why:'`--volumes` deletes every volume no *running* container references, which includes the database volume of a container you stopped ten minutes ago. Combined with `-f` there is no confirmation. Look with `docker system df` first, prune with an `until` filter, and never put `--volumes` in a cron job.' },

{ track:'containers', cat:'fw', level:'intermediate', cert:['cka:services-and-networking'],
  q:'You are asked to lock down a namespace. Which of these breaks it?',
  choices:['Apply a default-deny egress NetworkPolicy on its own',
    'Apply default-deny with an accompanying rule allowing UDP 53 to kube-system',
    'Apply default-deny ingress first and observe for a day',
    'Check which CNI is installed before writing any policy'],
  a:0,
  why:'A default-deny egress policy blocks DNS on UDP 53 like any other traffic, so every lookup in that namespace starts timing out while CoreDNS looks perfectly healthy and resolv.conf is correct. The DNS allow rule and the deny policy should ship as one template, never separately.' },

{ track:'containers', cat:'k8s', level:'intermediate', cert:['sec+:security-architecture'],
  q:'A container cannot write to a mounted volume. Which suggestion would you refuse?',
  choices:['Add privileged: true to the securityContext',
    'Set fsGroup so the volume is group-owned by the pod\'s group',
    'Check the volume\'s ownership with an initContainer that runs ls -l',
    'Confirm whether the root filesystem is mounted read-only by design'],
  a:0,
  why:'`privileged: true` gives the container effectively full access to the host — all capabilities, host devices, and a straightforward path to the node. It is a sledgehammer for a file-ownership problem, and `fsGroup` solves the actual issue. Privileged containers should need a written justification, not a debugging session.' },

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:troubleshooting'],
  q:'A deployment is failing and someone offers these. Which one loses the evidence you need?',
  choices:['kubectl delete pod, before reading logs --previous',
    'kubectl logs POD --previous --tail=100',
    'kubectl describe pod, to read Last State and the events',
    'kubectl get events --sort-by=.lastTimestamp'],
  a:0,
  why:'The terminated container and its logs are attached to that pod. Deleting it destroys the only copy of why the previous instance died, and the replacement starts with an empty history. Read `logs --previous` and `describe` first; the pod will still be there afterwards.' },

{ track:'containers', cat:'images', level:'beginner', cert:['sec+:security-architecture'],
  q:'Which of these is genuinely unsafe in a Dockerfile?',
  choices:['ARG DB_PASSWORD then RUN echo $DB_PASSWORD > /etc/app.conf',
    'USER 10001 near the end of the final stage',
    'COPY --from=build /out/app /app in a multi-stage build',
    'HEALTHCHECK CMD curl -f http://localhost:8080/health'],
  a:0,
  why:'Build arguments are recorded in the image history and are readable with `docker history` by anyone who can pull the image — the layer is baked in even if the file is deleted later. Secrets belong in a mounted secret at runtime, or in a BuildKit `--mount=type=secret` that leaves no layer behind.' },

{ track:'containers', cat:'svc', level:'intermediate', cert:['cka:services-and-networking'],
  q:'A Service has no endpoints. Which of these is the wrong move?',
  choices:['Recreate the Deployment to "reset" it',
    'kubectl get endpoints, then compare the selector to the pod labels',
    'kubectl describe svc to read the selector',
    'kubectl get pods --show-labels'],
  a:0,
  why:'Recreating the Deployment causes an outage and changes nothing: the new pods get the same labels from the same template, so the selector still misses them. This is a two-command diagnosis — the selector and the labels — and a one-command fix.' },

{ track:'containers', cat:'ops', level:'intermediate', cert:['cka:cluster-architecture'],
  q:'A node is NotReady. Which suggestion would you push back on?',
  choices:['kubectl delete node, to "force it to re-register"',
    'kubectl drain the node, then let the node group replace it',
    'Read the node conditions and the kubelet journal',
    'Check disk usage under /var/lib/containerd'],
  a:0,
  why:'Deleting the node object removes it from the API without touching the machine — which keeps running, may still hold volume attachments, and re-registers if the kubelet is alive. Pods are orphaned rather than gracefully moved. Drain first so workloads reschedule, then terminate the instance.' },

{ track:'containers', cat:'vpc', level:'intermediate', cert:['aws-saa:design-secure-architectures'],
  q:'A pod cannot reach an external API. Which fix would you reject?',
  choices:['Open 0.0.0.0/0 on all ports in the node security group',
    'Check whether a default-deny egress NetworkPolicy is in place',
    'Verify the subnet has a 0.0.0.0/0 route to a NAT gateway',
    'Test with a raw IP as well as a hostname to separate DNS from routing'],
  a:0,
  why:'Opening everything inbound to the internet to fix an *outbound* problem does not even address the symptom — security groups are stateful, so the reply to an allowed outbound connection is already permitted. It is a large, permanent exposure traded for no diagnostic value.' },

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:workloads-and-scheduling'],
  q:'Pods are Pending. Which of these costs money without fixing anything?',
  choices:['Scale the node group up before reading the FailedScheduling event',
    'kubectl describe pod and read why the scheduler refused',
    'Compare kubectl top nodes against describe node allocated requests',
    'Check the node taints and the pod tolerations'],
  a:0,
  why:'Roughly half of Pending pods are not a capacity problem at all — a taint, a nodeSelector or a zonal volume — and adding nodes changes nothing while adding cost. The event says which, and reading it takes ten seconds.' },

{ track:'containers', cat:'k8s', level:'intermediate', cert:['sec+:security-architecture'],
  q:'Which of these is not the answer to "the pod cannot read the file"?',
  choices:['Change the image to run as root',
    'Set fsGroup on the pod securityContext',
    'Check the file ownership inside the container with kubectl exec',
    'Confirm whether readOnlyRootFilesystem is set'],
  a:0,
  why:'Running as root undoes the most valuable single control in a container image and gives a container escape a much better outcome. The permission problem is solvable with fsGroup or by fixing the ownership at build time; "run as root" is how a debugging step becomes a permanent posture.' },

{ track:'containers', cat:'dns', level:'intermediate', cert:['cka:services-and-networking'],
  q:'Cluster DNS is failing in one namespace. Which action is premature?',
  choices:['Restart the CoreDNS deployment',
    'Test the fully qualified name to rule out the search path',
    'Read /etc/resolv.conf inside the affected pod',
    'Check kubectl get netpol -A for a default-deny policy'],
  a:0,
  why:'CoreDNS serves the whole cluster, so a failure in exactly one namespace almost certainly is not CoreDNS. Restarting it risks a brief cluster-wide DNS outage to test a hypothesis the symptom already contradicts — and the three checks below it cost nothing.' }

);

/* ── Recall ─────────────────────────────────────────────────── */
LX.quiz.push(

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:troubleshooting'],
  q:'A container exits with code 137. What happened?',
  choices:['It was killed with SIGKILL — usually the memory limit',
    'The application returned a non-zero status',
    'The image could not be pulled',
    'The liveness probe returned a 500'],
  a:0, why:'137 is 128 + 9, so SIGKILL. In a container that is nearly always the memory cgroup limit, and describe confirms it with Reason: OOMKilled.' },

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:troubleshooting'],
  q:'Which command shows the logs of the container instance that just crashed?',
  choices:['kubectl logs POD --previous', 'kubectl logs POD -f',
    'kubectl describe pod POD', 'kubectl get events'],
  a:0, why:'Without --previous you read the replacement container, which has not failed yet. The evidence belongs to the instance that already died.' },

{ track:'containers', cat:'svc', level:'beginner', cert:['cka:services-and-networking'],
  q:'A Service shows Endpoints: <none>. What are the only two possible causes?',
  choices:['The selector does not match the pod labels, or no pod is Ready',
    'The Ingress is misconfigured, or DNS is failing',
    'kube-proxy is down, or the node is NotReady',
    'The targetPort is wrong, or the namespace is wrong'],
  a:0, why:'Endpoints are built from pods that match the selector and pass readiness. Nothing else contributes to that list.' },

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:workloads-and-scheduling'],
  q:'Which decides where a pod is scheduled?',
  choices:['Its resource requests', 'Its resource limits',
    'Its actual CPU usage', 'The node with the most free memory'],
  a:0, why:'Requests are a reservation the scheduler adds up. Limits are enforced at runtime and play no part in placement, which is why a node at 10% usage can be unschedulable.' },

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:workloads-and-scheduling'],
  q:'A readiness probe fails. What happens to the pod?',
  choices:['It is removed from Service endpoints but keeps running',
    'The container is restarted', 'The pod is evicted from the node',
    'It is marked Pending and rescheduled'],
  a:0, why:'Readiness controls traffic; liveness controls life. That distinction is the most useful thing to know about probes.' },

{ track:'containers', cat:'runtime', level:'beginner', cert:['cka:workloads-and-scheduling'],
  q:'In docker run -p 8080:80, what does 80 refer to?',
  choices:['The port the process listens on inside the container',
    'The port clients connect to on the host',
    'The container\'s health-check port', 'The port the bridge network uses'],
  a:0, why:'The mapping is host:container. 8080 is the outside, 80 is inside — and the process must bind 0.0.0.0 in there, not 127.0.0.1.' },

{ track:'containers', cat:'fw', level:'beginner', cert:['aws-saa:design-secure-architectures'],
  q:'Which is true of a network ACL but not a security group?',
  choices:['It is stateless, so return traffic needs its own rule',
    'It is attached to the network interface',
    'It can reference another security group as a source',
    'It only supports allow rules'],
  a:0, why:'NACLs are stateless and subnet-level with explicit deny; security groups are stateful and ENI-level with allow only. The stateless part is what people forget.' },

{ track:'containers', cat:'netns', level:'intermediate', cert:['cka:services-and-networking'],
  q:'What gives a container its own network stack?',
  choices:['A network namespace', 'A cgroup', 'A seccomp profile', 'A separate kernel'],
  a:0, why:'Namespaces isolate what a process can see — network, mount, PID, UTS, IPC, user. Cgroups limit what it can consume. There is no separate kernel.' },

{ track:'containers', cat:'dns', level:'intermediate', cert:['cka:services-and-networking'],
  q:'Why does an external lookup from a pod try several names first?',
  choices:['ndots:5 means short names are tried against each search suffix',
    'CoreDNS retries failed queries three times',
    'The pod caches negative results', 'kube-proxy intercepts port 53'],
  a:0, why:'Any name with fewer than five dots is tried against every search-domain suffix before being tried as written. A trailing dot skips it.' },

{ track:'containers', cat:'storage', level:'intermediate', cert:['cka:storage'],
  q:'What does ReadWriteOnce actually restrict?',
  choices:['The volume can be mounted by one node at a time',
    'The volume can be written by one pod at a time',
    'The volume can only be written once',
    'The volume is read-only after the first write'],
  a:0, why:'One node, not one pod — several pods on the same node can share it. ReadWriteOncePod is the stricter mode that means one pod.' },

{ track:'containers', cat:'k8s', level:'intermediate', cert:['cka:workloads-and-scheduling'],
  q:'You edit a ConfigMap that is consumed as environment variables. What happens?',
  choices:['Nothing until the pods are restarted',
    'The variables update within about a minute',
    'The Deployment rolls out automatically',
    'The pods are marked NotReady until they re-read it'],
  a:0, why:'Environment variables are injected once at container creation. Mounted files do update in place; env does not, which is why rollout restart exists.' },

{ track:'containers', cat:'k8s', level:'beginner', cert:['cka:troubleshooting'],
  q:'A pod is Pending. What does that tell you?',
  choices:['The scheduler has not placed it on a node yet',
    'The image is being pulled', 'The container is starting',
    'The readiness probe has not passed'],
  a:0, why:'Pending means no node has been chosen, so nothing has been pulled and nothing has run. The FailedScheduling event says why.' },

{ track:'containers', cat:'images', level:'beginner', cert:['sec+:security-architecture'],
  q:'Why does deleting a file in a later Dockerfile layer not shrink the image?',
  choices:['The bytes are still present in the earlier layer',
    'Docker compresses layers only on push',
    'The build cache keeps a copy', 'Deletions require --squash to apply'],
  a:0, why:'Layers are additive and immutable; a deletion is just a whiteout marker in a later layer. Cleanup has to happen in the same RUN as the install.' },

{ track:'containers', cat:'svc', level:'intermediate', cert:['cka:services-and-networking'],
  q:'Why can you not ping a ClusterIP?',
  choices:['Nothing listens on it — it is an iptables or IPVS rule',
    'ICMP is blocked by default in every CNI',
    'ClusterIPs are IPv6 only', 'kube-proxy drops ICMP deliberately'],
  a:0, why:'A ClusterIP is a virtual address rewritten by a NAT rule on the calling node. There is no interface to answer ICMP, so a failed ping says nothing about health.' },

{ track:'containers', cat:'vpc', level:'beginner', cert:['aws-saa:design-secure-architectures'],
  q:'What makes a subnet public?',
  choices:['A route table entry sending 0.0.0.0/0 to an internet gateway',
    'A checkbox on the subnet', 'Instances in it having public IPs',
    'It being in the first availability zone'],
  a:0, why:'Nothing else distinguishes it. "Public subnet" is a property of the route table, not a setting on the subnet.' }

);
