/* Identity & M365 — the tooling layer. The job description is explicit that
   help-desk portal work does not meet the bar, and this is where that starts:
   Graph, the PowerShell SDK, and the parts of both that trip people up.      */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

{ name:'Connect-MgGraph', track:'entra', cat:'graph', level:'beginner',
  cert:['ms-102:identity','sc-300:identities'],
  sum:'Authenticate the Graph PowerShell SDK — and the scopes you ask for are the permissions you get.',
  syntax:'Connect-MgGraph -Scopes "Scope.One","Scope.Two" [-TenantId ID] [-Identity]',
  flags:[['-Scopes','Delegated permissions for this session; you get exactly what you ask for and nothing more'],
         ['-TenantId','Required when your account exists in several tenants, which is normal for consultants'],
         ['-Identity','Use a managed identity — how this runs unattended in Azure Automation'],
         ['-ClientSecretCredential / -CertificateThumbprint','App-only authentication for scripts; certificate over secret every time']],
  ex:[['Connect-MgGraph -Scopes "User.Read.All","AuditLog.Read.All","Policy.Read.All"','A read-only investigation session'],
      ['Get-MgContext | Select-Object Account,TenantId,Scopes','What you are actually connected as — the first thing to check when a call 403s'],
      ['Connect-MgGraph -TenantId $t -ClientId $a -CertificateThumbprint $c','Unattended, with no secret to leak']],
  tip:'A `Forbidden` from a Graph cmdlet is far more often a missing scope than a missing role — `Get-MgContext` shows what you actually hold, and reconnecting with the extra scope fixes it. Understand the two permission types before you debug anything: **delegated** permissions act as the signed-in user and are capped by that user\'s own rights, while **application** permissions act as the app itself with no user and no cap. A script that works interactively and fails as a scheduled job is nearly always that distinction.',
  related:['Get-MgContext','Invoke-MgGraphRequest','Find-MgGraphCommand'] },

{ name:'Get-MgContext', track:'entra', cat:'graph', level:'beginner',
  cert:['ms-102:identity'],
  sum:'Who you are, which tenant, and which scopes — the identity check before any policy reading.',
  syntax:'Get-MgContext [| Select-Object -ExpandProperty Scopes]',
  flags:[['.Account','The signed-in user, or the app id under app-only auth'],
         ['.TenantId','The tenant you are actually in, which is not always the one you meant'],
         ['.Scopes','Every delegated permission this session holds'],
         ['.AuthType','Delegated or AppOnly — the distinction behind most "works for me" reports']],
  ex:[['Get-MgContext','Account, tenant, auth type and scopes in one object'],
      ['(Get-MgContext).Scopes -contains "Policy.Read.All"','Check a specific scope before blaming the API'],
      ['Get-MgContext | Select-Object Account,TenantId,AuthType']],
  tip:'This is the `sts get-caller-identity` of the Microsoft world and it belongs at the top of every investigation. Two failure modes it settles instantly: you are connected to the wrong tenant, which is normal when you administer several, and you connected without the scope the cmdlet needs, so the API is answering correctly and the error is yours. Note that scopes are granted at connect time — adding one means reconnecting, not retrying.',
  related:['Connect-MgGraph','Get-MgUser'] },

{ name:'Invoke-MgGraphRequest', track:'entra', cat:'graph', level:'intermediate',
  cert:['ms-102:identity','sc-300:identities'],
  sum:'Call any Graph endpoint directly, including beta and anything the SDK has not wrapped.',
  syntax:'Invoke-MgGraphRequest -Method GET -Uri "https://graph.microsoft.com/v1.0/…"',
  flags:[['-Uri','Full URL, or a relative path — /beta reaches things v1.0 does not expose'],
         ['-Method GET|POST|PATCH|DELETE','The full verb set, so this covers writes too'],
         ['-Body','A hashtable or JSON string for POST and PATCH'],
         ['-OutputType PSObject','Friendlier than the default hashtable when you are piping into Select-Object']],
  ex:[['Invoke-MgGraphRequest -Method GET -Uri "https://graph.microsoft.com/v1.0/me" -OutputType PSObject','The simplest possible connectivity test'],
      ['Invoke-MgGraphRequest -Method GET -Uri "https://graph.microsoft.com/beta/identity/conditionalAccess/policies" -OutputType PSObject','Beta properties the SDK cmdlet does not surface'],
      ['Invoke-MgGraphRequest -Method GET -Uri "https://graph.microsoft.com/v1.0/auditLogs/signIns?`$filter=status/errorCode ne 0&`$top=50" -OutputType PSObject','Raw query when the cmdlet\'s filtering fights you']],
  tip:'Reach for this when an SDK cmdlet is missing a property or mangling a filter — the SDK is generated from the same API, so anything it can do this can do, and several things it cannot. Two practical notes: in PowerShell the `$` in an OData parameter must be backtick-escaped inside a double-quoted string, and beta endpoints are genuinely unstable, so a script that depends on one needs a comment saying why.',
  related:['Connect-MgGraph','Find-MgGraphCommand','Get-MgAuditLogSignIn'] },

{ name:'Find-MgGraphCommand', track:'entra', cat:'graph', level:'beginner',
  cert:['ms-102:identity'],
  sum:'Map between Graph URIs and SDK cmdlets, and find the permissions either one needs.',
  syntax:'Find-MgGraphCommand -Command NAME | -Uri PATH [-ApiVersion v1.0|beta]',
  flags:[['-Command Get-MgUser','What URI it calls and which permissions it accepts'],
         ['-Uri /users/{id}/licenseDetails','The other direction: which cmdlet covers this endpoint'],
         ['| Select-Object -ExpandProperty Permissions','The scope list to pass to Connect-MgGraph']],
  ex:[['Find-MgGraphCommand -Command Get-MgUser | Select-Object -First 1 -ExpandProperty Permissions','Exactly which scopes that cmdlet will accept'],
      ['Find-MgGraphCommand -Uri "/identity/conditionalAccess/policies" -ApiVersion v1.0','Which cmdlet wraps that endpoint'],
      ['Find-MgGraphCommand -Command "*SignIn*" | Select-Object Command,Method,Uri','Discovery when you do not know the cmdlet name']],
  tip:'This is how you stop guessing at scopes. Connecting with a broad set like `Directory.ReadWrite.All` because something failed is the habit worth breaking — it grants far more than the task needs and it will be a finding in any access review. Look up the minimum permission, request that, and note it in the script header so the next person does not have to rediscover it.',
  related:['Connect-MgGraph','Invoke-MgGraphRequest'] },

{ name:'Connect-ExchangeOnline', track:'entra', cat:'exo', level:'beginner',
  cert:['ms-102:exchange'],
  sum:'The other module. Exchange Online is a separate control plane from Graph and always has been.',
  syntax:'Connect-ExchangeOnline [-UserPrincipalName UPN] [-AppId ID -CertificateThumbprint T -Organization DOMAIN]',
  flags:[['-UserPrincipalName','Interactive, with modern auth and MFA'],
         ['-AppId / -CertificateThumbprint / -Organization','Certificate-based app-only — the supported unattended path'],
         ['-ShowBanner:$false','Quiet enough for a scheduled job'],
         ['Disconnect-ExchangeOnline','Sessions are a limited resource; leaking them exhausts the tenant quota']],
  ex:[['Connect-ExchangeOnline -UserPrincipalName admin@contoso.com','Interactive session'],
      ['Connect-ExchangeOnline -AppId $app -CertificateThumbprint $thumb -Organization contoso.onmicrosoft.com','Unattended, certificate only'],
      ['Get-ConnectionInformation | Select-Object UserPrincipalName,State,TokenStatus','Whether the session is still alive']],
  tip:'Exchange Online administration does not go through Graph and its cmdlets do not respect Graph scopes — a script that manages both needs both connections, and the permissions are granted in two different places. The other thing to know is throttling: EXO enforces per-tenant limits, so a loop calling `Get-Mailbox` per user will be slowed or cut off. Fetch in bulk with `-ResultSize Unlimited` and filter locally rather than calling once per object.',
  related:['Get-Mailbox','Get-MessageTrace','Connect-MgGraph'] },

{ name:'Get-MgAuditLogSignIn', track:'entra', cat:'mfa', level:'intermediate',
  cert:['ms-102:identity','sc-300:access'],
  sum:'The sign-in log — the single most useful object in Entra, and the answer to most "cannot log in" tickets.',
  syntax:'Get-MgAuditLogSignIn -Filter "…" [-Top N]',
  flags:[['-Filter "userPrincipalName eq \'a@b.com\'"','Server-side; the log is far too large to page through'],
         ['-Filter "status/errorCode ne 0"','Failures only'],
         ['-Filter "createdDateTime ge 2026-08-27T00:00:00Z"','Bound the window, always'],
         ['.Status.FailureReason / .Status.ErrorCode','The AADSTS code and its text — the actual answer'],
         ['.AppliedConditionalAccessPolicies','Every CA policy evaluated, and its result']],
  ex:[['Get-MgAuditLogSignIn -Filter "userPrincipalName eq \'jo@contoso.com\' and status/errorCode ne 0" -Top 20 | Select-Object CreatedDateTime,AppDisplayName,@{n=\'Err\';e={$_.Status.ErrorCode}},@{n=\'Why\';e={$_.Status.FailureReason}}','Failed sign-ins with the reason'],
      ['(Get-MgAuditLogSignIn -Filter "id eq \'…\'").AppliedConditionalAccessPolicies | Select-Object DisplayName,Result','Which CA policy said no'],
      ['Get-MgAuditLogSignIn -Filter "status/errorCode eq 53003" -Top 50 | Group-Object {$_.UserPrincipalName} | Sort-Object Count -Descending','Who is being blocked by Conditional Access, ranked']],
  tip:'`AppliedConditionalAccessPolicies` is the field that turns a CA investigation from guesswork into reading — every policy evaluated for that attempt is listed with a result of success, failure, notApplied or notEnabled, so you can see exactly which one blocked it and which ones did not apply. Retention is the constraint: thirty days with Entra ID P1 or P2 and only seven days without, so anything older has to have been shipped to a Log Analytics workspace or a SIEM before you needed it.',
  related:['Get-MgIdentityConditionalAccessPolicy','Get-MgAuditLogDirectoryAudit'] },

{ name:'Get-MgAuditLogDirectoryAudit', track:'entra', cat:'entra', level:'intermediate',
  cert:['ms-102:identity','sc-300:access'],
  sum:'Who changed what in the directory — the CloudTrail of Entra.',
  syntax:'Get-MgAuditLogDirectoryAudit -Filter "…" [-Top N]',
  flags:[['-Filter "activityDisplayName eq \'Update conditional access policy\'"','Change events by name'],
         ['-Filter "initiatedBy/user/userPrincipalName eq \'a@b.com\'"','Everything one person did'],
         ['.TargetResources','What was changed, including old and new values for many activities'],
         ['.Result','success or failure — failed attempts matter as much as successful ones']],
  ex:[['Get-MgAuditLogDirectoryAudit -Filter "activityDisplayName eq \'Update conditional access policy\'" -Top 10 | Select-Object ActivityDateTime,@{n=\'By\';e={$_.InitiatedBy.User.UserPrincipalName}},@{n=\'What\';e={$_.TargetResources.DisplayName}}','Recent CA policy edits and who made them'],
      ['(Get-MgAuditLogDirectoryAudit -Top 1 -Filter "…").TargetResources.ModifiedProperties | Select-Object DisplayName,OldValue,NewValue','The actual before and after'],
      ['Get-MgAuditLogDirectoryAudit -Filter "activityDisplayName eq \'Add member to role\'" -Top 20','Role assignments, which is where privilege escalation shows up']],
  tip:'`ModifiedProperties` carries `OldValue` and `NewValue` for most directory changes, which makes this both the who-did-it log and the diff — a combination AWS needs two services to provide. It is the first place to look when a policy "changed itself", and the honest answer is usually a Conditional Access template, a pipeline, or an administrator who did not realise a template applied broadly. Same retention limits as the sign-in log.',
  related:['Get-MgAuditLogSignIn','Get-MgIdentityConditionalAccessPolicy'] },

{ name:'Search-UnifiedAuditLog', track:'entra', cat:'purview', level:'advanced',
  cert:['ms-102:security','sc-300:access'],
  sum:'The tenant-wide audit log — mailbox access, file activity, admin actions, all of Microsoft 365.',
  syntax:'Search-UnifiedAuditLog -StartDate D -EndDate D [-Operations OP…] [-UserIds UPN…]',
  flags:[['-Operations MailItemsAccessed,FileDownloaded,Add-MailboxPermission','The specific activities; the operation list is enormous'],
         ['-RecordType ExchangeItem,SharePointFileOperation','Filter by workload instead'],
         ['-ResultSize 5000 -SessionCommand ReturnLargeSet','Paging; the default result size is small and silently truncates'],
         ['-FreeText','Substring search across the record when you do not know the operation name']],
  ex:[['Search-UnifiedAuditLog -StartDate (Get-Date).AddDays(-7) -EndDate (Get-Date) -Operations MailItemsAccessed -UserIds jo@contoso.com','Whose mailbox was read, and by whom'],
      ['Search-UnifiedAuditLog -StartDate … -EndDate … -Operations "Add-MailboxPermission","New-InboxRule" | Select-Object CreationDate,UserIds,Operations','The two operations that show up in almost every mailbox compromise'],
      ['(Search-UnifiedAuditLog … ).AuditData | ForEach-Object { $_ | ConvertFrom-Json }','The detail lives in AuditData as a JSON string']],
  tip:'This is the log that answers "was the data actually accessed", which is the question an incident review and a breach-notification decision both turn on. Two hard constraints: auditing must have been enabled before the events — it is on by default now but not retroactively — and retention depends on licensing, ninety days on E3 and up to a year on E5 or with an add-on. The single highest-value query in a suspected mailbox compromise is `New-InboxRule` and `Set-InboxRule`, because attackers create forwarding rules almost immediately.',
  related:['Get-MgAuditLogSignIn','Get-Mailbox','Get-MessageTrace'] }

);
