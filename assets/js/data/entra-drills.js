/* Identity & M365 — drills, hazard questions, read-the-output items and recall.
   The hazard set is unusually rich here because almost every identity mistake
   looks like helpfulness: unblocking someone, removing a prompt, granting a
   permission. That is what makes it the AI-verification track.              */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };
LX.drills = LX.drills || [];
LX.quiz = LX.quiz || [];
LX.dangerQs = LX.dangerQs || [];
LX.outputQs = LX.outputQs || [];

/* ── Drills ─────────────────────────────────────────────────── */
LX.drills.push(

{ track:'entra', cat:'ca', level:'advanced', cert:['sc-300:access','ms-102:identity'],
  q:'How do Conditional Access policies combine, and what does that mean for debugging?',
  a:'Additively. Every policy whose conditions match must be satisfied, and there is no allow that overrides another policy\'s block — adding a policy can only ever restrict. Exclusions are the only escape hatch, which is why break-glass accounts must be excluded from every policy rather than granted something. For debugging this is good news: the sign-in log records `AppliedConditionalAccessPolicies` with a result of success, failure, notApplied or notEnabled for every policy evaluated, so you can name the one that blocked instead of reasoning across twenty.',
  points:['Additive — every applicable policy must pass; no policy grants access',
    'AppliedConditionalAccessPolicies names the deciding policy in the sign-in log',
    'notApplied is useful: it rules a policy out rather than leaving it a suspect',
    'Report-only evaluates and records without blocking, which is the safe rollout'] },

{ track:'entra', cat:'ca', level:'intermediate', cert:['sc-300:access'],
  q:'What is a break-glass account, and what makes one actually correct?',
  a:'A cloud-only account on the tenant\'s .onmicrosoft.com domain — cloud-only so it does not depend on the sync server or an on-premises identity provider — with a long random password held offline, excluded from every Conditional Access policy, assigned Global Administrator permanently rather than through PIM, and alerted on whenever it signs in. Two of them, so losing or compromising one does not lock you out. And tested on a schedule, because an untested break-glass account is a belief rather than a control.',
  points:['Cloud-only, so it survives a sync or federation outage',
    'Excluded from every CA policy — the exclusion is the whole point',
    'Two accounts, credentials split and stored offline',
    'Alert on any sign-in, and test on a schedule or you do not know it works'] },

{ track:'entra', cat:'apps', level:'intermediate', cert:['sc-300:apps','ms-102:identity'],
  q:'Explain the difference between delegated and application permissions, and when each is wrong.',
  a:'Delegated permissions act on behalf of a signed-in user and the effective access is the intersection of the app\'s permission and that user\'s own rights — so a delegated `User.ReadWrite.All` held by an ordinary user still cannot edit other users. Application permissions act as the app itself with no user involved and no such cap, which is why they always require admin consent. A daemon or scheduled job has no user, so it needs application permissions; a script that works when a developer runs it interactively and fails as a scheduled task is nearly always holding delegated ones.',
  points:['Delegated = on behalf of a user, capped by that user\'s rights',
    'Application = as the app, uncapped, always needs admin consent',
    'A daemon with delegated permissions fails the moment there is no user',
    'Over-granting application permissions is how a reporting job becomes a tenant-wide risk'] },

{ track:'entra', cat:'apps', level:'advanced', cert:['ms-102:security','sc-300:apps'],
  q:'What is an illicit consent grant attack, and why does a password reset not stop it?',
  a:'The user is phished into approving an application that requests permissions such as Mail.Read, Mail.Send and offline_access. No credential is stolen — the user genuinely consented — and the attacker then calls Graph with a legitimate token, refreshed indefinitely because of offline_access. A password reset and even a session revocation do not touch the grant, so access continues. The fix is revoking the OAuth2 permission grant and the app\'s tokens. The prevention is a user consent policy limiting consent to verified publishers and low-impact permissions, plus the admin consent request workflow.',
  points:['No password is involved, so credential remediation does nothing',
    'offline_access is what makes it persistent',
    'Revoke the grant, not just the session',
    'Restrict user consent and enable the admin consent workflow before it happens'] },

{ track:'entra', cat:'hybrid', level:'advanced', cert:['ms-102:identity','az-104:identity'],
  q:'What is source of authority in a hybrid tenant, and what goes wrong when people ignore it?',
  a:'Whichever directory masters the object. An object with `onPremisesSyncEnabled` is mastered in Active Directory, so a cloud-side change is either rejected or silently reverted at the next sync cycle — by default within thirty minutes. The failure this produces is the worst kind: the change appears to work, the ticket is closed, and it reopens a day later with nobody able to explain it. A leaver disabled in the cloud but still enabled on-premises is the canonical case. Some attributes are exceptions where writeback is configured, which makes it more confusing rather than less.',
  points:['Synced object means AD wins; check onPremisesSyncEnabled before editing anything',
    'Cloud edits to synced objects are reverted, not rejected — so they look successful',
    'Password writeback and device writeback are deliberate exceptions',
    'Default sync interval is thirty minutes, which is also how long the illusion lasts'] },

{ track:'entra', cat:'lifecycle', level:'intermediate', cert:['ms-102:identity','sc-300:identities'],
  q:'Design a leaver process. What has to happen, and in what order?',
  a:'Disable the account first, then revoke sign-in sessions — without that, an already-issued token keeps working for up to an hour and a refresh token far longer, which is exactly the gap that matters in a hostile departure. Then remove group memberships that grant standing access, including any PIM eligibility. Then convert the mailbox to shared so the mail survives without a licence and reclaim the licence. Then check for forwarding rules and delegated mailbox access, because those outlive the account. If the user is synced, every step that touches the account happens in Active Directory, not the cloud.',
  points:['Disable then revoke — they are two different actions and both are required',
    'Convert the mailbox to shared before removing the licence, or the data goes',
    'PIM eligibility and group membership are standing access that outlives disabling',
    'Synced users are disabled on-premises; a cloud disable is reverted'] },

{ track:'entra', cat:'rbac', level:'advanced', cert:['sc-300:access','ms-102:identity'],
  q:'Someone shows you a tenant with two Global Administrators and says privilege is under control. What do you check?',
  a:'Whether PIM is in use, because `Get-MgDirectoryRoleMember` shows only active assignments and misses everyone who is merely eligible — which in a well-run tenant is most of the privilege. Then whether eligibility is actually constrained: approval required, justification required, MFA on activation, and a short maximum duration. Eligibility with no approval and an eight-hour window is standing privilege with extra steps. Then service principals, which hold application permissions nobody counts as administrators, and any role assignable groups, whose membership is a route to a role.',
  points:['Active assignments are only half the picture — check eligibility schedules too',
    'Approval, justification, MFA and duration are what make eligibility meaningful',
    'Service principals with Directory.ReadWrite.All are effectively administrators',
    'Role-assignable group membership is privilege, and it is easy to miss'] },

{ track:'entra', cat:'mfa', level:'intermediate', cert:['sc-300:identities','ms-102:identity'],
  q:'A user has lost their phone and cannot complete MFA. What do you do?',
  a:'Verify their identity out of band — a video call, their manager, an established process — because a lockout report is a standard social-engineering opening. Then issue a Temporary Access Pass: time-boxed, optionally single-use, fully audited, and it lets them sign in once and register a new method themselves. What you do not do is remove the MFA requirement, exclude them from the policy, or reset the password and read it out, because each of those is exactly what an attacker with a stolen password is asking for. Afterwards, check the method they registered is not SMS-only.',
  points:['Verify identity out of band first — the process is the control',
    'Temporary Access Pass restores access without weakening anything',
    'Never remove the requirement or add a policy exclusion for one person',
    'Steer registration towards Authenticator or FIDO2, not SMS'] },

{ track:'entra', cat:'lic', level:'intermediate', cert:['ms-102:identity'],
  q:'Compare direct and group-based licensing. What breaks with each?',
  a:'Group-based scales and is the right default: entitlement follows membership, so joiners and leavers are handled by the group rather than by a person remembering. What breaks is that you cannot repair an individual on the user object — the licence has to be fixed at the group or the tenant — and two groups granting the same service plan produce a MutuallyExclusiveViolation. Running out of a SKU produces a CountViolation recorded silently per user. Direct assignment is fine for a handful of exceptions and terrible at scale, because nothing ever removes it.',
  points:['LicenseAssignmentStates shows the source and any error',
    'CountViolation means the tenant is out; MutuallyExclusiveViolation means overlap',
    'A group-assigned licence cannot be removed on the user',
    'Assigned is not provisioned — each service plan has its own status'] },

{ track:'entra', cat:'exo', level:'intermediate', cert:['ms-102:exchange','ms-102:security'],
  q:'What do you check first in a suspected mailbox compromise, and why those things?',
  a:'Inbox rules and mailbox forwarding, because attackers create them within minutes and they survive a password reset — a rule named with a single character that forwards externally and deletes the original means the user never sees replies to mail sent in their name. Then registered authentication methods, in case the attacker added their own. Then OAuth consent grants, which is the quietest mechanism of all. Then the unified audit log for MailItemsAccessed to establish what was actually read, which is the question a breach-notification decision turns on.',
  points:['Rules and forwarding first — minutes to check, and they outlive the credential',
    'A newly registered authentication method means they can pass MFA themselves',
    'Consent grants survive password reset and session revocation both',
    'MailItemsAccessed answers "was it read", if auditing was on beforehand'] },

{ track:'entra', cat:'intune', level:'intermediate', cert:['ms-102:devices'],
  q:'How do Intune compliance and Conditional Access interact, and how does that go wrong?',
  a:'Intune evaluates compliance at device check-in and writes the state to the device object; Conditional Access reads that state as a grant control. They are separate systems joined by a stale value, which produces two failure modes. A tightened compliance rule with no grace period flips devices to non-compliant immediately and Conditional Access locks them out on the next sign-in. And a device that has stopped checking in keeps its last reported state, so it can stay compliant long after it should not, or stay blocked after it has been fixed. The fast rollback lever is the CA policy, because compliance changes wait for check-in.',
  points:['Intune judges, Conditional Access enforces — separate systems, delayed join',
    'Grace period on the scheduled action is what makes a rollout survivable',
    'Roll back at the CA policy; compliance changes take a check-in cycle',
    'A device not checking in is unmanaged in practice, whatever its state says'] },

{ track:'entra', cat:'fed', level:'advanced', cert:['sc-300:apps','ms-102:identity'],
  q:'A SaaS application is integrated with SAML and SCIM. Which is which, and what fails where?',
  a:'SAML asserts identity at sign-in: the user authenticates to Entra and Entra sends an assertion the application trusts. SCIM provisions the account in advance: Entra pushes create, update and disable operations to the application\'s user directory. They fail independently, and that produces the classic case of a user who authenticates perfectly and lands on "no account found" — SAML worked and SCIM never created them. The provisioning job also goes into quarantine after repeated failures, usually an expired credential on the target, and then retries only daily while joiners silently stop appearing.',
  points:['SAML is authentication at sign-in; SCIM is account lifecycle beforehand',
    'A perfect sign-in with no account at the far end means provisioning, not auth',
    'Quarantine backs the job off to daily retries and fails quietly',
    'Deprovisioning matters as much: SCIM is what disables the account on a leaver'] },

{ track:'entra', cat:'graph', level:'intermediate', cert:['ms-102:identity'],
  q:'A Graph cmdlet returns Forbidden. How do you work out why?',
  a:'`Get-MgContext` first — it shows the account, the tenant, the auth type and every scope the session holds, and the answer is usually a missing scope rather than a missing role. Scopes are granted at connect time, so adding one means reconnecting rather than retrying. `Find-MgGraphCommand -Command <name>` gives the minimum permissions that cmdlet accepts, which is how you request the right one instead of reaching for Directory.ReadWrite.All. If the scope is present, then it is a directory role or, for app-only auth, a missing application permission and admin consent.',
  points:['Get-MgContext is the get-caller-identity of this world',
    'Scopes are fixed at connect time — reconnect, do not retry',
    'Find-MgGraphCommand gives the minimum permission for the cmdlet',
    'Delegated permission is still capped by the signed-in user\'s own rights'] },

{ track:'entra', cat:'entra', level:'advanced', cert:['sc-300:identities','ms-102:security'],
  q:'Which tenant-wide defaults would you tighten first in an unfamiliar tenant, and why those?',
  a:'The authorization policy defaults. `AllowedToCreateApps` means every user can register an application and mint credentials for it, which is a service principal nobody governs. User consent means one phished person can grant an external app access to their mail. `AllowedToCreateSecurityGroups` matters wherever groups grant access or licences. And guest invitation settings plus guest access permissions decide how much of the directory an external account can read — by default, more than people expect. Each of these is low-risk to tighten, but read current usage first, because some self-service flows depend on them.',
  points:['User app registration creates ungoverned service principals with credentials',
    'User consent is the enabler for consent phishing — restrict to verified publishers',
    'Guests can read more of the directory by default than most people assume',
    'Check what currently depends on each setting before changing it'] },

{ track:'entra', cat:'mfa', level:'advanced', cert:['sc-300:identities','ms-102:security'],
  q:'Rank the authentication methods, and explain what "phishing-resistant" actually buys you.',
  a:'FIDO2 security keys, passkeys and Windows Hello for Business are phishing-resistant because the credential is bound to the origin and never leaves the authenticator — an attacker-in-the-middle proxy relaying a real sign-in page gets nothing usable, which is what defeats modern phishing kits. Authenticator with number matching and additional context is next: it stops MFA fatigue because approving requires reading a number from the sign-in screen. Then Authenticator push without number matching, then TOTP codes, then SMS — which is vulnerable to SIM swap and interception and should be a fallback, not a standard.',
  points:['Phishing-resistant means origin-bound, so a relay proxy cannot use it',
    'Number matching is what kills MFA fatigue attacks',
    'SMS is the weakest common method — SIM swap and interception',
    'Registration campaigns and the registration report move a tenant up this list'] },

{ track:'entra', cat:'ca', level:'intermediate', cert:['sc-300:access'],
  q:'You are asked to require MFA for everyone by Friday. How do you do it without an outage?',
  a:'Check the registration report first and find who has no method — those people cannot satisfy the policy and will be locked out rather than prompted, producing 50079. Run a registration campaign for them. Create the policy in report-only with the break-glass accounts excluded, leave it for several days, and read the sign-in logs for who would have failed. Handle service accounts and legacy protocol clients explicitly, since neither can do interactive MFA. Then enable it, watch the logs for the first hour, and keep the rollback — flipping back to report-only — one command away.',
  points:['Registration first: an unregistered user is blocked, not prompted',
    'Report-only, then read the logs, then enforce — no exceptions to that order',
    'Service principals and legacy clients need a deliberate answer, not an exclusion',
    'Break-glass exclusions before enabling, always'] },

{ track:'entra', cat:'purview', level:'advanced', cert:['ms-102:security','sc-400:information-protection'],
  q:'When do you reach for DLP and when for sensitivity labels?',
  a:'DLP inspects content in place and acts on movement — it detects patterns and blocks or warns when something is shared, mailed or uploaded. Sensitivity labels attach classification, and optionally encryption, that travels with the file, so a labelled document sent to a personal address is still unreadable without an authorised identity. Use labels for genuinely controlled data where protection has to survive leaving the tenant, and DLP for detection, blocking and evidence. Roll DLP out in test mode with notifications first, because a credit card rule matches far more than credit cards and a straight-to-block deployment destroys trust in the whole programme.',
  points:['DLP inspects and blocks movement; labels attach protection that travels',
    'A policy is a container — the conditions and actions live in the rules',
    'Test mode with notifications, and read the detail report before enforcing',
    'Encryption breaks some downstream processing, so it is a deliberate choice'] },

{ track:'entra', cat:'entra', level:'intermediate', cert:['ms-102:identity'],
  q:'What is the difference between a managed and a federated domain, and why does it matter at 3am?',
  a:'A managed domain means Entra validates the credential itself, using password hash sync or pass-through authentication. A federated domain redirects the user to an external identity provider — AD FS or a third party — which authenticates them and returns an assertion, so Entra never sees the password. It matters because it decides where a failure has to be debugged: with federation, a sign-in failure may never appear as a credential error in Entra at all, and chasing Conditional Access wastes an hour while the actual outage is an expired token-signing certificate on an AD FS server. It also decides your blast radius: federation adds an on-premises dependency to every cloud sign-in.',
  points:['Get-MgDomain AuthenticationType tells you which you are dealing with',
    'Federated means an on-premises dependency for every sign-in',
    'AD FS token-signing certificate expiry is a classic total outage',
    'Converting between them is tenant-wide and affects every user at once'] },

{ track:'entra', cat:'graph', level:'advanced', cert:['ms-102:identity','sc-300:access'],
  q:'How long do you have to investigate an identity incident, and what do you do about it?',
  a:'Sign-in and directory audit logs are thirty days with Entra ID P1 or P2 and seven days without. The unified audit log is ninety days on E3 and up to a year on E5 or with an add-on, and it only contains what auditing was enabled for at the time. Those windows are all shorter than the median time to detect an intrusion, so the answer is to ship the logs continuously into a Log Analytics workspace or a SIEM with your own retention — a decision made long before the incident. During one, capture what you need immediately rather than assuming it will still be there next week.',
  points:['30 days sign-in logs with P1/P2, 7 without; 90 days unified audit on E3',
    'Auditing is not retroactive — absence of records is not evidence of absence',
    'Diagnostic settings to Log Analytics or a SIEM is the only durable answer',
    'Export evidence during the incident, not after the review is scheduled'] },

{ track:'entra', cat:'lifecycle', level:'advanced', cert:['sc-300:identities','ms-102:identity'],
  q:'How would you find standing access nobody remembers granting?',
  a:'Four places. Directory role members and PIM eligibility, including role-assignable groups. Service principals with application permissions — `Directory.ReadWrite.All` on a forgotten integration is an administrator by another name. OAuth2 permission grants with `AllPrincipals` consent, which apply to every user in the tenant. And guest accounts, which accumulate from every project and rarely leave. Then bring in usage: Access Advisor and `signInActivity` turn "has permission" into "has used it", and access reviews put a recurring decision in front of an owner instead of leaving the list to grow.',
  points:['Roles, eligibility, service principals, consent grants, guests — all five',
    'An app with Directory.ReadWrite.All is an administrator nobody counts',
    'Usage data is what makes least privilege defensible rather than theoretical',
    'Access reviews turn a one-off cleanup into a recurring control'] }

);

/* ── Hazard questions ───────────────────────────────────────── */
LX.dangerQs.push(

{ track:'entra', cat:'mfa', level:'beginner', cert:['sc-300:identities'],
  q:'A user cannot complete MFA and is escalating. An assistant offers these. Which do you refuse?',
  choices:['Remove the MFA requirement for them temporarily, and re-enable it later',
    'Verify their identity out of band, then issue a Temporary Access Pass',
    'Check which authentication methods they have registered',
    'Read the sign-in log to see whether the password succeeded first'],
  a:0,
  why:'This is the exact request an attacker with a stolen password makes, and "re-enable it later" depends on someone remembering. A Temporary Access Pass restores access in the same amount of time, is time-boxed and single-use, is fully audited, and lets the user register a new method themselves — with no control removed at any point.' },

{ track:'entra', cat:'ca', level:'intermediate', cert:['sc-300:access'],
  q:'A Conditional Access policy is blocking a team. Which response makes things worse?',
  choices:['Add the affected users to the policy\'s exclusion list',
    'Flip the policy to report-only while you work out what it does',
    'Read AppliedConditionalAccessPolicies to confirm which policy is blocking',
    'Check whether the grant control is something those users can satisfy'],
  a:0,
  why:'Exclusions added under pressure are permanent in practice — nobody audits them, and the policy quietly stops covering the people it was written for. Report-only restores access just as fast, keeps the telemetry that tells you what the policy would have done, and leaves you with a policy you can fix rather than one full of holes.' },

{ track:'entra', cat:'apps', level:'intermediate', cert:['sc-300:apps'],
  q:'A script is failing with a 403 from Graph. Which suggestion would you push back on?',
  choices:['Grant it Directory.ReadWrite.All so the permission question goes away',
    'Use Find-MgGraphCommand to get the minimum permission the cmdlet needs',
    'Check whether it holds delegated permissions but runs with no user',
    'Confirm admin consent was granted, not just requested'],
  a:0,
  why:'Directory.ReadWrite.All on a reporting job lets it modify every object in the tenant, including privileged role membership. It closes the ticket and creates an application-permission holder nobody counts as an administrator — which is exactly what an attacker looks for. The minimum permission takes one lookup to find.' },

{ track:'entra', cat:'hybrid', level:'intermediate', cert:['ms-102:identity'],
  q:'A leaver is still enabled in the cloud although they were disabled in AD. Which action is wrong?',
  choices:['Disable the account in Entra so it takes effect immediately',
    'Check the tenant-wide last sync time to see whether sync has stopped',
    'Confirm the object is actually synced, with onPremisesSyncEnabled',
    'Revoke their sign-in sessions, which works regardless of sync state'],
  a:0,
  why:'A synced object is mastered in Active Directory, so a cloud disable is reverted at the next cycle — and the worst part is that it appears to work, so the ticket closes and reopens a day later. Session revocation is the action that genuinely helps immediately, because it applies to tokens rather than to the object.' },

{ track:'entra', cat:'exo', level:'intermediate', cert:['ms-102:exchange'],
  q:'A supplier\'s invoices keep going to quarantine. Which fix is the bad one?',
  choices:['Add the sender to the allow list so their mail always bypasses filtering',
    'Check whether their SPF or DKIM is failing and tell them to fix it',
    'Look at whether a transport rule matched rather than the spam filter',
    'Release the message and review why it scored as it did'],
  a:0,
  why:'A permanent allow entry disables authentication checking for exactly the domain most worth spoofing — and invoice fraud works by impersonating a supplier you trust. If they fail SPF, that is a real problem on their side that affects everyone they mail, and telling them is more useful than papering over it on yours.' },

{ track:'entra', cat:'apps', level:'advanced', cert:['ms-102:security'],
  q:'You find an OAuth grant giving an unknown app Mail.Read. Which action is insufficient on its own?',
  choices:['Reset the user\'s password and close the ticket',
    'Revoke the OAuth2 permission grant',
    'Revoke the user\'s sign-in sessions and refresh tokens',
    'Check the unified audit log for MailItemsAccessed by that application'],
  a:0,
  why:'The grant does not depend on the password — the user genuinely consented, and the app holds its own refresh token. Resetting the credential leaves the attacker reading mail through Graph exactly as before. Revoking the grant is what cuts the access, and the audit log is what tells you how much was read before you did.' },

{ track:'entra', cat:'rbac', level:'advanced', cert:['sc-300:access'],
  q:'Cleaning up privileged access. Which action would you not take without checking first?',
  choices:['Remove everyone from Global Administrator except the two you recognise',
    'List PIM eligibility as well as active role assignments',
    'Check which service principals hold application permissions',
    'Review role-assignable group membership'],
  a:0,
  why:'Some of those assignments are break-glass accounts, automation identities, or the only person who can administer a workload — and removing them can lock the tenant out or break a nightly job with no obvious link. Identify the owner and purpose of each first, move them to PIM eligibility rather than deleting, and keep at least two working administrators throughout.' },

{ track:'entra', cat:'ca', level:'advanced', cert:['sc-300:access'],
  q:'You are enabling a new Conditional Access policy for the whole tenant. Which step would you never skip?',
  choices:['Excluding the break-glass accounts before the policy is enabled',
    'Naming the policy after what it does',
    'Telling the service desk it is going out',
    'Documenting it in the change record'],
  a:0,
  why:'The others are good practice; this one is the difference between a rollback and a support case with Microsoft. A policy targeting All users with no exclusions catches the accounts you would use to fix it. The exclusion has to exist before the policy is enabled — creating it during the incident is exactly what you cannot do once you are locked out.' },

{ track:'entra', cat:'intune', level:'intermediate', cert:['ms-102:devices'],
  q:'A compliance policy is blocking a fleet. Which response is the wrong lever?',
  choices:['Delete the compliance policy so devices go back to compliant',
    'Put the Conditional Access policy into report-only',
    'Check the grace period on the scheduled action',
    'Look at the per-policy compliance state to see which rule is failing'],
  a:0,
  why:'Deleting it is slow and destructive: each device still has to check in before its state changes, so access does not come back quickly, and you have lost the policy and its history. The Conditional Access policy is the fast lever — report-only restores access in minutes while the compliance work is done properly.' },

{ track:'entra', cat:'lic', level:'beginner', cert:['ms-102:identity'],
  q:'Reclaiming licences from leavers. Which order is dangerous?',
  choices:['Remove the licence first, then decide what to do with the mailbox',
    'Convert the mailbox to shared, then remove the licence',
    'Check whether the licence is group-assigned before trying to remove it',
    'Confirm the account is disabled and sessions revoked first'],
  a:0,
  why:'Removing a licence starts a grace period after which the mailbox and its contents are deleted. Convert to shared first — a shared mailbox needs no licence and keeps up to fifty gigabytes accessible — and only then reclaim. Getting the order wrong is a data-loss incident that presents weeks later when somebody needs the mail.' },

{ track:'entra', cat:'graph', level:'intermediate', cert:['ms-102:identity'],
  q:'Writing a script that will run unattended. Which choice would you avoid?',
  choices:['Store a client secret in the pipeline variables and rotate it yearly',
    'Use a certificate credential so the private key never travels',
    'Use a managed identity where the script runs in Azure',
    'Request the minimum application permission the script actually needs'],
  a:0,
  why:'A secret in a pipeline variable gets copied into logs, config files and chat messages, and yearly rotation guarantees an annual outage nobody has diarised. A certificate cannot be used by someone who only saw it, and a managed identity removes the credential entirely — there is nothing to leak or expire.' },

{ track:'entra', cat:'purview', level:'advanced', cert:['ms-102:security'],
  q:'Rolling out DLP for credit card data. Which approach causes the most damage?',
  choices:['Enable it in blocking mode across all locations on day one',
    'Run it in test mode with notifications and read the detail report',
    'Scope it to one workload first and widen once the false-positive rate is known',
    'Check what the sensitive information type actually matches before enabling'],
  a:0,
  why:'A credit card rule matches far more than credit cards — order references, test data, product codes — and blocking on day one stops legitimate work across the company. Worse, it destroys trust in the programme, so the next control is resisted on principle. Test mode gives you the false-positive rate before anyone is affected.' },

{ track:'entra', cat:'entra', level:'advanced', cert:['sc-300:identities'],
  q:'Tightening tenant defaults. Which change would you not make without investigating first?',
  choices:['Set AllowedToCreateApps to false across the tenant immediately',
    'Check the audit log for how many app registrations users have created recently',
    'Restrict user consent to verified publishers and low-impact permissions',
    'Enable the admin consent request workflow before restricting consent'],
  a:0,
  why:'The change is right in principle and it can break real self-service flows — developer onboarding, self-registered integrations, tooling that creates its own registration. Look at what has actually been created in the last few months first, and enable the admin consent workflow so there is a path for legitimate requests before you close the current one.' },

{ track:'entra', cat:'mfa', level:'advanced', cert:['sc-300:identities','ms-102:security'],
  q:'A user reports MFA prompts they did not trigger. Which response is inadequate?',
  choices:['Tell them to decline the prompts and wait for it to stop',
    'Revoke their sessions and reset the password immediately',
    'Check for inbox rules, forwarding and newly registered authentication methods',
    'Enable number matching so approval requires reading a number from the screen'],
  a:0,
  why:'Those prompts mean someone already has the password — the first factor succeeded. Waiting gives the attacker unlimited attempts against a tired human, which is exactly how MFA fatigue works. Contain immediately, then hunt for what they set up while trying, because a rule or a consent grant survives every credential change.' }

);

/* ── Read the output ────────────────────────────────────────── */
LX.outputQs.push(

{ track:'entra', cat:'mfa', level:'intermediate', cmd:'Get-MgAuditLogSignIn -Filter "…" -Top 1',
  out:'ErrorCode     : 53003\nFailureReason : Access has been blocked by Conditional Access policies.\nClientAppUsed : Browser',
  q:'What does this tell you about the password?',
  choices:['It was accepted — the block came after authentication',
    'It was wrong','It has expired','Nothing at all'],
  a:0, why:'53003 is a Conditional Access refusal to issue a token, which happens after the credential is validated. A wrong password would be 50126. That distinction matters: if the user did not initiate this, the password is valid and possibly known to somebody else.' },

{ track:'entra', cat:'ca', level:'intermediate', cmd:'(Get-MgAuditLogSignIn …).AppliedConditionalAccessPolicies',
  out:'DisplayName                  Result\n---------------------------  ----------\nRequire MFA for all users    success\nBlock legacy authentication  notApplied\nRequire compliant device     failure',
  q:'Which policy blocked this sign-in?',
  choices:['Require compliant device','Require MFA for all users','Block legacy authentication','All three together'],
  a:0, why:'`failure` names the policy that blocked. `success` means it was satisfied, and `notApplied` means the conditions did not match this attempt — which rules that policy out entirely. Policies are additive, so any single failure blocks the sign-in.' },

{ track:'entra', cat:'apps', level:'intermediate', cmd:'# error body from a scheduled Graph job',
  out:'AADSTS7000215: Invalid client secret provided. Ensure the secret being sent in\nthe request is the client secret value, not the client secret ID.',
  q:'What is the most likely cause?',
  choices:['The client secret expired','Admin consent was revoked','The app lost its application permission','The tenant id is wrong'],
  a:0, why:'7000215 is an authentication failure on the credential itself, and for a job that ran fine for a year the overwhelmingly common cause is an expired secret. A revoked consent or missing permission would be a 403, and a wrong tenant would be 90002.' },

{ track:'entra', cat:'lic', level:'intermediate', cmd:'(Get-MgUser -Property licenseAssignmentStates).LicenseAssignmentStates',
  out:'SkuId           : 05e9a617-…\nAssignedByGroup : 7d2a91c4-…\nState           : Error\nError           : CountViolation',
  q:'What do you do?',
  choices:['Free up or buy units of that SKU — the tenant has run out',
    'Assign the licence directly to the user','Remove them from the group','Wait for provisioning to finish'],
  a:0, why:'CountViolation means there are no free units, so group-based licensing could not assign it. Assigning directly fails for the same reason. Reclaiming from disabled accounts is usually enough, and an alert at ninety percent consumption prevents the next one.' },

{ track:'entra', cat:'hybrid', level:'advanced', cmd:'Get-ADSyncScheduler',
  out:'SyncCycleEnabled      : True\nStagingModeEnabled    : True\nNextSyncCyclePolicyType : Delta',
  q:'Why are on-premises changes not reaching the cloud?',
  choices:['The server is in staging mode, so it exports nothing',
    'The scheduler is disabled','The delta cycle is too infrequent','The connector account expired'],
  a:0, why:'Staging mode imports and synchronises but exports nothing at all. It is the correct state for a standby server, and a failover that was started and never completed leaves a server looking entirely healthy while changing nothing in the tenant.' },

{ track:'entra', cat:'exo', level:'beginner', cmd:'Get-InboxRule -Mailbox jo@contoso.com',
  out:'Name  Enabled  ForwardTo               DeleteMessage  MoveToFolder\n----  -------  ----------------------  -------------  ------------\n.     True     ext-collect@mailbox.top  True          RSS Feeds',
  q:'What are you looking at?',
  choices:['A post-compromise persistence rule',
    'A normal user-created filter','An Exchange transport rule','A DLP policy action'],
  a:0, why:'A single-character name, external forwarding, delete the original and move to a folder nobody reads — this is the standard shape of an attacker rule. It survives a password reset because it lives in the mailbox, so it has to be removed explicitly.' },

{ track:'entra', cat:'intune', level:'intermediate', cmd:'…ScheduledActionsForRule.ScheduledActionConfigurations',
  out:'GracePeriodHours : 0\nActionType       : block',
  q:'What happens when a device fails this policy?',
  choices:['It is marked non-compliant immediately, and Conditional Access blocks the next sign-in',
    'The user gets two days to fix it','Nothing until an administrator acts','It is quarantined in Intune'],
  a:0, why:'Zero grace means the state flips the moment the device reports a failure, and any Conditional Access policy requiring compliance turns that into a lockout on the next sign-in. This one setting is behind most fleet-wide compliance incidents.' },

{ track:'entra', cat:'apps', level:'advanced', cmd:'Get-MgOauth2PermissionGrant -All -Filter "principalId eq \'…\'"',
  out:'ClientId    : 0a4f91b2-…\nConsentType : Principal\nScope       : Mail.Read Mail.Send offline_access',
  q:'Why will a password reset not stop this?',
  choices:['The app holds its own refresh token through offline_access',
    'The grant is tenant-wide','The user is a Global Administrator','The app is Microsoft-owned'],
  a:0, why:'No password is involved — the user consented, and offline_access gives the app a refresh token it can renew indefinitely. Resetting the credential and even revoking sessions leave the grant intact; revoking the grant is what cuts the access.' }

);

/* ── Recall ─────────────────────────────────────────────────── */
LX.quiz.push(

{ track:'entra', cat:'ca', level:'intermediate', cert:['sc-300:access'],
  q:'How do multiple Conditional Access policies combine?',
  choices:['Additively — every applicable policy must be satisfied',
    'The most permissive one wins','The first matching one wins','The one with the lowest priority number wins'],
  a:0, why:'There is no allow that overrides a block and no ordering. Every policy whose conditions match must pass, so adding a policy can only restrict. Exclusions are the only escape.' },

{ track:'entra', cat:'ca', level:'beginner', cert:['sc-300:access'],
  q:'What does report-only mode do?',
  choices:['Evaluates the policy and records the result without blocking anyone',
    'Applies the policy to a pilot group','Logs sign-ins but not policy results','Disables the policy entirely'],
  a:0, why:'The policy is evaluated against real sign-ins and the outcome is written to AppliedConditionalAccessPolicies, so you get the exact list of who would have been affected before affecting them.' },

{ track:'entra', cat:'mfa', level:'intermediate', cert:['ms-102:identity'],
  q:'AADSTS50076 appears in the sign-in log. What happened?',
  choices:['The password was accepted and MFA is required',
    'The password was wrong','The user is not assigned to the app','A Conditional Access policy blocked it'],
  a:0, why:'50076 is a strong-authentication requirement, which is only reached after the first factor succeeds. A wrong password is 50126, an unassigned app is 50105, and a CA block is 53003.' },

{ track:'entra', cat:'mfa', level:'intermediate', cert:['sc-300:identities'],
  q:'A user has lost their MFA device. What is the correct remediation?',
  choices:['Issue a Temporary Access Pass after verifying identity out of band',
    'Exclude them from the MFA policy','Reset their password','Remove their registered methods and let them sign in without MFA'],
  a:0, why:'A Temporary Access Pass is time-boxed, optionally single-use, audited, and lets them register a new method. Every other option removes a control, and a lockout report is a standard social-engineering opening.' },

{ track:'entra', cat:'apps', level:'intermediate', cert:['sc-300:apps'],
  q:'A daemon with no signed-in user needs which kind of permission?',
  choices:['Application permissions, with admin consent',
    'Delegated permissions','Either works','A directory role'],
  a:0, why:'Delegated permissions act on behalf of a signed-in user and there is not one. Application permissions act as the app itself, are not capped by any user, and therefore always require admin consent.' },

{ track:'entra', cat:'apps', level:'beginner', cert:['ms-102:identity'],
  q:'What is AADSTS7000215?',
  choices:['An invalid client secret','A missing application permission','A blocked Conditional Access policy','An unassigned enterprise application'],
  a:0, why:'It is a credential failure on the app registration, and for a job that has run for months the usual cause is a secret that expired. It also catches sending the secret id instead of the secret value.' },

{ track:'entra', cat:'apps', level:'intermediate', cert:['sc-300:apps'],
  q:'What is the difference between an application and a service principal?',
  choices:['The application is the global definition; the service principal is its local instance in your tenant',
    'They are the same object with two names','The service principal is the app registration','The application is the enterprise app'],
  a:0, why:'You assign users to, consent on, disable and see sign-ins for the service principal. Every enterprise app in the portal is a service principal; the application object lives in its home tenant.' },

{ track:'entra', cat:'hybrid', level:'intermediate', cert:['ms-102:identity'],
  q:'A user has onPremisesSyncEnabled set to true. Where do you disable them?',
  choices:['In Active Directory','In Entra ID','Either — the change syncs both ways','In Exchange Online'],
  a:0, why:'Active Directory is the source of authority for a synced object, so a cloud disable is reverted at the next cycle — and it appears to succeed first, which is what makes it expensive.' },

{ track:'entra', cat:'hybrid', level:'advanced', cert:['ms-102:identity'],
  q:'What does staging mode do on an Entra Connect server?',
  choices:['It imports and synchronises but exports nothing',
    'It pauses all synchronisation','It syncs only passwords','It runs a full import every cycle'],
  a:0, why:'It is for standby servers and migrations. A server left in staging after a failover looks completely healthy while making no changes in the tenant at all.' },

{ track:'entra', cat:'lifecycle', level:'intermediate', cert:['sc-300:identities'],
  q:'You disable a user. How long can they keep working?',
  choices:['Until the issued access token expires, typically up to an hour',
    'Immediately — access stops at once','Until they sign out','Until the next sync cycle'],
  a:0, why:'Disabling blocks new sign-ins; it does not invalidate tokens already issued. Revoke-MgUserSignInSession invalidates the refresh tokens, which is what actually closes the window.' },

{ track:'entra', cat:'lic', level:'intermediate', cert:['ms-102:identity'],
  q:'What does a CountViolation error on a licence assignment mean?',
  choices:['The tenant has run out of units of that SKU',
    'Two licences conflict','The user is in too many groups','The service is still provisioning'],
  a:0, why:'Group-based licensing records it per user and continues silently, so the first symptom is usually a new starter missing a service days later. An alert at ninety percent consumption removes the whole failure mode.' },

{ track:'entra', cat:'lic', level:'beginner', cert:['ms-102:identity'],
  q:'A licence is assigned by a group. How do you remove it from one user?',
  choices:['Remove them from the group','Remove it directly on the user','Disable the service plan','You cannot remove it at all'],
  a:0, why:'A group-assigned licence cannot be removed on the user object — the assignment is a property of membership, so membership is what you change.' },

{ track:'entra', cat:'exo', level:'beginner', cert:['ms-102:exchange'],
  q:'A message trace returns no record at all. What does that mean?',
  choices:['The message never reached Exchange Online',
    'It was quarantined','It was deleted by a rule','It is still in transit'],
  a:0, why:'No record means it never arrived, so the problem is upstream — MX records, the sending system, or a connector. Quarantined and rule-deleted messages both produce records.' },

{ track:'entra', cat:'exo', level:'intermediate', cert:['ms-102:exchange'],
  q:'What does StopRuleProcessing on a transport rule do?',
  choices:['Prevents any lower-priority rule from running on that message',
    'Stops the rule from running','Pauses mail flow','Quarantines the message'],
  a:0, why:'Rules evaluate in ascending priority order and this truncates the chain, which is why a correct-looking rule can be entirely unreachable. Reading them sorted by priority is the only way to see it.' },

{ track:'entra', cat:'intune', level:'intermediate', cert:['ms-102:devices'],
  q:'What is AADSTS53000?',
  choices:['The device is not compliant','The user is not assigned to the app','MFA is required','The device is not registered at all'],
  a:0, why:'It means a Conditional Access policy required a compliant device and Intune reported this one as non-compliant. 53001 is the related case of a device that is not hybrid joined at all.' },

{ track:'entra', cat:'intune', level:'beginner', cert:['ms-102:devices'],
  q:'What is the difference between a compliance policy and a configuration profile?',
  choices:['A compliance policy checks and reports; a configuration profile pushes settings',
    'They are the same thing','Compliance applies to users, configuration to devices','Configuration profiles only work on Windows'],
  a:0, why:'Expecting a compliance policy to fix a device is a common and expensive misunderstanding — it only judges. The profile is what actually changes the device.' },

{ track:'entra', cat:'rbac', level:'advanced', cert:['sc-300:access'],
  q:'You list Global Administrator members and find two people. What might you be missing?',
  choices:['Everyone eligible through PIM, who does not appear in the member list',
    'Nothing — that is the complete list','Guests, which are listed separately','Users in nested groups only'],
  a:0, why:'Active assignments are only half the picture. In a tenant using PIM properly, most privilege is eligibility, which requires the role eligibility schedule to see.' },

{ track:'entra', cat:'graph', level:'beginner', cert:['ms-102:identity'],
  q:'A Graph cmdlet returns Forbidden. What do you check first?',
  choices:['Get-MgContext, for the scopes the session actually holds',
    'The user\'s directory roles','The tenant\'s licence count','The Conditional Access policies'],
  a:0, why:'It is far more often a missing scope than a missing role, and scopes are fixed at connect time — so the fix is reconnecting with the right one rather than retrying.' },

{ track:'entra', cat:'entra', level:'intermediate', cert:['ms-102:identity'],
  q:'What does a federated domain mean for a sign-in failure?',
  choices:['Entra never saw the password — debug the identity provider',
    'Conditional Access blocked it','The user is not synced','MFA was required'],
  a:0, why:'A federated domain redirects authentication to AD FS or a third party. The failure may never appear as a credential error in Entra at all, and an expired token-signing certificate is a classic total outage.' },

{ track:'entra', cat:'mfa', level:'advanced', cert:['sc-300:identities'],
  q:'Which authentication method is phishing-resistant?',
  choices:['A FIDO2 security key','Microsoft Authenticator push','A TOTP code from an app','SMS to a registered number'],
  a:0, why:'The credential is bound to the origin and never leaves the authenticator, so an attacker-in-the-middle proxy relaying a real sign-in page gets nothing usable. Number matching improves Authenticator but does not make it origin-bound.' }

);
