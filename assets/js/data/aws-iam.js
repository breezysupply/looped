/* AWS track — identity, policy evaluation, keys and secrets. The evaluation
   order in the `simulate-principal-policy` tip is the single most valuable
   thing in this file; everything else is how to read one input to it.        */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

/* ── Who am I ───────────────────────────────────────────────── */
{ name:'aws sts get-caller-identity', track:'aws', cat:'iam', level:'beginner',
  cert:['aws-saa:secure-architectures','aws-sec:identity-and-access'],
  sum:'The first command of every AWS debugging session, without exception.',
  syntax:'aws sts get-caller-identity [--profile NAME]',
  flags:[['--query Arn --output text','Just the ARN, which is the part you needed'],
         ['--profile NAME','Prove which profile is really in effect, rather than which one you think is']],
  ex:[['aws sts get-caller-identity','Account, user id, and the ARN you are acting as'],
      ['aws sts get-caller-identity --query Arn --output text','arn:aws:sts::111122223333:assumed-role/deploy/i-0a1b'],
      ['aws sts get-caller-identity --profile prod --query Account --output text','Confirm the account before running anything destructive']],
  tip:'Half of "AccessDenied but the policy is right" turns out to be the wrong identity, and this settles it in one call. The precedence is `--profile`, then `AWS_PROFILE`, then explicit `AWS_ACCESS_KEY_ID` environment variables, then the shared credentials file, then the instance or container role — and an exported environment variable silently beating a `--profile`-less command is the everyday version of this bug. Read the ARN carefully too: `assumed-role/deploy/session` is the role, and the policy you should be reading is the role\'s, not the user\'s who assumed it.',
  related:['aws sts assume-role','aws iam simulate-principal-policy'] },

{ name:'aws sts assume-role', track:'aws', cat:'iam', level:'intermediate',
  cert:['aws-saa:secure-architectures','aws-sec:identity-and-access'],
  sum:'Cross-account and elevated access, as a temporary credential rather than a key.',
  syntax:'aws sts assume-role --role-arn ARN --role-session-name NAME',
  flags:[['--role-session-name','Appears in every CloudTrail record — make it a human or a ticket, not "session1"'],
         ['--duration-seconds','Up to the role\'s MaxSessionDuration, one hour by default'],
         ['--external-id','Required by third-party trust policies to prevent the confused-deputy problem'],
         ['--policy','A session policy: intersects with the role\'s permissions, never adds to them']],
  ex:[['aws sts assume-role --role-arn arn:aws:iam::444455556666:role/Audit --role-session-name alex-ticket-4821 --query Credentials','Three values to export'],
      ['aws sts assume-role --role-arn arn:aws:iam::444455556666:role/Audit --role-session-name alex --duration-seconds 3600 --query \'Credentials.[AccessKeyId,SecretAccessKey,SessionToken]\' --output text','The form a script can consume'],
      ['aws configure set profile.audit.role_arn arn:aws:iam::444455556666:role/Audit && aws configure set profile.audit.source_profile default','Let the CLI do it for you every time instead']],
  tip:'Assuming a role needs permission on **both** sides: an `sts:AssumeRole` allow in your identity policy, and a trust policy on the role naming your principal. A denial that mentions "is not authorized to perform: sts:AssumeRole on resource: role/X" is the identity side; one that says the role "cannot be assumed by" you is the trust side, and only the target account can fix it. Session policies are an intersection, so passing `--policy` can only narrow — it is a useful way to hand out a scoped credential you cannot escalate.',
  related:['aws sts get-caller-identity','aws iam get-role'] },

/* ── Reading policy ─────────────────────────────────────────── */
{ name:'aws iam simulate-principal-policy', track:'aws', cat:'iam', level:'advanced',
  cert:['aws-sec:identity-and-access','aws-saa:secure-architectures'],
  sum:'Ask IAM whether an action would be allowed, and which statement decides it.',
  syntax:'aws iam simulate-principal-policy --policy-source-arn ARN --action-names ACTION… [--resource-arns ARN…]',
  flags:[['--action-names s3:GetObject','One or many; the answer is per action'],
         ['--resource-arns','Without it the simulation runs against `*` and can look more permissive than reality'],
         ['--context-entries','Supply condition keys such as aws:SourceIp or aws:PrincipalTag/team'],
         ['--query \'EvaluationResults[].[EvalActionName,EvalDecision,MatchedStatements[].SourcePolicyId]\'','Decision plus the policy that produced it']],
  ex:[['aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::111122223333:role/deploy --action-names s3:PutObject --resource-arns arn:aws:s3:::artifacts/app.zip --query \'EvaluationResults[].[EvalDecision,MatchedStatements[].SourcePolicyId]\'','Allowed or denied, and by what'],
      ['aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::111122223333:role/deploy --action-names kms:Decrypt --resource-arns arn:aws:kms:us-east-1:111122223333:key/abc --context-entries ContextKeyName=aws:SourceIp,ContextKeyType=ip,ContextKeyValues=10.0.4.31','With a condition key supplied'],
      ['aws iam simulate-custom-policy --policy-input-list file://new-policy.json --action-names s3:DeleteObject --resource-arns \'arn:aws:s3:::prod-data/*\'','Test a policy before it exists']],
  tip:'**Evaluation order, and it is worth memorising in this exact sequence:** an explicit `Deny` anywhere wins immediately; then Service Control Policies must allow it (they only ever restrict, they never grant); then a resource-based policy is evaluated, and within one account an allow there is sufficient on its own (cross-account, both sides must allow); then the identity policy must allow it; then a permissions boundary must also allow it; then any session policy must also allow it. Everything is denied by default, so "no statement matched" and "denied" are the same outcome. The simulator does **not** evaluate SCPs or resource policies in every case — so a green result here with a real denial in practice points at exactly those two, which is a genuinely useful signal rather than a bug.',
  related:['aws iam get-role','aws organizations list-policies-for-target'] },

{ name:'aws iam get-role', track:'aws', cat:'iam', level:'beginner',
  cert:['aws-sec:identity-and-access'],
  sum:'The trust policy — who is allowed to become this role — plus its boundary.',
  syntax:'aws iam get-role --role-name NAME',
  flags:[['--query Role.AssumeRolePolicyDocument','The trust policy, which is a different thing from its permissions'],
         ['--query Role.PermissionsBoundary','The ceiling, if one is set — invisible in most consoles'],
         ['--query Role.MaxSessionDuration','Why a long-running job loses its credentials after an hour']],
  ex:[['aws iam get-role --role-name deploy --query Role.AssumeRolePolicyDocument','Who can assume it, and under what conditions'],
      ['aws iam get-role --role-name deploy --query \'[Role.PermissionsBoundary,Role.MaxSessionDuration]\'','The two fields people forget exist'],
      ['aws iam list-attached-role-policies --role-name deploy --query \'AttachedPolicies[].[PolicyName,PolicyArn]\' --output table','The managed policies attached to it']],
  tip:'A role has two policies that are constantly confused. The **trust policy** answers "who may assume this role" and lives on the role itself; the **permissions policies** answer "what can it then do". A `sts:AssumeRole` failure is always the first; an `AccessDenied` on an API call is always the second. The permissions boundary is a third thing again — a maximum, not a grant — and a role with `AdministratorAccess` attached and a boundary allowing only S3 can do exactly S3, which is a genuinely confusing five minutes if you do not know to look.',
  related:['aws iam list-attached-role-policies','aws iam simulate-principal-policy'] },

{ name:'aws iam list-attached-role-policies', track:'aws', cat:'iam', level:'beginner',
  cert:['aws-sec:identity-and-access'],
  sum:'What is attached — and the inline policies that this command does not show.',
  syntax:'aws iam list-attached-role-policies --role-name NAME',
  flags:[['list-role-policies --role-name NAME','Inline policies; a completely separate list'],
         ['get-role-policy --role-name NAME --policy-name NAME','The document of one inline policy'],
         ['get-policy-version --policy-arn ARN --version-id v3','A managed policy has versions, and the default is not always the newest']],
  ex:[['aws iam list-attached-role-policies --role-name deploy --query \'AttachedPolicies[].PolicyName\' --output text','Managed policies'],
      ['aws iam list-role-policies --role-name deploy --query PolicyNames --output text','Inline policies — easy to miss and often where the real grant is'],
      ['aws iam get-policy-version --policy-arn arn:aws:iam::111122223333:policy/deploy --version-id $(aws iam get-policy --policy-arn arn:aws:iam::111122223333:policy/deploy --query Policy.DefaultVersionId --output text) --query PolicyVersion.Document','The document actually in force']],
  tip:'Three separate places grant a role permissions and a review that checks only the first is not a review: attached managed policies, inline policies, and — for resources like S3 buckets and KMS keys — the resource\'s own policy in the other direction. Managed policies are versioned with up to five versions and one marked default, so reading "the policy" without resolving `DefaultVersionId` can show you a document that has not been in effect for months.',
  related:['aws iam get-role','aws iam get-account-authorization-details'] },

{ name:'aws iam get-account-authorization-details', track:'aws', cat:'iam', level:'advanced',
  cert:['aws-sec:identity-and-access'],
  sum:'Every user, group, role and policy document in the account, in one response.',
  syntax:'aws iam get-account-authorization-details [--filter Role User Group LocalManagedPolicy]',
  flags:[['--filter Role','Narrow it; the unfiltered response in a real account is enormous'],
         ['--query \'RoleDetailList[].[RoleName,length(AttachedManagedPolicies)]\'','Roles ranked by how much is attached']],
  ex:[['aws iam get-account-authorization-details --filter Role > iam-snapshot.json','The whole identity surface, for offline analysis'],
      ['aws iam get-account-authorization-details --filter Role --query \'RoleDetailList[?contains(to_string(AttachedManagedPolicies),`AdministratorAccess`)].RoleName\'','Every role with admin — the first question in any review'],
      ['aws iam generate-service-last-accessed-details --arn arn:aws:iam::111122223333:role/deploy','Which services this role has actually used']],
  tip:'This is the command behind every serious IAM audit and behind most evidence requests: one call produces the complete policy graph, which can then be diffed release over release or fed to a tool. Pair it with Access Advisor (`generate-service-last-accessed-details`, then `get-service-last-accessed-details`) to turn "has permission" into "has used the permission in the last year" — that gap is where least-privilege work actually starts, and it is defensible to an auditor in a way that guesswork is not.',
  related:['aws iam list-attached-role-policies','aws iam get-credential-report'] },

{ name:'aws iam get-credential-report', track:'aws', cat:'iam', level:'intermediate',
  cert:['aws-sec:identity-and-access','aws-sysops:security'],
  sum:'Every user, their keys, their MFA and how stale it all is — one CSV.',
  syntax:'aws iam generate-credential-report && aws iam get-credential-report --output text --query Content | base64 -d',
  flags:[['generate-credential-report','Must run first; the report is cached for four hours'],
         ['--query Content --output text | base64 -d','The response is base64-encoded CSV'],
         ['list-access-keys --user-name NAME','Per-user detail once the report has named a suspect']],
  ex:[['aws iam generate-credential-report >/dev/null && aws iam get-credential-report --query Content --output text | base64 -d | column -t -s,','Readable in one line'],
      ['aws iam get-credential-report --query Content --output text | base64 -d | awk -F, \'$8=="false" && $4=="true" {print $1}\'','Console users without MFA'],
      ['aws iam list-access-keys --user-name ci-bot --query \'AccessKeyMetadata[].[AccessKeyId,Status,CreateDate]\' --output table','Key age, which is the finding auditors actually check']],
  tip:'This one CSV answers most of the identity questions in a CIS benchmark or a SOC 2 evidence request: root access keys present, console users without MFA, access keys older than ninety days, credentials never used. It is also the honest starting point for a cleanup, because `password_last_used` and `access_key_1_last_used_date` tell you which accounts can be disabled with no argument. Deactivate a key before deleting it — deactivation is instantly reversible when you find out what it was for.',
  related:['aws iam get-account-authorization-details','aws cloudtrail lookup-events'] },

/* ── Organizations ──────────────────────────────────────────── */
{ name:'aws organizations list-policies-for-target', track:'aws', cat:'org', level:'advanced',
  cert:['aws-saa:secure-architectures','aws-sec:identity-and-access'],
  sum:'The Service Control Policies that apply to an account — the deny nobody can see from inside it.',
  syntax:'aws organizations list-policies-for-target --target-id ACCOUNT_OR_OU --filter SERVICE_CONTROL_POLICY',
  flags:[['--filter SERVICE_CONTROL_POLICY','Also TAG_POLICY, BACKUP_POLICY, AISERVICES_OPT_OUT_POLICY'],
         ['describe-policy --policy-id p-abc123','The document itself'],
         ['list-parents --child-id 111122223333','Walk up the OU tree — policies are inherited all the way down']],
  ex:[['aws organizations list-policies-for-target --target-id 111122223333 --filter SERVICE_CONTROL_POLICY --query \'Policies[].[Id,Name]\' --output table','Directly attached SCPs'],
      ['aws organizations describe-policy --policy-id p-abc123 --query Policy.Content --output text | jq .','The actual statements'],
      ['aws organizations list-parents --child-id 111122223333 --query \'Parents[].[Id,Type]\' --output text','The OU above it, whose SCPs also apply']],
  tip:'SCPs are the reason an account administrator with `AdministratorAccess` gets `AccessDenied` and can find nothing wrong with their own policy — and from inside the member account there is no API that shows them, which is why this call has to run against the management account or a delegated administrator. They are a **filter, not a grant**: an SCP allowing everything grants nothing, and every account below an OU inherits its SCPs cumulatively. The denial message is the tell — it says the action was denied "with an explicit deny in a service control policy".',
  related:['aws iam simulate-principal-policy','aws sts get-caller-identity'] },

/* ── Keys and secrets ───────────────────────────────────────── */
{ name:'aws kms describe-key', track:'aws', cat:'kms', level:'intermediate',
  cert:['aws-sec:data-protection','aws-saa:secure-architectures'],
  sum:'Which key, whose key, and whether it is the AWS-managed one you cannot control.',
  syntax:'aws kms describe-key --key-id ALIAS_OR_ID_OR_ARN',
  flags:[['--key-id alias/app-data','Aliases are the sane way to reference a key'],
         ['--query \'KeyMetadata.[KeyId,KeyManager,KeyState,MultiRegion]\'','Customer-managed or AWS-managed, and whether it is enabled'],
         ['get-key-policy --key-id … --policy-name default','The key policy, which is the real access control']],
  ex:[['aws kms describe-key --key-id alias/app-data --query \'KeyMetadata.[Arn,KeyManager,KeyState]\' --output text','Identify the key behind an alias'],
      ['aws kms get-key-policy --key-id alias/app-data --policy-name default --output text | jq .','Who may use it and who may administer it'],
      ['aws kms list-aliases --query \'Aliases[?starts_with(AliasName,`alias/app`)].[AliasName,TargetKeyId]\' --output table','Map aliases to key ids']],
  tip:'A KMS key is one of the few resources where the **resource policy is mandatory rather than optional**: unlike S3, an identity policy granting `kms:Decrypt` does nothing unless the key policy also allows that principal, directly or by delegating to IAM with a `kms:CallerAccount` style statement. That asymmetry is the cause of most cross-account KMS denials. `KeyManager: AWS` means it is an AWS-managed key — you cannot edit its policy, so cross-account sharing requires a customer-managed key, which is the reason to create one at all.',
  related:['aws kms list-grants','aws secretsmanager get-secret-value'] },

{ name:'aws kms list-grants', track:'aws', cat:'kms', level:'advanced',
  cert:['aws-sec:data-protection'],
  sum:'The permissions that are not in the key policy and do not show up in a policy review.',
  syntax:'aws kms list-grants --key-id KEY',
  flags:[['--query \'Grants[].[GrantId,GranteePrincipal,Operations]\'','Who was granted what'],
         ['--grantee-principal ARN','Filter to one principal'],
         ['revoke-grant --key-id … --grant-id …','Remove one; retiring is the other, gentler verb']],
  ex:[['aws kms list-grants --key-id alias/app-data --query \'Grants[].[GrantId,GranteePrincipal,Operations[0]]\' --output table','Every outstanding grant'],
      ['aws kms list-grants --key-id alias/app-data --query \'Grants[?contains(GranteePrincipal,`ec2`)]\'','Grants created by EBS on your behalf'],
      ['aws kms revoke-grant --key-id alias/app-data --grant-id abc123','Revoke — permanent, and it will break whatever was using it']],
  tip:'Grants are how AWS services get temporary, scoped use of your key — EBS creating an encrypted volume, Auto Scaling launching from an encrypted snapshot, RDS reading storage. They are created programmatically, they do not appear in the key policy, and a key policy review that ignores them is incomplete in both directions: you can miss access that exists, and you can revoke a grant and break a service that will only fail at the next instance launch, which may be days later.',
  related:['aws kms describe-key','aws ec2 describe-volumes'] },

{ name:'aws secretsmanager get-secret-value', track:'aws', cat:'kms', level:'intermediate',
  cert:['aws-sec:data-protection','aws-saa:secure-architectures'],
  sum:'Read a secret, and understand the rotation model behind it.',
  syntax:'aws secretsmanager get-secret-value --secret-id NAME_OR_ARN',
  flags:[['--query SecretString --output text','The payload; usually JSON'],
         ['--version-stage AWSCURRENT | AWSPREVIOUS','Rotation moves these labels; AWSPREVIOUS is your rollback'],
         ['describe-secret --secret-id NAME','Rotation config and last-rotated date, without reading the value']],
  ex:[['aws secretsmanager get-secret-value --secret-id prod/db --query SecretString --output text | jq -r .password','One field out of the JSON'],
      ['aws secretsmanager describe-secret --secret-id prod/db --query \'[RotationEnabled,LastRotatedDate,NextRotationDate]\'','Whether rotation is real or aspirational'],
      ['aws ssm get-parameter --name /prod/db/host --with-decryption --query Parameter.Value --output text','SSM Parameter Store — the cheaper option when rotation is not needed']],
  tip:'Every read is a CloudTrail event with the caller and the secret id, which makes access reviewable — and makes `get-secret-value` in a build log a genuine incident rather than an untidiness. Choose deliberately between the two stores: Secrets Manager costs per secret per month and gives you managed rotation and cross-account access; SSM Parameter Store SecureString is free at standard tier and gives you neither. Rotation is only real if the application re-reads the secret; a value cached at process start survives rotation until a restart, which is how a rotation "succeeds" and the service breaks four hours later.',
  related:['aws kms describe-key','aws cloudtrail lookup-events'] },

{ name:'aws iam list-roles', track:'aws', cat:'iam', level:'beginner',
  cert:['aws-sec:identity-and-access'],
  sum:'Enumerate roles — and spot the ones trusting principals outside the account.',
  syntax:'aws iam list-roles [--path-prefix /aws-service-role/]',
  flags:[['--path-prefix /aws-service-role/','Service-linked roles, which are noise in most reviews'],
         ['--query \'Roles[].[RoleName,CreateDate,MaxSessionDuration]\'','The basics'],
         ['--query \'Roles[?!contains(Path,`aws-service-role`)].RoleName\'','Roles a human actually created']],
  ex:[['aws iam list-roles --query \'Roles[?!contains(Path,`aws-service-role`)].[RoleName,CreateDate]\' --output table','Human-created roles, oldest first'],
      ['aws iam list-roles --query \'Roles[?contains(to_string(AssumeRolePolicyDocument),`:root`)].RoleName\'','Roles trusting an entire account rather than a specific role'],
      ['aws iam list-roles --query \'Roles[?contains(to_string(AssumeRolePolicyDocument),`Federated`)].RoleName\'','Roles assumed through SSO or an OIDC provider']],
  tip:'A trust policy naming `arn:aws:iam::444455556666:root` trusts *anything* in that account that also has `sts:AssumeRole` permission — not the account\'s root user, which is the common misreading. That is acceptable between your own accounts and a real finding when the other side is a vendor. The other pattern worth searching for is a GitHub Actions OIDC trust whose condition on `token.actions.githubusercontent.com:sub` uses a wildcard broad enough to match any repository in the organisation.',
  related:['aws iam get-role','aws organizations list-policies-for-target'] },

{ name:'aws configure sso', track:'aws', cat:'iam', level:'beginner',
  cert:['aws-saa:secure-architectures'],
  sum:'Short-lived credentials from IAM Identity Center instead of a key in a file.',
  syntax:'aws configure sso [--profile NAME]  ·  aws sso login --profile NAME',
  flags:[['sso login --profile NAME','Refresh an expired session; the browser does the work'],
         ['--profile NAME','Everything downstream then uses that profile\'s role'],
         ['sso logout','Clears the cached token — worth knowing on a shared machine']],
  ex:[['aws configure sso --profile prod','One-time setup: start URL, region, account, role'],
      ['aws sso login --profile prod','Daily: opens a browser, writes a token under ~/.aws/sso/cache'],
      ['aws sts get-caller-identity --profile prod','Confirm which role you landed in']],
  tip:'The reason to care is that this removes long-lived access keys from laptops entirely, which closes the single most common route to a real AWS compromise — a key in a repository, a dotfile or a CI variable. Sessions expire, typically in hours, and `ExpiredToken` mid-script means run `aws sso login` rather than anything more dramatic. If you still have `aws_access_key_id` in `~/.aws/credentials` for a human user, that is the thing to remove this week.',
  related:['aws sts get-caller-identity','aws iam get-credential-report'] }

);
