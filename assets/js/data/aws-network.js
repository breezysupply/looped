/* AWS track — the network path a packet takes, in the order you check it:
   is there a route, does the security group allow it, does the NACL allow it
   back, and is anything listening at the other end.                          */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

/* ── The layout ─────────────────────────────────────────────── */
{ name:'aws ec2 describe-vpcs', track:'aws', cat:'vpc', level:'beginner',
  cert:['aws-saa:secure-architectures'],
  sum:'The VPCs in a region and their CIDRs — the first thing to establish in an unfamiliar account.',
  syntax:'aws ec2 describe-vpcs [--vpc-ids ID…] [--filters …]',
  flags:[['--query \'Vpcs[].[VpcId,CidrBlock,IsDefault,Tags[?Key==`Name`].Value|[0]]\'','Id, range, whether it is the default, name'],
         ['--filters Name=isDefault,Values=true','The default VPC, which is usually the one nobody meant to deploy into']],
  ex:[['aws ec2 describe-vpcs --query \'Vpcs[].[VpcId,CidrBlock,IsDefault]\' --output table','Every VPC and its range'],
      ['aws ec2 describe-vpcs --query \'Vpcs[].CidrBlockAssociationSet[].CidrBlock\'','Secondary CIDRs, which is where overlaps hide'],
      ['aws ec2 describe-vpc-attribute --vpc-id vpc-0a1b --attribute enableDnsHostnames','Off by default in a hand-built VPC, and required for private hosted zones']],
  tip:'Two attributes are not in the main response and decide whether name resolution works at all: `enableDnsSupport` and `enableDnsHostnames`. A VPC built by hand or by an early Terraform module often has hostnames disabled, which makes private hosted zones, VPC endpoint DNS and RDS endpoint resolution fail in ways that look like a DNS outage. Overlapping CIDRs are the other quiet landmine — they cost nothing until the day you need to peer.',
  related:['aws ec2 describe-subnets','aws ec2 describe-route-tables'] },

{ name:'aws ec2 describe-subnets', track:'aws', cat:'vpc', level:'beginner',
  cert:['aws-saa:secure-architectures','aws-sysops:networking'],
  sum:'Subnets, their AZs, and how many addresses are left in each.',
  syntax:'aws ec2 describe-subnets [--filters Name=vpc-id,Values=vpc-…]',
  flags:[['--query \'Subnets[].[SubnetId,AvailabilityZone,CidrBlock,AvailableIpAddressCount,MapPublicIpOnLaunch]\'','Everything a launch failure needs'],
         ['--filters Name=vpc-id,Values=vpc-0a1b','Scope it, or you get every subnet in the region'],
         ['--filters Name=map-public-ip-on-launch,Values=true','Subnets that hand out public IPs — worth auditing']],
  ex:[['aws ec2 describe-subnets --filters Name=vpc-id,Values=vpc-0a1b --query \'Subnets[].[SubnetId,AvailabilityZone,CidrBlock,AvailableIpAddressCount]\' --output table','The layout, with headroom'],
      ['aws ec2 describe-subnets --query \'Subnets[?AvailableIpAddressCount<`10`].[SubnetId,AvailableIpAddressCount]\' --output table','Subnets about to run out'],
      ['aws ec2 describe-subnets --filters Name=subnet-id,Values=subnet-0a1b --query \'Subnets[0].MapPublicIpOnLaunch\'','Whether an instance here gets a public IP automatically']],
  tip:'AWS reserves five addresses in every subnet — network, VPC router, DNS, a future use, and broadcast — so a /28 gives you eleven usable, not sixteen. `AvailableIpAddressCount` is the number to watch: an EKS cluster or a Lambda in a VPC consumes addresses per pod or per concurrent execution, and address exhaustion presents as a launch failure or a stuck scaling event rather than as anything network-shaped. There is also nothing intrinsically "public" about a subnet — a subnet is public only because its route table has a default route to an internet gateway.',
  related:['aws ec2 describe-route-tables','aws ec2 describe-vpcs'] },

{ name:'aws ec2 describe-route-tables', track:'aws', cat:'vpc', level:'beginner',
  cert:['aws-saa:secure-architectures','aws-sysops:networking'],
  sum:'Where traffic goes. The definition of a public subnet lives here and nowhere else.',
  syntax:'aws ec2 describe-route-tables [--filters Name=association.subnet-id,Values=subnet-…]',
  flags:[['--filters Name=association.subnet-id,Values=subnet-0a1b','The table that actually governs one subnet'],
         ['--query \'RouteTables[].Routes[].[DestinationCidrBlock,GatewayId,NatGatewayId,State]\'','Destination and next hop'],
         ['--query \'RouteTables[?Associations[?Main==`true`]]\'','The main table, which every unassociated subnet silently inherits']],
  ex:[['aws ec2 describe-route-tables --filters Name=association.subnet-id,Values=subnet-0a1b --query \'RouteTables[].Routes[].[DestinationCidrBlock,GatewayId,NatGatewayId]\' --output table','The routes for one subnet'],
      ['aws ec2 describe-route-tables --filters Name=vpc-id,Values=vpc-0a1b --query \'RouteTables[].[RouteTableId,length(Associations)]\' --output table','Which tables are associated with anything at all'],
      ['aws ec2 describe-route-tables --query \'RouteTables[].Routes[?State==`blackhole`]\'','Routes pointing at something that no longer exists']],
  tip:'A subnet with no explicit association follows the VPC\'s **main** route table, which is why a newly created subnet can behave completely differently from its neighbours while looking identical in the console. `State: blackhole` is the other thing to grep for: it means the next hop — a NAT gateway, a peering connection, an instance — has been deleted, and every packet matching that route is silently dropped. Both failures look like a firewall problem and are not.',
  related:['aws ec2 describe-subnets','aws ec2 describe-nat-gateways'] },

/* ── Filtering ──────────────────────────────────────────────── */
{ name:'aws ec2 describe-security-groups', track:'aws', cat:'sg', level:'beginner',
  cert:['aws-saa:secure-architectures','aws-sec:infrastructure-security'],
  sum:'Stateful, allow-only, attached to interfaces — the first firewall to check and the easiest to misread.',
  syntax:'aws ec2 describe-security-groups [--group-ids ID…] [--filters …]',
  flags:[['--filters Name=ip-permission.from-port,Values=22','Every group opening SSH'],
         ['--filters Name=ip-permission.cidr,Values=0.0.0.0/0','Every group open to the world'],
         ['--query \'SecurityGroups[].IpPermissions[].[FromPort,ToPort,IpProtocol,IpRanges[].CidrIp,UserIdGroupPairs[].GroupId]\'','Ports, protocol, and both kinds of source']],
  ex:[['aws ec2 describe-security-groups --group-ids sg-0a1b --query \'SecurityGroups[0].IpPermissions\'','Inbound rules for one group, in full'],
      ['aws ec2 describe-security-groups --filters Name=ip-permission.cidr,Values=0.0.0.0/0 Name=ip-permission.from-port,Values=22 --query \'SecurityGroups[].[GroupId,GroupName]\' --output table','SSH open to the internet — the finding every auditor opens with'],
      ['aws ec2 describe-security-groups --query \'SecurityGroups[?length(IpPermissions)==`0` && length(IpPermissionsEgress)==`1`].GroupId\'','Groups doing nothing, safe to retire']],
  tip:'Security groups are **stateful**: an allowed inbound request gets its reply out regardless of the outbound rules, so a "the response is blocked" theory is nearly always wrong at this layer. They are also allow-only — there is no deny rule to hunt for — which means a connection refused at this layer is a missing allow, not a hidden block. The strongest pattern is referencing another group as the source (`UserIdGroupPairs`) instead of a CIDR: the rule then follows the instances rather than the addresses, and survives every re-IP.',
  related:['aws ec2 describe-network-acls','aws ec2 describe-network-interfaces'] },

{ name:'aws ec2 describe-network-acls', track:'aws', cat:'sg', level:'intermediate',
  cert:['aws-saa:secure-architectures','aws-sec:infrastructure-security'],
  sum:'Stateless, ordered, subnet-wide — and the reason a connection works one way only.',
  syntax:'aws ec2 describe-network-acls [--filters Name=association.subnet-id,Values=subnet-…]',
  flags:[['--filters Name=association.subnet-id,Values=subnet-0a1b','The NACL governing one subnet'],
         ['--query \'NetworkAcls[].Entries[].[RuleNumber,Egress,Protocol,CidrBlock,RuleAction,PortRange]\'','Rules in evaluation order'],
         ['--query \'NetworkAcls[?IsDefault==`false`]\'','Custom NACLs — the default allows everything and is rarely the problem']],
  ex:[['aws ec2 describe-network-acls --filters Name=association.subnet-id,Values=subnet-0a1b --query \'NetworkAcls[].Entries[?Egress==`false`]|[0]\' --output table','Inbound rules, in order'],
      ['aws ec2 describe-network-acls --query \'NetworkAcls[].Entries[?RuleAction==`deny` && RuleNumber<`32767`]\'','Explicit denies somebody added deliberately'],
      ['aws ec2 describe-network-acls --filters Name=association.subnet-id,Values=subnet-0a1b --query \'NetworkAcls[].Entries[?Egress==`true`]|[0]\' --output table','The outbound half, where ephemeral ports get forgotten']],
  tip:'The one fact that matters: NACLs are **stateless**, so the reply to an allowed inbound connection needs its own outbound rule — to the client\'s **ephemeral port range**, 1024–65535, not to port 443. A NACL that allows inbound 443 and outbound 443 lets requests in and drops every response, producing a timeout that looks exactly like a dead application. Rules are evaluated lowest number first and the first match wins, so a `deny` at rule 90 beats an `allow` at rule 100 no matter how correct the allow looks.',
  related:['aws ec2 describe-security-groups','aws ec2 describe-flow-logs'] },

{ name:'aws ec2 describe-network-interfaces', track:'aws', cat:'vpc', level:'intermediate',
  cert:['aws-sysops:networking'],
  sum:'The object security groups are really attached to — and what is holding an address.',
  syntax:'aws ec2 describe-network-interfaces [--filters …]',
  flags:[['--filters Name=addresses.private-ip-address,Values=10.0.4.31','Find the owner of an IP from a log line'],
         ['--filters Name=group-id,Values=sg-0a1b','Everything a security group is attached to'],
         ['--query \'NetworkInterfaces[].[NetworkInterfaceId,PrivateIpAddress,Status,Description,Attachment.InstanceId]\'','Who owns it']],
  ex:[['aws ec2 describe-network-interfaces --filters Name=addresses.private-ip-address,Values=10.0.4.31 --query \'NetworkInterfaces[0].[Description,Attachment.InstanceId,Groups[].GroupId]\'','Turn an IP from a flow log into a thing'],
      ['aws ec2 describe-network-interfaces --filters Name=group-id,Values=sg-0a1b --query \'length(NetworkInterfaces)\'','Whether a group is safe to delete'],
      ['aws ec2 describe-network-interfaces --filters Name=status,Values=available --query \'NetworkInterfaces[].[NetworkInterfaceId,SubnetId]\' --output table','Orphaned interfaces still consuming subnet addresses']],
  tip:'This is the command that answers "what is 10.0.4.31?" when a flow log or an application error gives you only an address. The `Description` field names the owner for managed services — an ALB node, an RDS instance, a VPC endpoint, a Lambda — none of which appear in `describe-instances`. It is also the reason a security group refuses to delete: something still references it, and the interface list is what tells you what.',
  related:['aws ec2 describe-security-groups','aws ec2 describe-flow-logs'] },

/* ── Getting out ────────────────────────────────────────────── */
{ name:'aws ec2 describe-nat-gateways', track:'aws', cat:'vpc', level:'intermediate',
  cert:['aws-saa:cost-optimized','aws-sysops:networking'],
  sum:'How private subnets reach the internet, and one of the larger lines on the bill.',
  syntax:'aws ec2 describe-nat-gateways [--filter Name=vpc-id,Values=vpc-…]',
  flags:[['--filter Name=state,Values=available','Note the singular --filter here; the EC2 API is not consistent about it'],
         ['--query \'NatGateways[].[NatGatewayId,SubnetId,State,NatGatewayAddresses[0].PublicIp]\'','Which AZ each one lives in'],
         ['--query \'NatGateways[?State!=`available`]\'','Deleted or failed gateways still referenced by a route']],
  ex:[['aws ec2 describe-nat-gateways --filter Name=vpc-id,Values=vpc-0a1b --query \'NatGateways[].[NatGatewayId,SubnetId,State]\' --output table','One per AZ, or one for everything?'],
      ['aws ec2 describe-route-tables --query \'RouteTables[].Routes[?NatGatewayId!=null].[DestinationCidrBlock,NatGatewayId]\'','Which tables point at which gateway'],
      ['aws cloudwatch get-metric-statistics --namespace AWS/NATGateway --metric-name BytesOutToDestination --dimensions Name=NatGatewayId,Value=nat-0a1b --start-time 2026-08-01T00:00:00Z --end-time 2026-08-27T00:00:00Z --period 86400 --statistics Sum','What it is actually moving, which is what you pay for']],
  tip:'A NAT gateway is zonal. One gateway serving all three AZs works and quietly bills every byte from the other two AZs as cross-AZ data transfer *on top of* the NAT processing charge — and it becomes a single point of failure for outbound traffic when that AZ has a bad day. The other half of the cost conversation is VPC endpoints: S3 and DynamoDB gateway endpoints are free and remove that traffic from NAT entirely, which is usually the largest single saving available in a VPC.',
  related:['aws ec2 describe-vpc-endpoints','aws ec2 describe-route-tables'] },

{ name:'aws ec2 describe-vpc-endpoints', track:'aws', cat:'vpc', level:'intermediate',
  cert:['aws-saa:secure-architectures','aws-saa:cost-optimized'],
  sum:'Reaching AWS services without traversing the internet — cheaper, and a control boundary.',
  syntax:'aws ec2 describe-vpc-endpoints [--filters Name=vpc-id,Values=vpc-…]',
  flags:[['--query \'VpcEndpoints[].[VpcEndpointId,ServiceName,VpcEndpointType,State]\'','Gateway or Interface, and whether it is live'],
         ['--query \'VpcEndpoints[?VpcEndpointType==`Interface`].[ServiceName,PrivateDnsEnabled]\'','Private DNS is what makes the endpoint transparent'],
         ['--query \'VpcEndpoints[].PolicyDocument\'','Endpoint policies — an under-used control']],
  ex:[['aws ec2 describe-vpc-endpoints --filters Name=vpc-id,Values=vpc-0a1b --query \'VpcEndpoints[].[ServiceName,VpcEndpointType,State]\' --output table','What this VPC can reach privately'],
      ['aws ec2 create-vpc-endpoint --vpc-id vpc-0a1b --service-name com.amazonaws.us-east-1.s3 --route-table-ids rtb-0a1b','A gateway endpoint for S3: free, and it edits the route table'],
      ['aws ec2 describe-vpc-endpoints --query \'VpcEndpoints[?PrivateDnsEnabled==`false`].ServiceName\'','Interface endpoints nobody is using because DNS still points at the public name']],
  tip:'Two kinds, and they fail differently. **Gateway** endpoints (S3 and DynamoDB only) are free and work by adding a prefix-list route — if the subnet\'s route table was not passed in, the endpoint exists and does nothing. **Interface** endpoints are ENIs with an hourly charge, and they only take effect when `PrivateDnsEnabled` is true, otherwise callers keep resolving the public endpoint and keep paying NAT. Endpoint policies are also a genuine security control: an S3 endpoint policy can restrict the VPC to your own buckets, which stops data leaving to a personal account.',
  related:['aws ec2 describe-nat-gateways','aws ec2 describe-route-tables'] },

{ name:'aws ec2 describe-internet-gateways', track:'aws', cat:'vpc', level:'beginner',
  cert:['aws-saa:secure-architectures'],
  sum:'The other half of "public subnet" — and it is either attached or it is not.',
  syntax:'aws ec2 describe-internet-gateways [--filters Name=attachment.vpc-id,Values=vpc-…]',
  flags:[['--filters Name=attachment.vpc-id,Values=vpc-0a1b','The gateway attached to this VPC, if any'],
         ['--query \'InternetGateways[].[InternetGatewayId,Attachments[0].VpcId,Attachments[0].State]\'','Id and attachment state']],
  ex:[['aws ec2 describe-internet-gateways --filters Name=attachment.vpc-id,Values=vpc-0a1b --query \'InternetGateways[].[InternetGatewayId,Attachments[0].State]\' --output table','Attached, or detached and useless'],
      ['aws ec2 describe-egress-only-internet-gateways','The IPv6 equivalent of a NAT gateway'],
      ['aws ec2 describe-addresses --query \'Addresses[?AssociationId==null].[PublicIp,AllocationId]\' --output table','Elastic IPs allocated and attached to nothing — billed hourly']],
  tip:'An instance reaches the internet only when four things line up: a public IP or Elastic IP on its interface, an internet gateway attached to the VPC, a `0.0.0.0/0` route to that gateway in the subnet\'s route table, and security group plus NACL rules that allow the traffic. Three of the four are silent when wrong. Unattached Elastic IPs are billed by the hour precisely because they are a scarce resource being held, which surprises people every month.',
  related:['aws ec2 describe-route-tables','aws ec2 describe-nat-gateways'] },

/* ── Seeing the traffic ─────────────────────────────────────── */
{ name:'aws ec2 describe-flow-logs', track:'aws', cat:'vpc', level:'advanced',
  cert:['aws-sec:logging-and-monitoring','aws-sysops:networking'],
  sum:'Whether packets arrive at all, and whether they are accepted or rejected.',
  syntax:'aws ec2 describe-flow-logs | create-flow-logs --resource-type Subnet --resource-ids …',
  flags:[['--traffic-type ALL | ACCEPT | REJECT','REJECT-only is cheap and answers most firewall questions'],
         ['--log-destination-type s3 | cloud-watch-logs','S3 for volume and Athena, CloudWatch for immediate querying'],
         ['--max-aggregation-interval 60','One minute instead of ten — worth it during an incident']],
  ex:[['aws ec2 create-flow-logs --resource-type NetworkInterface --resource-ids eni-0a1b --traffic-type REJECT --log-destination-type cloud-watch-logs --log-group-name /vpc/flow --deliver-logs-permission-arn arn:aws:iam::111122223333:role/flowlogs','Rejects only, on one interface'],
      ['aws logs start-query --log-group-name /vpc/flow --start-time $(date -d \'1 hour ago\' +%s) --end-time $(date +%s) --query-string \'fields srcAddr, dstAddr, dstPort, action | filter action="REJECT" | stats count() by dstPort | sort by count() desc\'','Which ports are being rejected, ranked'],
      ['aws ec2 describe-flow-logs --query \'FlowLogs[].[ResourceId,TrafficType,LogDestinationType,FlowLogStatus]\' --output table','What is being captured where']],
  tip:'Flow logs settle the argument that otherwise takes an afternoon: `REJECT` means the packet arrived and a security group or NACL dropped it; **no record at all** means it never reached the interface, so the problem is routing, DNS or the client. Note what they cannot tell you — they are metadata, not payload, they exclude traffic to the Amazon DNS server and instance metadata, and delivery lags by up to ten minutes at the default aggregation interval.',
  related:['aws ec2 describe-security-groups','aws ec2 describe-network-acls'] },

{ name:'aws ec2 start-network-insights-analysis', track:'aws', cat:'vpc', level:'advanced',
  cert:['aws-sysops:networking'],
  sum:'Reachability Analyzer: AWS evaluates the whole path for you and names the component that blocks it.',
  syntax:'aws ec2 create-network-insights-path --source … --destination … --protocol tcp --destination-port N',
  flags:[['--source / --destination','Instance, interface, gateway, or endpoint — not arbitrary IPs'],
         ['--destination-port','Required, or the answer is about the wrong thing'],
         ['describe-network-insights-analyses','Where the verdict lands, a few seconds later']],
  ex:[['aws ec2 create-network-insights-path --source i-0a1b --destination i-0c2d --protocol tcp --destination-port 5432 --query NetworkInsightsPath.NetworkInsightsPathId --output text','Define the question once'],
      ['aws ec2 start-network-insights-analysis --network-insights-path-id nip-0a1b --query NetworkInsightsAnalysis.NetworkInsightsAnalysisId --output text','Ask it'],
      ['aws ec2 describe-network-insights-analyses --network-insights-analysis-ids nia-0a1b --query \'NetworkInsightsAnalyses[0].[NetworkPathFound,Explanations[].ExplanationCode]\'','The verdict and the reason code']],
  tip:'This is a static analysis of configuration, not a live probe, and that is both its strength and its limit: it will tell you `ENI_SG_RULES_MISMATCH` or `NO_ROUTE_TO_DESTINATION` without needing anything to be running, but it cannot see a process that is not listening, a host firewall, or an application that is refusing connections. Use it to eliminate the AWS layer in seconds, then move to the instance. It is billed per analysis, so it is a diagnostic tool rather than a monitor.',
  related:['aws ec2 describe-flow-logs','aws ec2 describe-route-tables'] },

{ name:'aws route53 list-resource-record-sets', track:'aws', cat:'vpc', level:'intermediate',
  cert:['aws-saa:high-performing','aws-sysops:networking'],
  sum:'What a name resolves to, according to the zone rather than according to your laptop.',
  syntax:'aws route53 list-resource-record-sets --hosted-zone-id ZONE',
  flags:[['--query \'ResourceRecordSets[?Name==`api.example.com.`]\'','Note the trailing dot — Route 53 stores fully qualified names'],
         ['--start-record-name / --max-items','Paging through a large zone'],
         ['list-hosted-zones --query \'HostedZones[].[Id,Name,Config.PrivateZone]\'','Public and private zones with the same name is a real and confusing setup']],
  ex:[['aws route53 list-hosted-zones --query \'HostedZones[].[Id,Name,Config.PrivateZone]\' --output table','Find the zone before you query it'],
      ['aws route53 list-resource-record-sets --hosted-zone-id Z123 --query \'ResourceRecordSets[?Type==`A`].[Name,AliasTarget.DNSName,ResourceRecords[0].Value]\' --output table','A records and aliases together'],
      ['aws route53 get-health-check-status --health-check-id abc-123','Whether a failover record thinks its primary is up']],
  tip:'A private hosted zone attached to the VPC overrides the public zone of the same name for anything inside that VPC — which is deliberate and is also why an endpoint resolves differently from an instance than from your laptop. Two prerequisites are easy to miss: the VPC needs `enableDnsSupport` and `enableDnsHostnames`, and the zone must be explicitly associated with the VPC. Alias records are Route 53-specific, resolve to AWS targets, are free to query, and can sit at the zone apex where a CNAME is illegal.',
  related:['aws ec2 describe-vpcs','aws ec2 describe-vpc-endpoints'] },

{ name:'aws ec2 describe-vpc-peering-connections', track:'aws', cat:'vpc', level:'advanced',
  cert:['aws-saa:secure-architectures'],
  sum:'VPC-to-VPC links, and why the traffic still does not flow after one is created.',
  syntax:'aws ec2 describe-vpc-peering-connections [--filters …]',
  flags:[['--query \'VpcPeeringConnections[].[VpcPeeringConnectionId,Status.Code,RequesterVpcInfo.CidrBlock,AccepterVpcInfo.CidrBlock]\'','Both sides and the state'],
         ['--filters Name=status-code,Values=active','Pending or expired connections look identical in a diagram'],
         ['describe-transit-gateway-attachments','The scalable alternative once there are more than a handful of VPCs']],
  ex:[['aws ec2 describe-vpc-peering-connections --query \'VpcPeeringConnections[].[VpcPeeringConnectionId,Status.Code]\' --output table','Which links are actually active'],
      ['aws ec2 describe-route-tables --filters Name=vpc-id,Values=vpc-0a1b --query \'RouteTables[].Routes[?VpcPeeringConnectionId!=null]\'','The routes that make a peering usable'],
      ['aws ec2 describe-transit-gateway-route-tables --query \'TransitGatewayRouteTables[].[TransitGatewayRouteTableId,State]\' --output table','The transit gateway equivalent']],
  tip:'Creating and accepting a peering connection changes nothing on its own — both sides need a route to the other\'s CIDR *and* security groups that allow the traffic, and overlapping CIDRs make peering impossible from the start. Peering is also not transitive: A↔B and B↔C does not give A↔C, which is exactly the wall that pushes an estate onto a Transit Gateway. Security group references do work across a peering, but only within the same region.',
  related:['aws ec2 describe-route-tables','aws ec2 describe-security-groups'] }

);
