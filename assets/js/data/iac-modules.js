/* Terraform & IaC — modules, variables and the expression constructs that
   decide what a resource address looks like. Addresses are the thing that
   makes a plan safe or terrifying, so most of this file is really about them. */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

{ name:'module', track:'iac', cat:'modules', level:'beginner',
  cert:['tf-associate:modules'],
  sum:'Reusable configuration — and the source and version arguments decide whether it is safe.',
  syntax:'module "name" { source = "…" version = "…" <inputs> }',
  flags:[['source = "./modules/network"','Local path; versioned with the repository itself'],
         ['source = "terraform-aws-modules/vpc/aws"  version = "~> 5.0"','Registry module, with a constraint'],
         ['source = "git::https://…//modules/vpc?ref=v1.4.2"','Git, pinned to a tag. Note the double slash for a subdirectory'],
         ['source = "git::https://…?ref=main"','A time bomb: what applies today is not what applied yesterday'],
         ['count / for_each on a module','Modules take meta-arguments too']],
  ex:[['module "vpc" { source = "terraform-aws-modules/vpc/aws"  version = "~> 5.1.0" }','Registry, patch-level constraint'],
      ['module "svc" { source = "git::ssh://git@github.com/org/tf-modules.git//service?ref=v2.3.0" }','Private git, pinned to a tag'],
      ['terraform init -upgrade','Re-resolve module and provider versions after widening a constraint']],
  tip:'`version` only applies to registry sources — for a git source the pin lives in the `ref`, and `ref=main` means the module can change under you between two applies with no diff in your own repository to explain it. That is the single most confusing failure in a shared module estate: nothing in your code changed and the plan is different. Pin to a tag, and treat a module bump as a reviewed change like any other. Also note that a module boundary is an interface: every input and output is a contract, and modules with fifty inputs are usually a copy-paste with extra steps.',
  related:['terraform init','count','for_each'] },

{ name:'for_each', track:'iac', cat:'modules', level:'intermediate',
  cert:['tf-associate:modules'],
  sum:'One resource per map entry or set member, with stable, meaningful addresses.',
  syntax:'resource "type" "name" { for_each = MAP_OR_SET  … each.key … each.value }',
  flags:[['each.key / each.value','The key and its value inside the block'],
         ['toset(["a","b"])','A set of strings; keys and values are the same'],
         ['{ for x in var.list : x.name => x }','Turn a list into a keyed map — the usual conversion'],
         ['addresses','aws_instance.web["api"] — stable regardless of ordering']],
  ex:[['for_each = { api = "t3.small", worker = "t3.medium" }','Keyed by role, not position'],
      ['for_each = toset(var.subnet_cidrs)','A set of strings, keyed by the value itself'],
      ['for_each = { for s in var.subnets : s.name => s }','List to map, keeping the whole object as the value']],
  tip:'Use this rather than `count` for anything that is a set of distinct things, because the address contains a meaningful key instead of a position — so removing the middle element removes exactly one resource instead of renumbering every resource after it. The constraint is that the keys must be known at plan time: keying by an attribute that only exists after another resource is created produces "the for_each value depends on resource attributes that cannot be determined until apply", and the fix is to key by something static like a name from a variable.',
  related:['count','moved','terraform console'] },

{ name:'count', track:'iac', cat:'modules', level:'beginner',
  cert:['tf-associate:modules'],
  sum:'N copies, addressed by position — which is exactly the problem.',
  syntax:'resource "type" "name" { count = N  … count.index … }',
  flags:[['count.index','Zero-based position'],
         ['count = var.enabled ? 1 : 0','The conditional-resource idiom, and the one good use of count'],
         ['addresses','aws_instance.web[0] — positional, so the index shifts when the list changes'],
         ['length(var.list)','Counting a list; convert to for_each if the list can be reordered']],
  ex:[['count = var.create_bastion ? 1 : 0','Create it or do not — clear and safe'],
      ['count = length(var.azs)','Works, and reordering var.azs recreates everything'],
      ['moved { from = aws_instance.web[0]  to = aws_instance.web["api"] }','The migration when you fix it']],
  tip:'`count` is right for one thing — a resource that either exists or does not — and wrong for most collections, because the index is positional. Remove the first element of a three-item list and Terraform sees `[0]` change identity, `[1]` change identity and `[2]` disappear: three destroy-and-recreate operations for what you thought was one deletion. That plan is the single most alarming diff a Terraform user meets, and the fix is `for_each` plus `moved` blocks rather than applying and hoping. Note also that `count = 1` and no count are different addresses — `web[0]` versus `web`.',
  related:['for_each','moved','terraform state mv'] },

{ name:'moved', track:'iac', cat:'import', level:'intermediate',
  cert:['tf-associate:state','tf-associate:modules'],
  sum:'Tell Terraform a resource was renamed, in code, so it moves instead of replacing.',
  syntax:'moved { from = ADDRESS  to = ADDRESS }',
  flags:[['from / to','Old and new addresses; both must be valid address syntax'],
         ['(declarative)','Reviewed in a pull request and applied identically by everyone, unlike state mv'],
         ['(idempotent)','Once applied it is a no-op; safe to leave in place for a release, then delete'],
         ['modules too','moved { from = module.old  to = module.new } moves a whole subtree']],
  ex:[['moved { from = aws_instance.web  to = aws_instance.api }','A rename with no replacement'],
      ['moved { from = aws_instance.web[0]  to = aws_instance.web["api"] }','count to for_each, per resource'],
      ['moved { from = module.db  to = module.database }','Move an entire module']],
  tip:'This is the answer to "the plan wants to destroy and recreate everything after a refactor", and it is strictly better than `terraform state mv` because it lives in the code: reviewers see it, CI applies it, and every colleague gets the same result without being told to run something locally. Write the blocks, run `plan`, and the diff should collapse to no changes — if it does not, one of the addresses is wrong and the plan will tell you which. Leave them in for a release so anyone on an older checkout converges, then remove them.',
  related:['terraform state mv','for_each','count'] },

{ name:'variable', track:'iac', cat:'vars', level:'beginner',
  cert:['tf-associate:vars'],
  sum:'Inputs — with types and validation, or they are just untyped hope.',
  syntax:'variable "name" { type = … default = … sensitive = … validation { … } }',
  flags:[['type = map(object({...}))','Constrain the shape; a type error at plan beats a provider error at apply'],
         ['validation { condition = … error_message = … }','Reject bad input with a message that says what to do'],
         ['sensitive = true','Redacts it from output — and it is still plain text in state'],
         ['nullable = false','Refuse an explicit null'],
         ['no default','Makes it required, which is often what you want for an environment']],
  ex:[['variable "env" { type = string  validation { condition = contains(["dev","stg","prod"], var.env)  error_message = "env must be dev, stg or prod." } }','Fail at plan, not at apply'],
      ['variable "db_password" { type = string  sensitive = true }','Redacted in output; still in state'],
      ['terraform plan -var-file=env/prod.tfvars','Per-environment inputs from a file']],
  tip:'Precedence, highest first: `-var` on the command line, then `-var-file`, then `TF_VAR_` environment variables, then the `default`. A value that "is not taking effect" is almost always something higher in that list winning silently. Validation blocks are underused and pay for themselves: an environment name checked against a list fails in seconds at plan time rather than after ten minutes of apply against the wrong account. And `sensitive = true` is about display only — treat state as the secret it is.',
  related:['locals','terraform output','terraform console'] },

{ name:'locals', track:'iac', cat:'vars', level:'beginner',
  cert:['tf-associate:vars'],
  sum:'Named expressions — the place tagging conventions and naming rules should live.',
  syntax:'locals { name = expression }',
  flags:[['local.name','Referenced with the singular; declared with the plural. Everyone trips on this once'],
         ['merge(local.common_tags, { Role = "api" })','The standard tagging idiom'],
         ['(no dependency on inputs at runtime)','Locals are computed, not passed in']],
  ex:[['locals { common_tags = { Env = var.env, Owner = var.team, ManagedBy = "terraform" } }','Tag once, use everywhere'],
      ['locals { name_prefix = "${var.env}-${var.service}" }','One naming rule instead of forty string interpolations'],
      ['tags = merge(local.common_tags, { Name = "${local.name_prefix}-api" })','Compose rather than repeat']],
  tip:'The highest-value local in most configurations is a `common_tags` map merged into every resource, because tags are how cost allocation, ownership and incident routing all work — and doing it by hand means half the estate is untagged within a quarter. A `default_tags` block on the AWS provider does the same job at provider level and is better still where it is available. Keep locals for expressions used more than once; a local used in exactly one place usually just adds a hop for the reader.',
  related:['variable','terraform console'] },

{ name:'depends_on', track:'iac', cat:'vars', level:'intermediate',
  cert:['tf-associate:modules'],
  sum:'Force an ordering the graph cannot infer — and it usually can.',
  syntax:'resource "…" "…" { depends_on = [aws_iam_role_policy.x] }',
  flags:[['implicit dependencies','A reference to another resource\'s attribute already creates the edge'],
         ['depends_on on a module','Legal, and it serialises the whole module'],
         ['terraform graph','Render the dependency graph when the ordering is not obvious']],
  ex:[['depends_on = [aws_iam_role_policy.lambda_logs]','The classic: the function needs the policy attached before it runs'],
      ['terraform graph | dot -Tsvg > graph.svg','See what Terraform actually inferred'],
      ['terraform plan -parallelism=1','Serialise everything when debugging a suspected ordering problem']],
  tip:'Terraform builds its graph from references, so passing `aws_vpc.main.id` into a subnet is already a dependency and adding `depends_on` for it is noise. Reach for it only where the dependency is real but invisible to the graph — an IAM policy that must be attached before a service can assume the role is the canonical case, because nothing in the resource arguments refers to it. Overusing it serialises the apply and makes it slower for no benefit, and `depends_on` on a module is a blunt instrument that serialises everything inside it.',
  related:['module','terraform plan'] },

{ name:'lifecycle', track:'iac', cat:'modules', level:'intermediate',
  cert:['tf-associate:modules'],
  sum:'Change how Terraform treats a resource: what forces replacement, what it ignores, what it refuses.',
  syntax:'resource "…" "…" { lifecycle { prevent_destroy = true  ignore_changes = [tags] } }',
  flags:[['prevent_destroy = true','Terraform errors rather than destroying it. The seatbelt for databases and state buckets'],
         ['ignore_changes = [tags["LastScanned"]]','Stop fighting something else that writes the same attribute'],
         ['create_before_destroy = true','Stand the replacement up before removing the old one'],
         ['replace_triggered_by = [aws_instance.web.id]','Recreate this when something else changes'],
         ['ignore_changes = all','A blunt instrument that hides genuine drift']],
  ex:[['lifecycle { prevent_destroy = true }','On the production database, always'],
      ['lifecycle { ignore_changes = [ami] }','When an image pipeline owns the AMI, not this configuration'],
      ['lifecycle { create_before_destroy = true }','Replace a launch template or a security group with no gap in service']],
  tip:'`prevent_destroy` is the cheapest insurance there is on anything holding data, and the fact that it later blocks a legitimate destroy is exactly the interruption you want. `ignore_changes` is the right answer when another system genuinely owns an attribute — an autoscaler managing desired capacity, a scanner writing a tag — and the wrong answer when it is being used to silence a diff nobody has investigated, because that hides real drift permanently. `create_before_destroy` needs the resource to tolerate two existing at once, which names and ports often do not.',
  related:['terraform plan','moved'] },

{ name:'provider alias', track:'iac', cat:'providers', level:'advanced',
  cert:['tf-associate:providers'],
  sum:'Several configurations of one provider — multi-region and multi-account in one apply.',
  syntax:'provider "aws" { alias = "west" region = "us-west-2" }  ·  provider = aws.west',
  flags:[['alias = "west"','Names the alternate configuration'],
         ['provider = aws.west','Selects it on a resource'],
         ['providers = { aws = aws.west }','Passing an aliased provider into a module'],
         ['assume_role { role_arn = … }','Per-provider credentials, which is how one apply spans accounts']],
  ex:[['provider "aws" { alias = "dr"  region = "us-west-2" }','A second region for replicas or backups'],
      ['module "dr" { source = "./modules/app"  providers = { aws = aws.dr } }','Same module, other region'],
      ['provider "aws" { alias = "audit"  assume_role { role_arn = "arn:aws:iam::4444:role/tf" } }','Cross-account in one configuration']],
  tip:'Aliases are the supported way to do multi-region and multi-account, and they are also how a single blast radius quietly grows to span three accounts — an apply that can touch production in two regions and the audit account is one mistake away from a very bad afternoon. Modules do not inherit aliased providers implicitly; you pass them with a `providers` map, and forgetting that is a common and confusing error. Consider whether separate configurations with separate state would be safer than one apply that can reach everywhere.',
  related:['module','backend "s3"','terraform providers'] },

{ name:'dynamic', track:'iac', cat:'modules', level:'advanced',
  cert:['tf-associate:modules'],
  sum:'Generate repeated nested blocks from a collection.',
  syntax:'dynamic "ingress" { for_each = var.rules  content { … ingress.value … } }',
  flags:[['for_each','The collection, same semantics as the resource-level meta-argument'],
         ['content { }','The block body, referencing <label>.value and <label>.key'],
         ['iterator = rule','Rename the iterator when the label is unhelpful']],
  ex:[['dynamic "ingress" { for_each = var.rules  content { from_port = ingress.value.port  cidr_blocks = ingress.value.cidrs } }','Security group rules from a variable'],
      ['dynamic "setting" { for_each = var.settings  content { name = setting.key  value = setting.value } }','Key-value settings blocks'],
      ['terraform console  # then: var.rules','Check the shape before writing the block']],
  tip:'Useful and easy to overuse. A dynamic block that generates two rules is harder to read than two written-out blocks, and the failure mode of the whole idiom is a configuration nobody can review — reviewers approve what they can follow, and a nest of dynamics gets approved without being read. Reach for it when the collection genuinely varies by environment. For security group rules specifically, separate `aws_security_group_rule` resources with `for_each` often read better and produce far more legible plan diffs.',
  related:['for_each','terraform console','locals'] }

);
