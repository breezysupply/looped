/* AWS track — compute: instances, images, scaling and the load balancer in
   front of them. Every example is a read command; the mutating ones carry a
   warning in the tip, because this track has no sandbox to be wrong in.      */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

/* ── EC2 ────────────────────────────────────────────────────── */
{ name:'aws ec2 describe-instances', track:'aws', cat:'ec2', level:'beginner',
  cert:['aws-saa:compute','aws-sysops:monitoring'],
  sum:'The workhorse. Useless without --query and --filters, which is the whole skill.',
  syntax:'aws ec2 describe-instances [--instance-ids ID…] [--filters …] [--query …]',
  flags:[['--filters Name=tag:Env,Values=prod','Server-side filter — always cheaper than piping into grep'],
         ['--filters Name=instance-state-name,Values=running','Terminated instances linger in the API for an hour and skew every count'],
         ['--query','A JMESPath expression, applied client-side after the response arrives'],
         ['--output table','Readable at a glance; use text for scripts and json for jq'],
         ['--no-paginate','Off by default in v2 — the CLI pages for you, which hides how much you asked for']],
  ex:[['aws ec2 describe-instances --filters Name=instance-state-name,Values=running --query \'Reservations[].Instances[].[InstanceId,PrivateIpAddress,Tags[?Key==`Name`].Value|[0]]\' --output table','The listing you actually want: id, IP, name'],
      ['aws ec2 describe-instances --instance-ids i-0a1b2c3d --query \'Reservations[0].Instances[0].{state:State.Name,az:Placement.AvailabilityZone,sg:SecurityGroups[].GroupId,subnet:SubnetId}\'','One instance, only the fields a network problem needs'],
      ['aws ec2 describe-instances --filters Name=tag:Env,Values=prod Name=instance-type,Values=t3.* --query \'length(Reservations[].Instances[])\'','Count without printing anything']],
  tip:'The response shape trips everyone: instances are nested inside Reservations, so the path is always `Reservations[].Instances[]`. A reservation is a launch request, not a grouping you chose, and one launch of five instances is one reservation. If a `--query` returns an empty list when you know the instance exists, that nesting is usually why.',
  related:['aws ec2 describe-instance-status','aws ec2 get-console-output','aws ssm start-session'] },

{ name:'aws ec2 describe-instance-status', track:'aws', cat:'ec2', level:'beginner',
  cert:['aws-sysops:monitoring'],
  sum:'The two status checks — the difference between "AWS broke it" and "you broke it".',
  syntax:'aws ec2 describe-instance-status --instance-ids ID [--include-all-instances]',
  flags:[['--include-all-instances','Also report stopped instances; without it they are silently omitted'],
         ['--query \'InstanceStatuses[].{i:InstanceId,sys:SystemStatus.Status,inst:InstanceStatus.Status}\'','Both checks side by side']],
  ex:[['aws ec2 describe-instance-status --instance-ids i-0a1b2c3d','System and instance status plus any scheduled events'],
      ['aws ec2 describe-instance-status --filters Name=instance-status.status,Values=impaired','Every instance failing its own check'],
      ['aws ec2 describe-instance-status --instance-ids i-0a1b2c3d --query \'InstanceStatuses[0].Events\'','Scheduled retirement or a required reboot']],
  tip:'Two checks, two owners. **System status** covers the host, its network and its power — if that is impaired, it is AWS\'s problem and a stop/start moves you to new hardware (a reboot does not). **Instance status** covers your operating system — a failed boot, an exhausted filesystem, a kernel panic — and no amount of stopping and starting fixes it. Reading which one failed decides whether you open a support case or open a console.',
  related:['aws ec2 describe-instances','aws ec2 get-console-output'] },

{ name:'aws ec2 get-console-output', track:'aws', cat:'ec2', level:'intermediate',
  cert:['aws-sysops:monitoring'],
  sum:'The boot log of an instance you cannot reach. The first thing to try when SSH fails.',
  syntax:'aws ec2 get-console-output --instance-id ID [--latest] [--output text]',
  flags:[['--latest','Live-ish output on Nitro instances rather than the cached snapshot'],
         ['--output text','Otherwise you get base64 wrapped in JSON']],
  ex:[['aws ec2 get-console-output --instance-id i-0a1b2c3d --latest --output text | tail -50','The end of the boot, where the failure is'],
      ['aws ec2 get-console-output --instance-id i-0a1b2c3d --output text | grep -i "cloud-init\\|error\\|fail"','User-data failures announce themselves here'],
      ['aws ec2 get-console-screenshot --instance-id i-0a1b2c3d','A literal screenshot — the only way to read a Windows blue screen']],
  tip:'This is the command that separates guessing from knowing when an instance will not come back. A full root filesystem, an fstab entry for a volume that no longer exists, a kernel that will not boot after a patch, and a cloud-init script that hung all look identical from outside — unreachable — and all say so plainly in the console log. On non-Nitro instances the output can lag by several minutes, so an empty result is not proof of anything.',
  related:['aws ec2 describe-instance-status','aws ssm start-session'] },

{ name:'aws ssm start-session', track:'aws', cat:'ec2', level:'intermediate',
  cert:['aws-saa:secure-architectures','aws-sec:infrastructure-security'],
  sum:'A shell on an instance with no SSH port, no key pair and no public IP.',
  syntax:'aws ssm start-session --target i-INSTANCE [--document-name AWS-StartPortForwardingSession]',
  flags:[['--target','The instance id; no inbound rule is required'],
         ['--document-name AWS-StartPortForwardingSession','Tunnel a port instead of opening a shell'],
         ['--parameters \'{"portNumber":["3306"],"localPortNumber":["3306"]}\'','Reach a private RDS endpoint through the instance']],
  ex:[['aws ssm start-session --target i-0a1b2c3d','Interactive shell, audited in CloudTrail'],
      ['aws ssm start-session --target i-0a1b2c3d --document-name AWS-StartPortForwardingSessionToRemoteHost --parameters \'{"host":["db.internal"],"portNumber":["5432"],"localPortNumber":["5432"]}\'','Reach a private database without a bastion'],
      ['aws ssm describe-instance-information --query \'InstanceInformationList[].[InstanceId,PingStatus]\' --output table','Which instances the agent can actually reach']],
  tip:'Three things must be true and only one of them is on the instance: the SSM agent is running, the instance role includes `AmazonSSMManagedInstanceCore`, and the agent can reach the SSM endpoints — either through a NAT gateway or through the `ssm`, `ssmmessages` and `ec2messages` VPC endpoints. Missing the third is the usual cause of "the agent is installed but the instance is not managed". The reason to prefer this over SSH is not convenience: it removes port 22 from every security group and puts every session in CloudTrail.',
  related:['aws ec2 get-console-output','aws ec2 describe-instances'] },

{ name:'aws ec2 describe-instance-attribute', track:'aws', cat:'ec2', level:'intermediate',
  cert:['aws-sysops:deployment'],
  sum:'Read one attribute — user data, termination protection, the source/dest check.',
  syntax:'aws ec2 describe-instance-attribute --instance-id ID --attribute NAME',
  flags:[['--attribute userData','Base64; pipe through base64 -d to read what actually ran'],
         ['--attribute disableApiTermination','Whether termination protection is on'],
         ['--attribute sourceDestCheck','Must be false for an instance acting as a NAT or router'],
         ['--attribute instanceType','What it is now, which is not always what the template says']],
  ex:[['aws ec2 describe-instance-attribute --instance-id i-0a1b2c3d --attribute userData --query UserData.Value --output text | base64 -d','The script that ran at first boot'],
      ['aws ec2 describe-instance-attribute --instance-id i-0a1b2c3d --attribute sourceDestCheck','Why a routing instance drops traffic it should forward'],
      ['aws ec2 describe-instance-attribute --instance-id i-0a1b2c3d --attribute disableApiTermination','Why terminate keeps failing']],
  tip:'User data runs once, on the first boot, unless the script explicitly re-arms cloud-init. That is why an instance launched from a template with a fixed user-data script behaves differently after someone edits the script and reboots rather than replacing the instance — the edit has never run. `cloud-init` writes its output to `/var/log/cloud-init-output.log`, which is where to look on the instance and what to grep for in the console log when you cannot get on it.',
  related:['aws ec2 get-console-output','aws ec2 describe-launch-templates'] },

{ name:'aws ec2 describe-volumes', track:'aws', cat:'ec2', level:'beginner',
  cert:['aws-saa:resilient-architectures','aws-sysops:reliability'],
  sum:'EBS volumes, their attachment state, and whether they are the bottleneck.',
  syntax:'aws ec2 describe-volumes [--volume-ids ID…] [--filters …]',
  flags:[['--filters Name=attachment.instance-id,Values=i-…','Everything attached to one instance'],
         ['--filters Name=status,Values=available','Detached volumes — pure cost, and usually forgotten'],
         ['--query \'Volumes[].{id:VolumeId,size:Size,type:VolumeType,iops:Iops,del:Attachments[0].DeleteOnTermination}\'','The four fields that matter']],
  ex:[['aws ec2 describe-volumes --filters Name=status,Values=available --query \'sum(Volumes[].Size)\'','Gigabytes you are paying for and not using'],
      ['aws ec2 describe-volumes --filters Name=attachment.instance-id,Values=i-0a1b2c3d --query \'Volumes[].[VolumeId,Attachments[0].Device,Size,VolumeType]\' --output table','What is attached where'],
      ['aws ec2 modify-volume --volume-id vol-0a1b --size 200 --volume-type gp3','Grow and change type online — the filesystem still needs resizing afterwards']],
  tip:'`DeleteOnTermination` is the field that decides whether an incident costs you data or costs you money. It defaults to true for the root volume and false for volumes you attach later, which is the opposite of what most people assume in both directions. Growing a volume is online and instant; the filesystem does not grow with it, so it is always `modify-volume` then `growpart` then `resize2fs` or `xfs_growfs` on the instance.',
  related:['aws ec2 describe-snapshots','aws ec2 describe-instances'] },

{ name:'aws ec2 describe-snapshots', track:'aws', cat:'dr', level:'intermediate',
  cert:['aws-saa:resilient-architectures','aws-sysops:reliability'],
  sum:'Snapshots you own — and the ones nobody has pruned since 2021.',
  syntax:'aws ec2 describe-snapshots --owner-ids self [--filters …]',
  flags:[['--owner-ids self','Without it you get every public snapshot in the region, which is tens of thousands'],
         ['--filters Name=volume-id,Values=vol-…','The history of one volume'],
         ['--query \'sort_by(Snapshots,&StartTime)[-5:].[SnapshotId,StartTime,VolumeSize]\'','The five most recent']],
  ex:[['aws ec2 describe-snapshots --owner-ids self --query \'length(Snapshots)\'','How many you have; the answer is usually surprising'],
      ['aws ec2 describe-snapshots --owner-ids self --filters Name=volume-id,Values=vol-0a1b --query \'sort_by(Snapshots,&StartTime)[-1].[SnapshotId,StartTime]\'','The newest restore point for one volume'],
      ['aws backup list-recovery-points-by-backup-vault --backup-vault-name Default --query \'RecoveryPoints[].[CreationDate,Status]\' --output table','The managed equivalent, if AWS Backup owns the schedule']],
  tip:'Snapshots are incremental against the previous snapshot of the same volume but each one is independently restorable, so deleting an old snapshot never breaks a newer one — the blocks it still needs are retained. This is the fact that makes retention policies safe, and the one people are most afraid to act on. Note also that a snapshot is regional: a disaster-recovery plan that never copies snapshots to a second region is a backup plan, not a DR plan.',
  related:['aws ec2 describe-volumes','aws ec2 create-image'] },

/* ── Images and launch templates ─────────────────────────────── */
{ name:'aws ec2 describe-images', track:'aws', cat:'img', level:'beginner',
  cert:['aws-sysops:deployment'],
  sum:'Find an AMI without copying an id out of a blog post from three years ago.',
  syntax:'aws ec2 describe-images --owners OWNER [--filters …]',
  flags:[['--owners amazon | self | 099720109477','Canonical publishes Ubuntu under that account id; "amazon" covers AL2023'],
         ['--filters Name=name,Values=al2023-ami-*-x86_64','Name patterns are how AMI families are versioned'],
         ['--query \'sort_by(Images,&CreationDate)[-1].ImageId\'','The newest one, which is what you meant']],
  ex:[['aws ec2 describe-images --owners amazon --filters \'Name=name,Values=al2023-ami-2023*-x86_64\' \'Name=state,Values=available\' --query \'sort_by(Images,&CreationDate)[-1].[ImageId,Name]\' --output text','The current Amazon Linux 2023 image'],
      ['aws ssm get-parameter --name /aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64 --query Parameter.Value --output text','The same answer, one call, no sorting — AWS publishes it as a parameter'],
      ['aws ec2 describe-images --owners self --query \'sort_by(Images,&CreationDate)[].[ImageId,Name,CreationDate]\' --output table','Your own golden images, oldest first']],
  tip:'Prefer the SSM public parameter over `describe-images` when you just want "latest": it is a single call, it cannot be sorted wrongly, and it is the same source the console uses. Hard-coding an AMI id in a launch template is how a fleet ends up running an unpatched image for two years — the id is immutable, so nothing ever tells you it is stale. AMI ids are also region-specific, which is why a copy-pasted Terraform module fails in the second region.',
  related:['aws ec2 create-image','aws ec2 describe-launch-templates'] },

{ name:'aws ec2 create-image', track:'aws', cat:'img', level:'intermediate',
  cert:['aws-sysops:deployment'],
  sum:'Bake a golden image from a running instance.',
  syntax:'aws ec2 create-image --instance-id ID --name NAME [--no-reboot]',
  flags:[['--no-reboot','Does not quiesce the filesystem — faster, and the snapshot may be crash-consistent rather than clean'],
         ['--tag-specifications','Tag the image *and* its snapshots, or the snapshots become untraceable cost'],
         ['--description','The field future-you reads when deciding whether this image can be deleted']],
  ex:[['aws ec2 create-image --instance-id i-0a1b2c3d --name "app-base-$(date +%Y%m%d)" --description "AL2023 + agent + hardening" --tag-specifications \'ResourceType=image,Tags=[{Key=Owner,Value=platform}]\'','A dated, owned, described image'],
      ['aws ec2 wait image-available --image-ids ami-0a1b2c3d','Block until it can actually be launched'],
      ['aws ec2 deregister-image --image-ids ami-0a1b2c3d','Retire it — the snapshots survive and must be deleted separately']],
  tip:'Deregistering an AMI does not delete its snapshots, and that single asymmetry is responsible for a large share of unexplained EBS spend. The other trap is baking secrets or host identity into an image: an instance-specific SSH host key, a machine id, or a cached IAM credential in the image means every instance launched from it is the same machine as far as some tools are concerned. Bake the packages; supply the identity at boot.',
  related:['aws ec2 describe-images','aws ec2 describe-snapshots'] },

{ name:'aws ec2 describe-launch-templates', track:'aws', cat:'img', level:'intermediate',
  cert:['aws-sysops:deployment'],
  sum:'The versioned definition an Auto Scaling group launches from.',
  syntax:'aws ec2 describe-launch-templates | describe-launch-template-versions --launch-template-id ID',
  flags:[['--versions \'$Latest\' | \'$Default\'','Two different pointers, and an ASG follows whichever it was told to'],
         ['--query \'LaunchTemplateVersions[].[VersionNumber,LaunchTemplateData.ImageId]\'','Which AMI each version launches'],
         ['--source-version','Create a new version from an existing one instead of from nothing']],
  ex:[['aws ec2 describe-launch-template-versions --launch-template-id lt-0a1b --query \'LaunchTemplateVersions[].[VersionNumber,LaunchTemplateData.ImageId,LaunchTemplateData.InstanceType]\' --output table','The version history at a glance'],
      ['aws ec2 create-launch-template-version --launch-template-id lt-0a1b --source-version 4 --launch-template-data \'{"ImageId":"ami-0a1b2c3d"}\'','New AMI, everything else inherited'],
      ['aws autoscaling describe-auto-scaling-groups --query \'AutoScalingGroups[].[AutoScalingGroupName,LaunchTemplate.Version]\' --output table','Which version each group is pinned to']],
  tip:'`$Latest` and `$Default` are the whole story behind "I updated the template and nothing changed". A group pinned to a numbered version or to `$Default` ignores the version you just created; only a group set to `$Latest` picks it up, and only on the next instance launch. Neither replaces running instances — that is `start-instance-refresh`, and forgetting it is why a fleet can run three different AMIs at once.',
  related:['aws autoscaling describe-auto-scaling-groups','aws ec2 describe-images'] },

/* ── Auto Scaling and load balancing ─────────────────────────── */
{ name:'aws autoscaling describe-auto-scaling-groups', track:'aws', cat:'scale', level:'beginner',
  cert:['aws-saa:resilient-architectures','aws-sysops:reliability'],
  sum:'Desired, min, max, and what each instance is currently doing.',
  syntax:'aws autoscaling describe-auto-scaling-groups [--auto-scaling-group-names NAME…]',
  flags:[['--query \'AutoScalingGroups[].{n:AutoScalingGroupName,d:DesiredCapacity,min:MinSize,max:MaxSize}\'','The capacity triple'],
         ['--query \'AutoScalingGroups[0].Instances[].[InstanceId,LifecycleState,HealthStatus]\'','Per-instance state — InService, Pending, Terminating:Wait'],
         ['--query \'AutoScalingGroups[0].[HealthCheckType,HealthCheckGracePeriod]\'','The two settings behind most replacement loops']],
  ex:[['aws autoscaling describe-auto-scaling-groups --auto-scaling-group-names app-prod --query \'AutoScalingGroups[0].Instances[].[InstanceId,LifecycleState,HealthStatus]\' --output table','Who is in service and who is being replaced'],
      ['aws autoscaling set-desired-capacity --auto-scaling-group-name app-prod --desired-capacity 6','Scale now; the group may scale it back if a policy disagrees'],
      ['aws autoscaling suspend-processes --auto-scaling-group-name app-prod --scaling-processes ReplaceUnhealthy','Stop the churn while you debug — and write yourself a note to resume it']],
  tip:'`HealthCheckType: EC2` only watches the EC2 status checks, so an instance whose application is dead but whose OS is fine stays in service forever. `ELB` delegates the judgement to the target group, which is what you almost always want — paired with a `HealthCheckGracePeriod` longer than the application takes to boot. A grace period that is too short is the classic replacement loop: the instance is killed at 60 seconds while the app needs 90, forever.',
  related:['aws autoscaling describe-scaling-activities','aws elbv2 describe-target-health'] },

{ name:'aws autoscaling describe-scaling-activities', track:'aws', cat:'scale', level:'intermediate',
  cert:['aws-sysops:reliability'],
  sum:'The group\'s own account of why it launched or killed each instance.',
  syntax:'aws autoscaling describe-scaling-activities --auto-scaling-group-name NAME',
  flags:[['--max-items 10','Newest first, so a handful is usually enough'],
         ['--query \'Activities[].[StartTime,StatusCode,Cause]\'','Timestamp, outcome, and the sentence that explains it']],
  ex:[['aws autoscaling describe-scaling-activities --auto-scaling-group-name app-prod --max-items 5 --query \'Activities[].[StartTime,StatusCode,Description]\' --output table','What it did, most recent first'],
      ['aws autoscaling describe-scaling-activities --auto-scaling-group-name app-prod --query \'Activities[?contains(Cause,`health`)].Cause\'','Just the health-driven replacements'],
      ['aws autoscaling describe-scaling-activities --auto-scaling-group-name app-prod --query \'Activities[?StatusCode!=`Successful`]\'','Failed launches — quota, subnet capacity, bad AMI']],
  tip:'The `Cause` field is written in English and is usually the entire answer: "an instance was taken out of service in response to an ELB system health check failure" versus "in response to a difference between desired and actual capacity" are different incidents with different owners. A failed launch here is also the fastest way to spot a capacity or quota problem — `InsufficientInstanceCapacity` for a type in one AZ looks like an application failure from every other angle.',
  related:['aws autoscaling describe-auto-scaling-groups','aws elbv2 describe-target-health'] },

{ name:'aws elbv2 describe-target-health', track:'aws', cat:'scale', level:'beginner',
  cert:['aws-saa:high-performing','aws-sysops:reliability'],
  sum:'The single best command for a 502 or 503 behind an ALB.',
  syntax:'aws elbv2 describe-target-health --target-group-arn ARN',
  flags:[['--query \'TargetHealthDescriptions[].[Target.Id,TargetHealth.State,TargetHealth.Reason]\'','Id, state, and why'],
         ['--targets Id=i-0a1b,Port=8080','Check one target rather than all of them']],
  ex:[['aws elbv2 describe-target-health --target-group-arn $TG --query \'TargetHealthDescriptions[].[Target.Id,TargetHealth.State,TargetHealth.Reason,TargetHealth.Description]\' --output table','The whole picture in one table'],
      ['aws elbv2 describe-target-groups --query \'TargetGroups[].[TargetGroupName,Protocol,Port,HealthCheckPath]\' --output table','What the health check is actually requesting'],
      ['aws elbv2 describe-listeners --load-balancer-arn $LB --query \'Listeners[].[Port,Protocol,DefaultActions[0].TargetGroupArn]\' --output table','Which listener sends traffic where']],
  tip:'Read the `Reason` rather than the state. `Target.FailedHealthChecks` means the check reached the instance and got the wrong answer — wrong path, wrong status code, app still booting. `Target.Timeout` means it never got an answer at all, which is a security group or a process not listening. `Target.NotRegistered` means the ASG never added it, so the problem is upstream in scaling. `Elb.InternalError` is rare and genuinely AWS. Those four reasons route to four completely different investigations.',
  related:['aws autoscaling describe-auto-scaling-groups','aws ec2 describe-security-groups'] },

/* ── Quotas and sizing ──────────────────────────────────────── */
{ name:'aws service-quotas list-service-quotas', track:'aws', cat:'cost', level:'intermediate',
  cert:['aws-sysops:deployment'],
  sum:'The limit you hit at 2am that nobody knew existed.',
  syntax:'aws service-quotas list-service-quotas --service-code CODE',
  flags:[['--service-code ec2 | vpc | lambda | rds','Per service; `list-services` enumerates the codes'],
         ['get-service-quota --quota-code L-1216C47A','One quota — that code is Running On-Demand Standard instances'],
         ['list-requested-service-quota-change-history','Increases you already asked for, and whether they landed']],
  ex:[['aws service-quotas get-service-quota --service-code ec2 --quota-code L-1216C47A --query \'Quota.Value\'','vCPU limit for standard on-demand instances'],
      ['aws service-quotas list-service-quotas --service-code vpc --query \'Quotas[?contains(QuotaName,`per VPC`)].[QuotaName,Value]\' --output table','The VPC limits that bite during a migration'],
      ['aws service-quotas request-service-quota-increase --service-code ec2 --quota-code L-1216C47A --desired-value 512','Ask for more — it is a support ticket with an API in front of it']],
  tip:'EC2 on-demand limits are counted in **vCPUs, per family group, per region**, not in instances — so sixteen large instances and one enormous one can hit the same wall. Quota failures surface as `VcpuLimitExceeded` on a launch, which an Auto Scaling group reports as a failed activity rather than as an error anyone gets paged for. Checking quotas is part of capacity planning, and a CloudWatch alarm on quota utilisation is cheaper than the outage.',
  related:['aws autoscaling describe-scaling-activities','aws ec2 describe-instance-types'] },

{ name:'aws ec2 describe-instance-types', track:'aws', cat:'cost', level:'intermediate',
  cert:['aws-saa:cost-optimized','aws-sysops:cost'],
  sum:'What a type actually gives you, before you commit a fleet to it.',
  syntax:'aws ec2 describe-instance-types [--instance-types TYPE…] [--filters …]',
  flags:[['--filters Name=processor-info.supported-architecture,Values=arm64','Graviton types only'],
         ['--filters Name=network-info.ena-support,Values=required','Enhanced networking, which some older AMIs lack'],
         ['--query \'InstanceTypes[].[InstanceType,VCpuInfo.DefaultVCpus,MemoryInfo.SizeInMiB,NetworkInfo.NetworkPerformance]\'','The comparison table']],
  ex:[['aws ec2 describe-instance-types --instance-types m7g.large m6i.large --query \'InstanceTypes[].[InstanceType,VCpuInfo.DefaultVCpus,MemoryInfo.SizeInMiB]\' --output table','Graviton against x86, same size class'],
      ['aws ec2 describe-instance-type-offerings --location-type availability-zone --filters Name=instance-type,Values=m7g.large --query \'InstanceTypeOfferings[].Location\'','Which AZs actually offer it — the cause of many one-AZ launch failures'],
      ['aws ec2 describe-instance-types --filters Name=instance-storage-supported,Values=true --query \'InstanceTypes[].InstanceType\'','Types with local NVMe']],
  tip:'`describe-instance-type-offerings` is the underused half. Instance types are not available in every Availability Zone, and a subnet in the wrong AZ produces `Unsupported` or `InsufficientInstanceCapacity` at launch — which looks like a quota problem, an ASG problem or a Terraform problem depending on where you are standing. Check offerings per AZ before pinning a fleet to a type, especially for newer Graviton and GPU families.',
  related:['aws service-quotas list-service-quotas','aws ec2 describe-instances'] }

);
