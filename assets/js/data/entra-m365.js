/* Identity & M365 — the workloads: Intune, Exchange Online, Purview. The
   identity plane decides who gets in; these decide what happens once they do. */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

/* ── Intune ─────────────────────────────────────────────────── */
{ name:'Get-MgDeviceManagementManagedDevice', track:'entra', cat:'intune', level:'intermediate',
  cert:['ms-102:devices','az-104:identity'],
  sum:'Enrolled devices, their compliance state, and when they last checked in.',
  syntax:'Get-MgDeviceManagementManagedDevice [-ManagedDeviceId ID] [-Filter "…"] [-All]',
  flags:[['-Filter "complianceState eq \'noncompliant\'"','The list that matters when Conditional Access starts blocking people'],
         ['.LastSyncDateTime','A device that has not checked in cannot have evaluated a new policy'],
         ['.OperatingSystem / .OsVersion','Version floors are the most common compliance rule'],
         ['Get-MgDeviceManagementManagedDeviceCompliancePolicyState -ManagedDeviceId ID','Which policy it failed, not just that it failed']],
  ex:[['Get-MgDeviceManagementManagedDevice -Filter "complianceState eq \'noncompliant\'" -All | Select-Object DeviceName,UserPrincipalName,OsVersion,LastSyncDateTime','Non-compliant devices with the fields that explain why'],
      ['Get-MgDeviceManagementManagedDeviceCompliancePolicyState -ManagedDeviceId $id | Select-Object DisplayName,State','Per-policy result for one device'],
      ['Get-MgDeviceManagementManagedDevice -All | Where-Object { $_.LastSyncDateTime -lt (Get-Date).AddDays(-30) } | Select-Object DeviceName,UserPrincipalName','Devices that have stopped checking in entirely']],
  tip:'Compliance is evaluated by the device at check-in, not by the service continuously, so a device that has not synced recently keeps whatever state it last reported — which is why "I fixed it and it is still blocked" is usually a sync-interval problem rather than a policy one. The interaction to understand is with Conditional Access: a policy requiring a compliant device turns every compliance failure into a sign-in failure with AADSTS53000, so a tightened compliance rule and a CA policy together can lock out a fleet faster than either would alone. Check the grace period on the compliance policy before enabling anything.',
  related:['Get-MgIdentityConditionalAccessPolicy','Get-MgAuditLogSignIn'] },

{ name:'Get-MgDeviceManagementDeviceCompliancePolicy', track:'entra', cat:'intune', level:'intermediate',
  cert:['ms-102:devices'],
  sum:'What compliance actually requires, and how long a device gets before it is marked non-compliant.',
  syntax:'Get-MgDeviceManagementDeviceCompliancePolicy [-All]',
  flags:[['.ScheduledActionsForRule','The grace period before non-compliance bites — check this before enabling'],
         ['.AdditionalProperties','The platform-specific rules; the shape differs per operating system'],
         ['Get-MgDeviceManagementDeviceCompliancePolicyDeviceStatus -DeviceCompliancePolicyId ID','Per-device outcome for one policy'],
         ['Get-MgDeviceManagementDeviceConfiguration','Configuration profiles — settings pushed, as distinct from rules checked']],
  ex:[['Get-MgDeviceManagementDeviceCompliancePolicy -All | Select-Object DisplayName,@{n=\'Type\';e={$_.AdditionalProperties["@odata.type"]}}','Every compliance policy and its platform'],
      ['Get-MgDeviceManagementDeviceCompliancePolicyDeviceStatus -DeviceCompliancePolicyId $id | Group-Object Status','How many devices pass and fail one policy'],
      ['(Get-MgDeviceManagementDeviceCompliancePolicy -DeviceCompliancePolicyId $id).ScheduledActionsForRule.ScheduledActionConfigurations','The grace period, in hours']],
  tip:'Compliance policies **check** and report; configuration profiles **push** settings. Confusing them produces the classic wrong conclusion that a policy will fix a device when it will only judge it. The setting that decides whether a rollout is safe is the scheduled action grace period: zero hours means devices are marked non-compliant the moment they fail, which with a Conditional Access requirement is an immediate outage. A day or two of grace, and a report-only CA policy alongside, turns a fleet-wide lockout into a ticket queue.',
  related:['Get-MgDeviceManagementManagedDevice','Get-MgIdentityConditionalAccessPolicy'] },

/* ── Exchange Online ────────────────────────────────────────── */
{ name:'Get-MessageTrace', track:'entra', cat:'exo', level:'beginner',
  cert:['ms-102:exchange'],
  sum:'Where a message went — delivered, filtered, quarantined or never accepted.',
  syntax:'Get-MessageTrace -SenderAddress A -RecipientAddress B -StartDate D -EndDate D',
  flags:[['-StartDate / -EndDate','Ten days maximum through this cmdlet; older needs a historical search'],
         ['.Status','Delivered, Failed, Pending, Quarantined, FilteredAsSpam, Expanded'],
         ['Get-MessageTraceDetail -MessageTraceId ID -RecipientAddress B','The per-hop detail, including which transport rule matched'],
         ['-PageSize 5000','The default is small enough to mislead you']],
  ex:[['Get-MessageTrace -RecipientAddress jo@contoso.com -StartDate (Get-Date).AddDays(-2) -EndDate (Get-Date) | Select-Object Received,SenderAddress,Subject,Status','Everything that arrived for one person'],
      ['Get-MessageTraceDetail -MessageTraceId $id -RecipientAddress jo@contoso.com | Select-Object Event,Detail','Why it was not delivered'],
      ['Get-MessageTrace -SenderAddress noreply@vendor.com -StartDate … -EndDate … | Group-Object Status','How a whole sender is being treated']],
  tip:'Read `Status` before theorising. **FilteredAsSpam** or **Quarantined** means Exchange accepted the message and made a judgement, so the fix is a policy or an allow entry. **Failed** means it was rejected, and the detail carries the SMTP response. **No record at all** is the most informative outcome of the three: the message never reached Exchange Online, so the problem is DNS, the sending system, or a connector — and no amount of mailbox investigation will find it. Note the ten-day window; anything older needs a historical search, which is asynchronous.',
  related:['Get-TransportRule','Get-Mailbox','Get-InboundConnector'] },

{ name:'Get-TransportRule', track:'entra', cat:'exo', level:'intermediate',
  cert:['ms-102:exchange'],
  sum:'Mail flow rules — the invisible hand behind "some messages just disappear".',
  syntax:'Get-TransportRule [-Identity NAME]',
  flags:[['.State / .Priority','Enabled and ordered; lower priority number is evaluated first'],
         ['.StopRuleProcessing','A rule that stops processing hides every rule below it'],
         ['-Identity NAME | Format-List','The full condition and action set, which the table view truncates'],
         ['Get-InboundConnector / Get-OutboundConnector','Connectors, which act before rules do']],
  ex:[['Get-TransportRule | Select-Object Name,State,Priority,StopRuleProcessing | Sort-Object Priority','Every rule in evaluation order'],
      ['Get-TransportRule | Where-Object { $_.RedirectMessageTo -or $_.BlindCopyTo } | Select-Object Name,RedirectMessageTo,BlindCopyTo','Rules that copy mail elsewhere — a standing exfiltration check'],
      ['Get-TransportRule -Identity "External warning" | Format-List Conditions,Actions,ExceptIfSenderDomainIs','One rule in full']],
  tip:'Rules run in priority order and `StopRuleProcessing` truncates the chain, so a rule that looks correct can be unreachable because something above it stopped evaluation — reading them sorted by priority is the only way to see that. The other reason to know this cmdlet is security: a transport rule that redirects or blind-copies mail to an external address is a classic post-compromise persistence mechanism, and it survives password resets because it lives in the tenant rather than in the mailbox.',
  related:['Get-MessageTrace','Get-Mailbox','Search-UnifiedAuditLog'] },

{ name:'Get-Mailbox', track:'entra', cat:'exo', level:'beginner',
  cert:['ms-102:exchange'],
  sum:'Mailbox settings — forwarding, delegation, type, and the leaver questions.',
  syntax:'Get-Mailbox [-Identity UPN] [-ResultSize Unlimited] [-Filter "…"]',
  flags:[['.ForwardingSmtpAddress / .ForwardingAddress','External and internal forwarding — the first thing to check in a compromise'],
         ['.RecipientTypeDetails','UserMailbox, SharedMailbox, RoomMailbox — a shared mailbox needs no licence'],
         ['Get-MailboxPermission -Identity UPN','Who else can open it'],
         ['Get-InboxRule -Mailbox UPN','Client-side rules, where attacker forwarding usually hides']],
  ex:[['Get-Mailbox -ResultSize Unlimited | Where-Object { $_.ForwardingSmtpAddress } | Select-Object UserPrincipalName,ForwardingSmtpAddress','Every mailbox forwarding externally'],
      ['Get-InboxRule -Mailbox jo@contoso.com | Select-Object Name,Enabled,ForwardTo,DeleteMessage,MoveToFolder','Rules on one mailbox, including the hidden-name ones'],
      ['Set-Mailbox -Identity jo@contoso.com -Type Shared','Convert a leaver\'s mailbox so the licence can be reclaimed']],
  tip:'Two things to check in every suspected mailbox compromise, and they are cheap: `ForwardingSmtpAddress` at the mailbox level, and `Get-InboxRule` for client-side rules — attackers commonly create a rule named with a single character or a full stop that forwards to an external address and deletes the original, so the user never sees the replies. On the leaver side, converting a mailbox to Shared frees the licence while keeping the contents accessible for up to fifty gigabytes, which is usually the right answer to "we need to keep their mail".',
  related:['Get-MessageTrace','Get-TransportRule','Search-UnifiedAuditLog'] },

{ name:'Get-InboxRule', track:'entra', cat:'exo', level:'intermediate',
  cert:['ms-102:security','ms-102:exchange'],
  sum:'Client-side mailbox rules — the first thing to check in a suspected compromise.',
  syntax:'Get-InboxRule -Mailbox UPN [-Identity NAME]',
  flags:[['.ForwardTo / .ForwardAsAttachmentTo','Where copies are going, including outside the tenant'],
         ['.DeleteMessage','A rule that forwards and deletes means the user never sees the replies'],
         ['.MoveToFolder','RSS Subscriptions, Archive and Conversation History are the usual hiding places'],
         ['Remove-InboxRule -Mailbox UPN -Identity NAME','Delete it — record what it did first']],
  ex:[['Get-InboxRule -Mailbox jo@contoso.com | Select-Object Name,Enabled,ForwardTo,DeleteMessage,MoveToFolder','Every rule with the fields that matter'],
      ['Get-Mailbox -ResultSize Unlimited | ForEach-Object { Get-InboxRule -Mailbox $_.UserPrincipalName | Where-Object ForwardTo }','Sweep the tenant for forwarding rules'],
      ['Search-UnifiedAuditLog -StartDate … -EndDate … -Operations New-InboxRule,Set-InboxRule','When rules were created, and by which session']],
  tip:'Attackers create these within minutes of getting in, and the shape is consistent enough to recognise on sight: a name that is a single character or a full stop, forwarding to an external address, `DeleteMessage` set, and anything left moved to a folder nobody opens. That is why the victim\'s sent items look clean and why replies to fraudulent mail never reach them. Crucially the rule lives in the mailbox rather than in the credential, so it survives a password reset and a session revocation both — it has to be removed explicitly.',
  related:['Get-Mailbox','Search-UnifiedAuditLog','Get-TransportRule'] },

/* ── Purview ────────────────────────────────────────────────── */
{ name:'Get-DlpCompliancePolicy', track:'entra', cat:'purview', level:'advanced',
  cert:['ms-102:security','sc-400:information-protection'],
  sum:'Data loss prevention policies, their scope, and whether they are actually enforcing.',
  syntax:'Get-DlpCompliancePolicy [-Identity NAME]  ·  Get-DlpComplianceRule',
  flags:[['.Mode','Enable, TestWithNotifications, TestWithoutNotifications, Disable — test mode is the safe rollout'],
         ['.ExchangeLocation / .SharePointLocation / .OneDriveLocation','Where it applies; "All" is not the default'],
         ['Get-DlpComplianceRule -Policy NAME','The conditions and actions — the policy is only a container'],
         ['.BlockAccess / .NotifyUser','Whether it blocks or only warns']],
  ex:[['Get-DlpCompliancePolicy | Select-Object Name,Mode,ExchangeLocation,SharePointLocation','Every policy and whether it enforces'],
      ['Get-DlpComplianceRule | Select-Object Name,Policy,BlockAccess,NotifyUser,@{n=\'Types\';e={$_.ContentContainsSensitiveInformation.Name}}','What each rule detects and does'],
      ['Get-DlpDetailReport -StartDate (Get-Date).AddDays(-7) -EndDate (Get-Date)','What matched, before you turn enforcement on']],
  tip:'Roll DLP out in test mode with notifications first and read the detail report — a rule for credit card numbers matches far more than credit card numbers, and going straight to block breaks legitimate work and destroys trust in the whole programme. The other half of the story is that a policy is only a container: the conditions and actions live in the **rules**, so a policy that looks correct with no rules attached does nothing at all. Sensitivity labels are a separate mechanism that travels with the file; DLP inspects content in place.',
  related:['Search-UnifiedAuditLog','Get-TransportRule'] },

{ name:'Get-LabelPolicy', track:'entra', cat:'purview', level:'advanced',
  cert:['sc-400:information-protection','ms-102:security'],
  sum:'Sensitivity labels — protection that travels with the file, including outside the tenant.',
  syntax:'Get-LabelPolicy  ·  Get-Label [-Identity NAME]',
  flags:[['Get-Label | Select-Object DisplayName,ContentType,Priority','The label set and its order; priority defines the hierarchy'],
         ['.EncryptionEnabled','Whether the label applies rights-management encryption'],
         ['Get-LabelPolicy | Select-Object Name,Labels,ExchangeLocation','Which labels are published to whom'],
         ['.Settings','Default label, mandatory labelling, justification on downgrade']],
  ex:[['Get-Label | Select-Object DisplayName,Priority,@{n=\'Encrypts\';e={$_.EncryptionEnabled}} | Sort-Object Priority','The label hierarchy and which ones encrypt'],
      ['Get-LabelPolicy | Select-Object Name,@{n=\'Labels\';e={$_.Labels -join \', \'}},ModifiedBy','What is published, and to which users'],
      ['Get-Label -Identity "Confidential" | Select-Object -ExpandProperty EncryptionRightsDefinitions','Exactly who gets which rights on a labelled file']],
  tip:'A labelled and encrypted document carries its protection with it, so a file sent to a personal address is still unreadable without an authorised identity — which is the property that makes labels the right control for genuinely controlled data, and DLP the right control for detecting and blocking movement. Two practical constraints: a label must be **published** by a policy before anyone sees it, so a perfectly configured label nobody can apply is a common state; and encryption breaks some downstream processing, including certain DLP inspection and third-party tools, so it is a decision to make deliberately.',
  related:['Get-DlpCompliancePolicy','Search-UnifiedAuditLog'] },

{ name:'Get-MgSubscribedSku', track:'entra', cat:'lic', level:'beginner',
  cert:['ms-102:identity'],
  sum:'What the tenant owns, what is consumed, and how close you are to running out.',
  syntax:'Get-MgSubscribedSku',
  flags:[['.SkuPartNumber','The licence name, e.g. SPE_E3, ENTERPRISEPREMIUM, EMSPREMIUM'],
         ['.ConsumedUnits vs .PrepaidUnits.Enabled','Assigned against owned — the number to alert on'],
         ['.PrepaidUnits.Warning','Licences in a grace period after expiry'],
         ['.ServicePlans','What is inside the SKU, which is how you answer "do we already own that"']],
  ex:[['Get-MgSubscribedSku | Select-Object SkuPartNumber,@{n=\'Used\';e={$_.ConsumedUnits}},@{n=\'Owned\';e={$_.PrepaidUnits.Enabled}}','Consumption at a glance'],
      ['Get-MgSubscribedSku | Where-Object { $_.PrepaidUnits.Enabled - $_.ConsumedUnits -lt 5 } | Select-Object SkuPartNumber','Nearly exhausted — the cause of CountViolation errors on new starters'],
      ['(Get-MgSubscribedSku | Where-Object SkuPartNumber -eq "SPE_E3").ServicePlans | Select-Object ServicePlanName,ProvisioningStatus','What E3 actually includes']],
  tip:'Running out of a SKU does not fail loudly — group-based licensing records a `CountViolation` on the user and the person simply does not get the service, which surfaces days later as "the new starter cannot use Teams". An alert at ninety percent consumption is the cheap fix. The other question this answers is procurement-shaped: before buying an add-on, check `ServicePlans` on what you already own, because Entra ID P1, Intune and Defender for Office are bundled into several SKUs people do not realise they hold.',
  related:['Get-MgUserLicenseDetail','Get-MgGroup'] }

);
