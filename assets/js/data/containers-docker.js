/* Containers track — Docker: images, builds, runtime, registries, storage. */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

/* ── Runtime & lifecycle ────────────────────────────────────── */
{ name:'docker run', track:'containers', cat:'runtime', level:'beginner',
  cert:['cka:workloads-and-scheduling'],
  sum:'Create and start a container from an image — the flags are most of the learning.',
  syntax:'docker run [options] IMAGE [command]',
  flags:[['-d','Detached: run in the background and print the container id'],
         ['--rm','Delete the container when it exits — the default for anything throwaway'],
         ['-it','Interactive plus a TTY; the pair you want for a shell'],
         ['-p 8080:80','Publish host port 8080 to container port 80 (host:container)'],
         ['-e KEY=value','Set an environment variable'],
         ['--name web','Give it a name instead of a random one'],
         ['-v /data:/var/lib/app','Bind-mount a host path'],
         ['--network app-net','Attach to a user-defined network'],
         ['--memory 512m --cpus 1.5','Cap resources — without these a container can take the host down'],
         ['--restart unless-stopped','Restart policy for a long-running service']],
  ex:[['docker run --rm -it alpine sh','Throwaway shell for poking at something'],
      ['docker run -d --name web -p 8080:80 nginx:1.27','A published service, pinned to a version'],
      ['docker run --rm --memory 256m --cpus 0.5 myapp:2.1','Prove it behaves inside the limits it will get in production'],
      ['docker run --rm --network container:web nicolaka/netshoot ss -lntp','Borrow another container\'s network namespace to debug it']],
  tip:'The port mapping is `host:container` and getting it backwards is the most common first mistake. `-p 8080:80` means "the world reaches me on 8080, the process inside still listens on 80" — and the process must bind 0.0.0.0 inside the container, not 127.0.0.1, or the mapping forwards to nothing. Always set `--memory` and `--cpus`: an unbounded container is the OOM killer\'s problem, and it will pick a victim that is not necessarily yours.',
  related:['docker ps','docker logs','docker exec','Dockerfile'] },

{ name:'docker ps', track:'containers', cat:'runtime', level:'beginner',
  cert:['cka:troubleshooting'],
  sum:'List containers, and — with -a — the ones that already died, which is usually what you want.',
  syntax:'docker ps [-a] [--filter …] [--format …]',
  flags:[['-a','Include stopped and exited containers'],
         ['--filter status=exited','Narrow by state, name, label, ancestor'],
         ['--format "{{.Names}}\\t{{.Status}}"','Pick the columns; the default is too wide for a terminal'],
         ['-q','Ids only, for piping into another command'],
         ['-s','Show the writable layer size of each container']],
  ex:[['docker ps -a --filter status=exited','What died, and how long ago'],
      ['docker ps --format "table {{.Names}}\\t{{.Status}}\\t{{.Ports}}"','A readable three-column view'],
      ['docker stop $(docker ps -q --filter ancestor=myapp:2.0)','Stop everything from one image']],
  tip:'The moment a container is "not running", `docker ps` alone lies by omission — it only shows running containers, so a crash-looping service looks like nothing at all. `-a` plus the STATUS column gives you the exit code: `Exited (137)` is SIGKILL, which on a container almost always means the memory limit; `Exited (1)` is the application choosing to quit.',
  related:['docker logs','docker inspect','docker run'] },

{ name:'docker logs', track:'containers', cat:'runtime', level:'beginner',
  cert:['cka:troubleshooting'],
  sum:'Read a container\'s stdout and stderr — the only log there is, if the image is built correctly.',
  syntax:'docker logs [options] CONTAINER',
  flags:[['-f','Follow, like tail -f'],
         ['--tail 100','Last N lines instead of the whole history'],
         ['--since 15m','Bound the window — accepts durations or timestamps'],
         ['-t','Prefix timestamps, which the app usually does not']],
  ex:[['docker logs --tail 100 --since 15m web','The incident window, not the whole life of the container'],
      ['docker logs -f web 2>&1 | grep -i error','Follow and filter — stderr is a separate stream and grep only reads stdout without the redirect']],
  tip:'This reads the container\'s stdout/stderr, nothing else. An image that writes to a file inside itself produces empty `docker logs` and a confused engineer — which is why "log to stdout" is a container rule rather than a style preference. Logs also die with the container unless a logging driver ships them off, so on a crash-loop you are racing the restart: `docker logs --previous` does not exist in Docker (that is Kubernetes), so capture them or lose them.',
  related:['docker ps','kubectl logs','journalctl'] },

{ name:'docker exec', track:'containers', cat:'runtime', level:'beginner',
  cert:['cka:troubleshooting'],
  sum:'Run a command inside a container that is already running.',
  syntax:'docker exec [options] CONTAINER COMMAND',
  flags:[['-it','Interactive with a TTY, for a shell'],
         ['-u root','Run as a different user — useful when the image drops privileges'],
         ['-e KEY=v','Extra environment for this command only'],
         ['-w /app','Working directory']],
  ex:[['docker exec -it web sh','A shell inside — try sh before bash, most slim images have no bash'],
      ['docker exec web cat /etc/nginx/nginx.conf','Read the config the container actually has, not the one on the host'],
      ['docker exec -u root web apk add --no-cache curl','Install a tool into a running container for debugging only']],
  tip:'Two things people trip on. The image may have no shell at all — distroless and scratch images do not — and then the answer is `docker debug`, a sidecar, or `nsenter` from the host. And anything you install with `exec` vanishes on restart, which is correct: if you needed it permanently it belongs in the Dockerfile. Never let an `exec` fix become the fix.',
  related:['docker run','kubectl exec','nsenter'] },

{ name:'docker inspect', track:'containers', cat:'runtime', level:'intermediate',
  cert:['cka:troubleshooting'],
  sum:'The full JSON truth about a container or image: mounts, networks, env, limits, exit reason.',
  syntax:'docker inspect [--format …] NAME|ID',
  flags:[['--format \'{{.State.ExitCode}}\'','Pull one field instead of reading 200 lines of JSON'],
         ['--format \'{{json .NetworkSettings}}\'','A whole subtree as JSON'],
         ['-s','Include size information']],
  ex:[['docker inspect --format \'{{.State.ExitCode}} {{.State.OOMKilled}}\' web','Did it exit, or was it killed for memory?'],
      ['docker inspect --format \'{{range .Mounts}}{{.Source}} -> {{.Destination}}{{"\\n"}}{{end}}\' web','What is actually mounted where'],
      ['docker inspect --format \'{{.NetworkSettings.Networks}}\' web','Which networks it is on and its address on each']],
  tip:'`.State.OOMKilled` is the field that ends the "did the memory limit kill it?" argument, and it is nowhere in `docker ps`. Learn `--format` rather than piping to jq — it works on a box where jq is not installed, which is most of them.',
  related:['docker ps','docker stats','jq'] },

{ name:'docker stats', track:'containers', cat:'runtime', level:'beginner',
  cert:['cka:troubleshooting'],
  sum:'Live CPU, memory, network and block I/O per container, read from cgroups.',
  syntax:'docker stats [--no-stream] [CONTAINER…]',
  flags:[['--no-stream','One sample and exit, for scripts'],
         ['--format "table {{.Name}}\\t{{.MemUsage}}\\t{{.MemPerc}}"','Just the columns you need'],
         ['-a','Include stopped containers']],
  ex:[['docker stats --no-stream','A single snapshot of every container'],
      ['docker stats --no-stream --format "{{.Name}} {{.MemUsage}} {{.CPUPerc}}"','Scriptable output']],
  tip:'The MEM USAGE column is `used / limit`, and the limit is the cgroup limit, not the host\'s memory — so a container sitting at 505MiB/512MiB is one allocation away from being killed even though the host has 30GB free. This is why `free -h` inside a container is misleading: on older images it reports the *host* memory, because /proc is not namespaced.',
  related:['docker inspect','kubectl top','free'] },

{ name:'docker system prune', track:'containers', cat:'storage', level:'beginner',
  cert:['cka:cluster-architecture'],
  sum:'Reclaim disk from stopped containers, unused networks, dangling images and the build cache.',
  syntax:'docker system prune [-a] [--volumes] [-f]',
  flags:[['-a','Also remove images not used by any container, not just dangling ones'],
         ['--volumes','Also remove unused volumes — this is the one that deletes data'],
         ['--filter "until=168h"','Only things older than a week'],
         ['-f','No confirmation prompt']],
  ex:[['docker system df','Look before you delete: what is actually using the space'],
      ['docker system prune -f --filter "until=168h"','Safe routine cleanup — nothing from the last week'],
      ['docker builder prune','Just the build cache, which is usually the biggest single item']],
  tip:'This is the answer to "the build host is out of disk" and it is also a way to cause an outage. `--volumes` deletes any volume no *running* container references, which includes the database volume of a container you stopped ten minutes ago. Run `docker system df` first, prune with `--filter until=`, and never put `--volumes` in a cron job.',
  related:['df','du','docker volume'] },

/* ── Images & builds ────────────────────────────────────────── */
{ name:'Dockerfile', track:'containers', cat:'images', level:'beginner',
  cert:['cka:workloads-and-scheduling','sec+:security-architecture'],
  sum:'The build recipe. Layer order decides your build times; the final stage decides your attack surface.',
  syntax:'FROM … / RUN … / COPY … / USER … / ENTRYPOINT …',
  flags:[['FROM image:tag AS build','Name a stage so a later stage can copy from it'],
         ['COPY --from=build /out /app','Multi-stage: take the artefact, leave the toolchain behind'],
         ['RUN --mount=type=cache,target=/root/.cache','Cache a package directory across builds'],
         ['USER 10001','Drop root — the single highest-value line in the file'],
         ['ENTRYPOINT ["/app"] / CMD ["--flag"]','Exec form, so signals reach the process'],
         ['HEALTHCHECK CMD …','Lets the runtime know the difference between running and working']],
  ex:[['FROM golang:1.23 AS build\\nWORKDIR /src\\nCOPY go.mod go.sum ./\\nRUN go mod download\\nCOPY . .\\nRUN CGO_ENABLED=0 go build -o /out/app','Dependencies copied before source, so a code change does not re-download the world'],
      ['FROM gcr.io/distroless/static:nonroot\\nCOPY --from=build /out/app /app\\nUSER nonroot:nonroot\\nENTRYPOINT ["/app"]','Final stage with no shell, no package manager and no root']],
  tip:'Two rules carry most of the value. **Order layers least-changing first** — copy the dependency manifest and install before copying source, or every one-line code change rebuilds everything. And **use the exec form** of ENTRYPOINT: the shell form wraps your process in `/bin/sh -c`, so PID 1 is the shell, SIGTERM goes to the shell and not to you, and every deploy takes the full 30-second kill timeout before dying hard.',
  related:['docker build','docker images','Pod'] },

{ name:'docker build', track:'containers', cat:'images', level:'beginner',
  cert:['cka:workloads-and-scheduling'],
  sum:'Turn a Dockerfile and a context directory into an image.',
  syntax:'docker build [options] PATH',
  flags:[['-t name:tag','Tag it — an untagged image is a dangling image you will prune by accident'],
         ['--target build','Stop at a named stage, for debugging a multi-stage build'],
         ['--build-arg KEY=value','Pass a build-time variable (never a secret — it lands in history)'],
         ['--no-cache','Ignore the layer cache when you suspect it is stale'],
         ['--platform linux/amd64','Build for an architecture that is not yours'],
         ['--progress=plain','Full output instead of the collapsing UI, for CI logs']],
  ex:[['docker build -t myapp:2.1 .','The everyday form; the trailing dot is the build context'],
      ['docker build --target build -t debug . && docker run --rm -it debug sh','Get a shell in the toolchain stage when the final image has none'],
      ['docker build --platform linux/amd64 -t myapp:2.1 .','Build x86 images from an Apple Silicon laptop']],
  tip:'The build *context* — that trailing `.` — is tarred and sent to the daemon before anything runs, so a repository with a 2GB `.git` and no `.dockerignore` uploads all of it on every build. `.dockerignore` is not an optimisation, it is also a security control: without it a stray `.env` or `.aws/credentials` gets copied by a broad `COPY . .`.',
  related:['Dockerfile','docker images','docker system prune'] },

{ name:'docker images', track:'containers', cat:'images', level:'beginner',
  cert:['cka:workloads-and-scheduling'],
  sum:'List local images with their tags, ages and sizes.',
  syntax:'docker images [--filter …] [--format …]',
  flags:[['--filter dangling=true','Untagged layers left behind by rebuilds'],
         ['--format "{{.Repository}}:{{.Tag}} {{.Size}}"','Two columns instead of six'],
         ['--digests','Show the content digest, which is the only truly immutable reference'],
         ['-q','Ids only']],
  ex:[['docker images --filter dangling=true','What prune would reclaim'],
      ['docker image history myapp:2.1','Layer by layer, with the command that made each — and its size'],
      ['docker images --digests myapp','The sha256 to pin in production']],
  tip:'`docker image history` is the tool for "why is this image 1.4GB?" — it shows the size each instruction added, and the answer is almost always a `RUN apt-get install` without a cleanup in the same layer. Deleting a file in a later layer does not shrink the image; the bytes are still in the earlier layer. And never deploy `:latest`: tags are mutable pointers, so the image you tested and the image that ran can differ. Pin a digest.',
  related:['docker build','Dockerfile','docker system prune'] },

{ name:'docker compose', track:'containers', cat:'runtime', level:'beginner',
  cert:['cka:workloads-and-scheduling'],
  sum:'Run a multi-container stack from one declarative file — the local-development counterpart to a manifest.',
  syntax:'docker compose [-f file] up|down|ps|logs|exec …',
  flags:[['up -d','Start everything in the background'],
         ['down -v','Stop and remove, including named volumes — data loss, deliberately'],
         ['logs -f svc','Follow one service'],
         ['exec svc sh','Shell into a service by name, not container id'],
         ['config','Print the fully resolved file with variables substituted'],
         ['--profile debug','Bring up an optional group of services']],
  ex:[['docker compose up -d && docker compose ps','Start the stack and check what came up'],
      ['docker compose config','Resolve every variable and interpolation — the fastest way to debug a compose file'],
      ['docker compose logs -f --tail 50 api','Follow one service during a reproduction']],
  tip:'Services reach each other by *service name* on the default network — `db:5432`, not `localhost:5432` — which is the same DNS model Kubernetes uses and the concept worth carrying forward. `depends_on` waits for the container to start, not for the application inside to be ready, so a service that connects to a database on boot still needs a retry loop or a healthcheck condition.',
  related:['docker run','docker network','Service'] },

{ name:'docker network', track:'containers', cat:'netns', level:'intermediate',
  cert:['cka:services-and-networking','sec+:security-architecture'],
  sum:'Create and inspect the virtual networks containers attach to.',
  syntax:'docker network create|ls|inspect|connect NAME',
  flags:[['create app-net','A user-defined bridge — gives you DNS between containers'],
         ['ls','Every network, with its driver'],
         ['inspect bridge','Subnet, gateway and which containers are attached'],
         ['connect app-net web','Attach a running container to another network'],
         ['--driver host','No isolation at all: the container uses the host namespace']],
  ex:[['docker network create app-net','Make one; the default bridge deliberately has no DNS'],
      ['docker network inspect app-net --format \'{{range .Containers}}{{.Name}} {{.IPv4Address}}{{"\\n"}}{{end}}\'','Who is on it and at what address'],
      ['docker run --rm --network app-net nicolaka/netshoot dig api','Resolve a service name from inside the network']],
  tip:'The default `bridge` network has no service discovery — containers on it reach each other only by IP. A *user-defined* bridge adds an embedded DNS server, so `curl http://api:8080` works. That difference is why every compose file effectively creates its own network, and it is a common interview question phrased as "why does the container name resolve here but not there?"',
  related:['docker compose','ip netns','bridge','CoreDNS'] },

{ name:'docker volume', track:'containers', cat:'storage', level:'beginner',
  cert:['cka:storage'],
  sum:'Named storage that outlives the container — the only thing in a container that should.',
  syntax:'docker volume create|ls|inspect|rm NAME',
  flags:[['create pgdata','A named volume, managed by Docker'],
         ['inspect pgdata','Where it actually lives on the host'],
         ['ls -f dangling=true','Volumes no container references — candidates for deletion'],
         ['prune','Delete all of those, which is how people lose databases']],
  ex:[['docker run -d -v pgdata:/var/lib/postgresql/data postgres:16','Named volume: Docker manages the host path'],
      ['docker run -d -v "$PWD/src":/app:ro node:22','Bind mount, read-only — the shape for local development'],
      ['docker volume inspect pgdata --format \'{{.Mountpoint}}\'','Find the real path when you need to back it up']],
  tip:'Know the three kinds and when each is right: a **named volume** for data (Docker owns the path, survives `docker rm`), a **bind mount** for source you are editing (host path, exact control, and it will happily shadow a directory in the image), and **tmpfs** for secrets you do not want on disk. The container filesystem itself is the fourth kind and is always the wrong answer for anything you would miss.',
  related:['docker run','PersistentVolume','df'] },

{ name:'docker registry', track:'containers', cat:'registry', level:'intermediate',
  cert:['sec+:security-architecture','aws-saa:design-secure-architectures'],
  sum:'Pushing to and pulling from a registry, and the auth that goes with it.',
  syntax:'docker login|pull|push|tag REGISTRY/REPO:TAG',
  flags:[['tag local:1 reg/repo:1','A tag is a name that points at a digest; retagging copies nothing'],
         ['push reg/repo:1','Upload the layers the registry does not already have'],
         ['pull reg/repo@sha256:…','Pull by digest — immutable, unlike a tag'],
         ['login -u AWS --password-stdin','Never put a password in argv; it lands in shell history and ps']],
  ex:[['aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 1234.dkr.ecr.us-east-1.amazonaws.com','ECR auth: a 12-hour token, not a stored password'],
      ['az acr login --name myregistry','ACR auth through the Azure CLI'],
      ['docker pull myapp@sha256:3f2a…','Deploy the exact bytes you tested']],
  tip:'ECR and ACR tokens are short-lived by design, so a node that could pull yesterday and cannot today is usually an expired credential or a missing instance-role permission rather than a network problem. In Kubernetes the equivalent failure is `ImagePullBackOff`, and `kubectl describe pod` names it exactly — read the message before assuming the registry is down.',
  related:['docker images','kubectl describe','kubectl events'] }

);
