/* Identity & M365 — the access plane: Conditional Access, authentication
   methods, app registrations and federation. This is where a tenant is either
   secured or locked out, usually by the same policy.                        */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

/* ── Conditional Access ─────────────────────────────────────── */
{ name:'Get-MgIdentityConditionalAccessPolicy', track:'entra', cat:'ca', level:'intermediate',
  cert:['ms-102:identity','sc-300:access','az-104:identity'],
  sum:'Every Conditional Access policy, its state, and exactly who it applies to.',
  syntax:'Get-MgIdentityConditionalAccessPolicy [-ConditionalAccessPolicyId ID] [-All]',
  flags:[['.State','enabled, disabled, or enabledForReportingButNotEnforced — report-only is the safe way in'],
         ['.Conditions.Users','IncludeUsers, ExcludeUsers, IncludeGroups, ExcludeGroups — the assignment'],
         ['.Conditions.Applications','"All" here is the setting that most often catches something unintended'],
         ['.GrantControls','BuiltInControls plus Operator: AND requires all, OR requires any'],
         ['.SessionControls','Sign-in frequency, persistent browser, app-enforced restrictions']],
  ex:[['Get-MgIdentityConditionalAccessPolicy -All | Select-Object DisplayName,State','Every policy and whether it is live'],
      ['Get-MgIdentityConditionalAccessPolicy -All | Where-Object { $_.State -eq "enabled" -and $_.Conditions.Users.IncludeUsers -contains "All" -and -not $_.Conditions.Users.ExcludeUsers } | Select-Object DisplayName','Enabled policies targeting everyone with no exclusions — the lockout shape'],
      ['(Get-MgIdentityConditionalAccessPolicy -ConditionalAccessPolicyId $id).GrantControls | Format-List','What it actually requires']],
  tip:'Policies are **additive and every applicable one must be satisfied**, so adding a policy can only ever restrict — there is no "allow" that overrides another policy\'s block. Two things to check before enabling anything: `IncludeUsers: All` with an empty `ExcludeUsers` is the shape that locks out break-glass accounts, and a policy whose grant control is `block` with an application condition of `All` takes down service principals and legacy clients you have not thought about. `enabledForReportingButNotEnforced` shows the impact in the sign-in logs without enforcing it, and skipping that step is how tenants get locked out.',
  related:['Get-MgAuditLogSignIn','Get-MgUserAuthenticationMethod'] },

{ name:'New-MgIdentityConditionalAccessPolicy', track:'entra', cat:'ca', level:'advanced',
  cert:['sc-300:access','ms-102:identity'],
  sum:'Create a policy — and the exclusions that have to exist before you do.',
  syntax:'New-MgIdentityConditionalAccessPolicy -BodyParameter @{ displayName=…; state=…; conditions=…; grantControls=… }',
  flags:[['state = "enabledForReportingButNotEnforced"','Always start here. Always'],
         ['conditions.users.excludeUsers','The break-glass accounts, by object id'],
         ['grantControls.operator = "OR" | "AND"','OR satisfies with any one control; AND requires every one'],
         ['Update-MgIdentityConditionalAccessPolicy','Edit in place — and it takes effect within minutes']],
  ex:[['New-MgIdentityConditionalAccessPolicy -BodyParameter @{ displayName="Require MFA for admins"; state="enabledForReportingButNotEnforced"; conditions=@{ users=@{ includeRoles=@("62e90394-69f5-4237-9190-012177145e10"); excludeUsers=@($breakGlass1,$breakGlass2) }; applications=@{ includeApplications=@("All") } }; grantControls=@{ operator="OR"; builtInControls=@("mfa") } }','Report-only, admins only, break-glass excluded'],
      ['Update-MgIdentityConditionalAccessPolicy -ConditionalAccessPolicyId $id -State "enabled"','Promote it once the report-only data looks right'],
      ['Update-MgIdentityConditionalAccessPolicy -ConditionalAccessPolicyId $id -State "disabled"','The fastest rollback there is']],
  tip:'The order that keeps a tenant reachable: create two cloud-only break-glass accounts with long random passwords stored offline, exclude them from **every** Conditional Access policy, alert on their sign-ins, and only then start writing policies. Deploy report-only first and read the `AppliedConditionalAccessPolicies` field in the sign-in logs to see who *would* have been blocked. Changes propagate in minutes, not instantly, so a rollback is fast but not immediate — which is exactly why the excluded accounts have to exist in advance rather than be created during the incident.',
  related:['Get-MgIdentityConditionalAccessPolicy','Get-MgAuditLogSignIn'] },

/* ── Authentication methods ─────────────────────────────────── */
{ name:'Get-MgUserAuthenticationMethod', track:'entra', cat:'mfa', level:'intermediate',
  cert:['ms-102:identity','sc-300:identities'],
  sum:'What a user can actually authenticate with — the answer to "MFA is not working for them".',
  syntax:'Get-MgUserAuthenticationMethod -UserId UPN',
  flags:[['.AdditionalProperties["@odata.type"]','The method type: microsoftAuthenticator, fido2, phone, password, temporaryAccessPass'],
         ['Get-MgUserAuthenticationPhoneMethod -UserId UPN','Registered phone numbers'],
         ['Get-MgUserAuthenticationFido2Method -UserId UPN','Security keys'],
         ['New-MgUserAuthenticationTemporaryAccessPassMethod -UserId UPN -BodyParameter @{…}','A time-boxed pass for onboarding or recovery']],
  ex:[['Get-MgUserAuthenticationMethod -UserId jo@contoso.com | ForEach-Object { $_.AdditionalProperties["@odata.type"] }','Every method they have registered'],
      ['New-MgUserAuthenticationTemporaryAccessPassMethod -UserId jo@contoso.com -BodyParameter @{ lifetimeInMinutes=60; isUsableOnce=$true }','Recovery without weakening MFA'],
      ['Get-MgReportAuthenticationMethodUserRegistrationDetail -All | Where-Object { -not $_.IsMfaRegistered } | Select-Object UserPrincipalName','Everyone still unregistered — the gap before enforcement']],
  tip:'A user whose only registered method is a phone number is one SIM swap from compromise, and the registration report is how you find them before an attacker does. A **Temporary Access Pass** is the correct answer to "they lost their phone and cannot sign in": it is time-boxed, optionally single-use, auditable, and it lets them register a new method — unlike the tempting alternative of removing the MFA requirement, which is what an attacker with a stolen password is hoping you will do. Number matching and additional context in Authenticator are what blunt MFA-fatigue attacks.',
  related:['Get-MgAuditLogSignIn','Get-MgIdentityConditionalAccessPolicy'] },

/* ── Applications ───────────────────────────────────────────── */
{ name:'Get-MgApplication', track:'entra', cat:'apps', level:'intermediate',
  cert:['ms-102:identity','sc-300:apps'],
  sum:'The app registration — and the client secret that is about to expire.',
  syntax:'Get-MgApplication [-ApplicationId OBJECT_ID] [-Filter "appId eq \'…\'"] [-All]',
  flags:[['.PasswordCredentials','Client secrets, each with an EndDateTime — the field to alert on'],
         ['.KeyCredentials','Certificates; preferred over secrets for anything unattended'],
         ['.RequiredResourceAccess','The permissions the app asks for, delegated and application'],
         ['-Filter "appId eq \'…\'"','Note that appId and the object id are different identifiers']],
  ex:[['Get-MgApplication -All | Select-Object DisplayName,@{n=\'SecretExpires\';e={($_.PasswordCredentials.EndDateTime | Sort-Object)[0]}} | Sort-Object SecretExpires','Every app registration by soonest secret expiry'],
      ['Get-MgApplication -All | Where-Object { $_.PasswordCredentials.EndDateTime -lt (Get-Date).AddDays(30) } | Select-Object DisplayName,AppId','Expiring within the month'],
      ['(Get-MgApplication -ApplicationId $id).RequiredResourceAccess | ConvertTo-Json -Depth 5','What it is asking for, and whether that is still justified']],
  tip:'An expired client secret is one of the most common self-inflicted outages in a Microsoft estate, and it is entirely predictable — the expiry date is an attribute you can query, so an alert thirty days out costs nothing. Certificates are better than secrets for unattended workloads because the private key never travels. Two identifiers cause endless confusion: the **application (client) id** is the app\'s public identity, the **object id** is the directory record, and cmdlets differ over which one they want.',
  related:['Get-MgServicePrincipal','Get-MgOauth2PermissionGrant'] },

{ name:'Get-MgServicePrincipal', track:'entra', cat:'apps', level:'intermediate',
  cert:['ms-102:identity','sc-300:apps'],
  sum:'The tenant-local identity of an app — where assignment, consent and sign-in actually happen.',
  syntax:'Get-MgServicePrincipal [-ServicePrincipalId ID] [-Filter "…"] [-All]',
  flags:[['.AppRoleAssignmentRequired','If true, only assigned users may sign in — the source of AADSTS50105'],
         ['Get-MgServicePrincipalAppRoleAssignedTo -ServicePrincipalId ID','Who is assigned to the app'],
         ['Get-MgServicePrincipalAppRoleAssignment -ServicePrincipalId ID','Application permissions the app itself holds'],
         ['.AccountEnabled','A disabled service principal blocks every sign-in to that app']],
  ex:[['Get-MgServicePrincipal -Filter "displayName eq \'Salesforce\'" -Property id,appId,accountEnabled,appRoleAssignmentRequired | Format-List','The enterprise app behind a SAML integration'],
      ['Get-MgServicePrincipalAppRoleAssignedTo -ServicePrincipalId $sp -All | Select-Object PrincipalDisplayName,PrincipalType','Who and what is assigned'],
      ['Get-MgServicePrincipal -All -Filter "servicePrincipalType eq \'Application\'" | Where-Object { $_.KeyCredentials.Count -eq 0 -and $_.PasswordCredentials.Count -gt 0 }','App-only identities still using secrets rather than certificates']],
  tip:'The distinction that unlocks this whole area: an **application** is the global definition and lives in its home tenant, while a **service principal** is the local instance in your tenant — that is what you assign users to, grant consent on, disable, and see in sign-in logs. `AppRoleAssignmentRequired: true` with an unassigned user produces AADSTS50105, "not assigned to a role for the application", which reads like a permissions bug and is really an assignment one. Every enterprise app in the portal is a service principal.',
  related:['Get-MgApplication','Get-MgOauth2PermissionGrant','Get-MgAuditLogSignIn'] },

{ name:'Get-MgOauth2PermissionGrant', track:'entra', cat:'apps', level:'advanced',
  cert:['sc-300:apps','ms-102:security'],
  sum:'Delegated consent grants — where illicit consent attacks live.',
  syntax:'Get-MgOauth2PermissionGrant -All [-Filter "clientId eq \'…\'"]',
  flags:[['.ConsentType','AllPrincipals means an administrator consented for the whole tenant; Principal means one user did'],
         ['.Scope','The space-separated permission list actually granted'],
         ['.ResourceId','What it was granted against — usually the Microsoft Graph service principal'],
         ['Remove-MgOauth2PermissionGrant -OAuth2PermissionGrantId ID','Revoke it']],
  ex:[['Get-MgOauth2PermissionGrant -All | Where-Object { $_.Scope -match "Mail.Read|Files.Read.All|Mail.Send" } | Select-Object ClientId,ConsentType,Scope','Grants over mail and files — the ones that matter in a consent phishing incident'],
      ['Get-MgOauth2PermissionGrant -All | Where-Object ConsentType -eq "AllPrincipals" | Select-Object ClientId,Scope','Tenant-wide consent, which should be a short and deliberate list'],
      ['Get-MgServicePrincipal -ServicePrincipalId $clientId | Select-Object DisplayName,AppOwnerOrganizationId','Resolve the client id, and note whose tenant owns it']],
  tip:'Illicit consent grant attacks do not steal a password at all — the user is phished into approving an app that requests `Mail.Read` and `offline_access`, and the attacker then reads mail through Graph with a legitimate token, surviving every password reset. The defences are configuring the user consent policy so users can only consent to verified publishers and low-impact permissions, turning on the admin consent request workflow, and reviewing `AllPrincipals` grants regularly. During an incident, revoking the grant and the refresh tokens is what actually cuts access.',
  related:['Get-MgServicePrincipal','Search-UnifiedAuditLog'] },

/* ── Federation and provisioning ────────────────────────────── */
{ name:'Get-MgServicePrincipalSynchronizationJob', track:'entra', cat:'fed', level:'advanced',
  cert:['sc-300:apps','ms-102:identity'],
  sum:'SCIM provisioning to a SaaS app: whether it is running, and what it is failing on.',
  syntax:'Get-MgServicePrincipalSynchronizationJob -ServicePrincipalId ID',
  flags:[['.Status.Code','Active, Paused, Quarantine — quarantine means repeated failures have stopped it'],
         ['.Status.Progress','Objects processed and how many failed'],
         ['.Schedule.State','Whether the job is actually enabled'],
         ['Restart-MgServicePrincipalSynchronizationJob','Full re-sync; use it after fixing an attribute mapping']],
  ex:[['Get-MgServicePrincipalSynchronizationJob -ServicePrincipalId $sp | Select-Object Id,@{n=\'State\';e={$_.Status.Code}},@{n=\'LastRun\';e={$_.Status.LastExecution.TimeEnded}}','Is provisioning alive'],
      ['(Get-MgServicePrincipalSynchronizationJob -ServicePrincipalId $sp).Status.Quarantine | Format-List','Why it was quarantined, with the error reason'],
      ['Restart-MgServicePrincipalSynchronizationJob -ServicePrincipalId $sp -SynchronizationJobId $job -BodyParameter @{ criteria=@{ resetScope="Full" } }','Re-provision everything']],
  tip:'Quarantine is the state to watch for: after repeated failures — usually an expired credential on the target application or an attribute mapping that no longer matches — Entra backs the job off to a daily retry and provisioning silently stops. New joiners then do not appear in the SaaS application and nobody notices until someone complains. Understand the direction too: SCIM provisioning pushes from Entra to the application, while SAML only asserts identity at sign-in, so a user can authenticate perfectly and still have no account at the other end.',
  related:['Get-MgServicePrincipal','Get-MgAuditLogSignIn'] },

{ name:'Get-MgPolicyAuthorizationPolicy', track:'entra', cat:'entra', level:'advanced',
  cert:['sc-300:identities','ms-102:security'],
  sum:'The tenant-wide defaults that decide what ordinary users are allowed to do.',
  syntax:'Get-MgPolicyAuthorizationPolicy',
  flags:[['.DefaultUserRolePermissions.AllowedToCreateApps','Whether any user can register an application'],
         ['.DefaultUserRolePermissions.AllowedToCreateSecurityGroups','Whether any user can create groups'],
         ['.AllowInvitesFrom','Who may invite guests — everyone, admins only, or nobody'],
         ['.DefaultUserRolePermissions.PermissionGrantPoliciesAssigned','Which apps users may consent to on their own']],
  ex:[['Get-MgPolicyAuthorizationPolicy | Select-Object -ExpandProperty DefaultUserRolePermissions','The four defaults that shape a tenant\'s attack surface'],
      ['(Get-MgPolicyAuthorizationPolicy).AllowInvitesFrom','Guest invitation policy'],
      ['Update-MgPolicyAuthorizationPolicy -DefaultUserRolePermissions @{ allowedToCreateApps=$false }','Stop every user being able to register an app']],
  tip:'These defaults are permissive out of the box and they are where a tenant quietly becomes hard to govern: every user able to register applications means every user can create a service principal with credentials, and every user able to consent means a phishing page only has to convince one person. Tightening them is high value and low risk, but check first — some legitimate self-service flows depend on them, so read the current values and the sign-in logs before changing anything.',
  related:['Get-MgOauth2PermissionGrant','Get-MgApplication'] }

);
