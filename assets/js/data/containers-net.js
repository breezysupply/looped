/* Containers track — the network path a packet actually takes: namespaces and
   bridges on the host, DNS and kube-proxy inside the cluster, VPC and security
   groups around it. */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

/* ── Namespaces, veth, bridges ──────────────────────────────── */
{ name:'ip netns', track:'containers', cat:'netns', level:'intermediate',
  cert:['cka:services-and-networking','sec+:security-architecture'],
  sum:'Network namespaces: the kernel feature that gives a container its own stack. A container is not magic.',
  syntax:'ip netns list|add|exec NAME COMMAND',
  flags:[['list','Named namespaces under /var/run/netns'],
         ['add blue','Create one'],
         ['exec blue ip addr','Run a command inside it — its interfaces, routes, iptables, sockets'],
         ['ip -n blue link','Shorthand for exec … ip link']],
  ex:[['sudo ip netns add blue && sudo ip netns exec blue ip link set lo up','A namespace with only loopback — what a container starts as'],
      ['sudo ip netns exec blue ss -lntp','Which sockets are listening in there'],
      ['sudo ip netns exec blue ip route','Its routing table, which is not the host\'s'],
      ['ls -l /proc/$(pgrep -f myapp)/ns/net','The namespace a process is in, as an inode']],
  tip:'This is the concept that makes containers stop being magic. A container is a process with its own **network, mount, PID, UTS, IPC and user namespaces** plus a cgroup for limits — nothing more. Docker does not register its namespaces in /var/run/netns, so `ip netns list` looks empty on a busy host; that is why `nsenter -t PID -n` is the tool for reaching into a container\'s network from the host.',
  related:['nsenter','veth pair','docker network'] },

{ name:'nsenter', track:'containers', cat:'netns', level:'intermediate',
  cert:['cka:troubleshooting'],
  sum:'Run a command inside another process\'s namespaces — how you debug a container with no shell, from the host.',
  syntax:'nsenter -t PID -n|-m|-p|-u COMMAND',
  flags:[['-t PID','Target process — get it from docker inspect or crictl'],
         ['-n','Enter its network namespace'],
         ['-m','Mount namespace: see its filesystem'],
         ['-p','PID namespace'],
         ['-a','All of them']],
  ex:[['sudo nsenter -t $(docker inspect -f "{{.State.Pid}}" web) -n ss -lntp','What the container is really listening on, using the host\'s tools'],
      ['sudo nsenter -t 4821 -n tcpdump -i eth0 -nn port 8080','Packet capture inside a container that has no tcpdump'],
      ['sudo nsenter -t 4821 -m ls -l /etc/nginx','Read its filesystem from the host']],
  tip:'The escape hatch when the image is distroless: the container has no tools, but the *host* does, and namespaces are just something a process is in. This is also the honest security note — anyone with root on the node can enter any container\'s namespaces, which is why node access is production access and why you isolate tenants by node or by cluster, not by namespace.',
  related:['ip netns','docker exec','kubectl debug'] },

{ name:'veth pair', track:'containers', cat:'netns', level:'intermediate',
  cert:['cka:services-and-networking'],
  sum:'The virtual cable: one end in the container, the other on a bridge on the host.',
  syntax:'ip link add veth0 type veth peer name veth1',
  flags:[['type veth peer name X','Create both ends at once'],
         ['ip link set veth1 netns blue','Move one end into a namespace — it disappears from the host'],
         ['ip link set veth0 master docker0','Attach the host end to a bridge'],
         ['ip -d link show','The -d shows the type: veth, bridge, vxlan']],
  ex:[['ip -d link show type veth','Every virtual cable on the host — one per container'],
      ['cat /sys/class/net/eth0/iflink','Inside a container: the peer index, which identifies the host end'],
      ['ip link | grep -A1 "^$(docker exec web cat /sys/class/net/eth0/iflink):"','Match a container interface to its host side']],
  tip:'Draw the path once and it stays with you: process → eth0 in the container → veth pair → docker0 or cni0 bridge on the host → iptables NAT → host eth0 → the world. Every container network problem sits at one of those hops, and knowing the list turns "networking is broken" into four checkable places.',
  related:['ip netns','bridge','CNI'] },

{ name:'bridge', track:'containers', cat:'netns', level:'intermediate',
  cert:['cka:services-and-networking'],
  sum:'The virtual switch containers plug into — and the tool for inspecting it.',
  syntax:'bridge link|fdb show   ·   ip link show type bridge',
  flags:[['link show','Which interfaces are attached to which bridge'],
         ['fdb show br docker0','The forwarding database: MAC to port'],
         ['ip addr show docker0','The bridge address — the default gateway inside containers'],
         ['sysctl net.bridge.bridge-nf-call-iptables','Whether bridged traffic traverses iptables; Kubernetes requires 1']],
  ex:[['ip -br link show type bridge','Every bridge, one line each'],
      ['bridge link show','Which veth ends are on which bridge'],
      ['sysctl net.ipv4.ip_forward net.bridge.bridge-nf-call-iptables','The two sysctls that break a cluster when they are 0']],
  tip:'Two sysctls cause a large share of "the CNI installed fine but nothing can reach anything": `net.ipv4.ip_forward=0` stops the host routing between containers and the world, and `net.bridge.bridge-nf-call-iptables=0` means bridged traffic skips iptables so kube-proxy\'s rules never apply. Both are standard kubeadm preflight checks precisely because they fail silently.',
  related:['veth pair','iptables','kube-proxy'] },

{ name:'iptables', track:'containers', cat:'fw', level:'intermediate',
  cert:['sec+:security-architecture','cka:services-and-networking'],
  sum:'The packet-filter and NAT rules that container port publishing and kube-proxy are actually made of.',
  syntax:'iptables -t nat|filter -L CHAIN -n -v --line-numbers',
  flags:[['-t nat -L DOCKER -n','The rules docker run -p wrote'],
         ['-t nat -L KUBE-SERVICES -n','How a ClusterIP becomes a pod IP'],
         ['-L FORWARD -n -v','Whether traffic between interfaces is allowed at all'],
         ['-v','Packet and byte counters — a rule with zero hits is not matching'],
         ['-S','Print rules as the commands that would recreate them']],
  ex:[['sudo iptables -t nat -L DOCKER -n --line-numbers','The DNAT rule behind -p 8080:80'],
      ['sudo iptables -t nat -L KUBE-SERVICES -n | head -20','Service virtual IPs and their jump targets'],
      ['sudo iptables -L FORWARD -n -v | head','Counters here rising with a DROP policy explains a lot'],
      ['sudo nft list ruleset | head -40','The nftables equivalent on a modern distro']],
  tip:'`docker run -p` is not a feature of Docker so much as a DNAT rule plus a MASQUERADE rule, and a ClusterIP is a virtual address that exists only as an iptables jump — nothing is listening on it, which is why `ping` to a ClusterIP fails on a perfectly healthy cluster and confuses people. Read the `-v` counters: a rule that never increments is not the rule that is dropping your traffic.',
  related:['bridge','kube-proxy','Security Group vs NACL'] },

{ name:'conntrack', track:'containers', cat:'fw', level:'advanced',
  cert:['sec+:security-architecture'],
  sum:'The kernel connection-tracking table — what makes a security group "stateful", and what fills up.',
  syntax:'conntrack -L|-S|-D   ·   sysctl net.netfilter.nf_conntrack_max',
  flags:[['-S','Per-CPU statistics, including insert_failed and drop'],
         ['-L -p tcp --dport 5432','List tracked connections to one port'],
         ['-D -s 10.0.3.9','Delete entries for a source, to clear a stuck NAT mapping'],
         ['sysctl net.netfilter.nf_conntrack_count','How full the table is right now']],
  ex:[['sudo conntrack -S | head','Rising insert_failed means the table is full and packets are being dropped'],
      ['sysctl net.netfilter.nf_conntrack_count net.netfilter.nf_conntrack_max','Current against maximum'],
      ['dmesg -T | grep -i conntrack','"nf_conntrack: table full, dropping packet" — the smoking gun']],
  tip:'This is why a security group is stateful and a NACL is not: the reply to an allowed outbound connection matches a conntrack entry, so no inbound rule is needed. It is also a real limit — a busy NAT gateway or an ingress node fills the table, and the symptom is intermittent, random connection failures under load with nothing wrong in the application. `dmesg` names it outright.',
  related:['iptables','Security Group vs NACL','NAT Gateway vs IGW'] },

{ name:'MTU', track:'containers', cat:'netns', level:'advanced',
  cert:['cka:services-and-networking','cka:troubleshooting'],
  sum:'Packet size, and the overlay overhead that makes large responses hang while small ones succeed.',
  syntax:'ip link show dev eth0 | grep mtu   ·   ping -M do -s SIZE HOST',
  flags:[['ping -M do -s 1472','Do-not-fragment with a 1472-byte payload = exactly 1500 on the wire'],
         ['ip link set dev eth0 mtu 1450','Set it — CNIs usually manage this'],
         ['tracepath HOST','Reports the path MTU where it changes'],
         ['ethtool -k eth0 | grep segmentation','Offloads that can mask the problem locally']],
  ex:[['ping -M do -s 1472 10.0.3.9','Succeeds at 1500 MTU, fails with "Frag needed" below it'],
      ['ping -M do -s 1422 10.0.3.9','The same test for a 1450-MTU overlay'],
      ['ip -br link | awk \'{print $1, $NF}\'','Every interface MTU on the host at a glance']],
  tip:'The signature is unmistakable once you have seen it: the TCP handshake succeeds, small requests work, and anything large hangs and times out. VXLAN takes 50 bytes and WireGuard 60, so an overlay on a 1500-byte network must run about 1450 — and AWS jumbo frames at 9001 make it worse when traffic crosses a VPC peering that does not support them. If ICMP "fragmentation needed" is blocked by a firewall, path MTU discovery cannot work and the connection just hangs.',
  related:['veth pair','CNI','tcpdump'] },

{ name:'CNI', track:'containers', cat:'netns', level:'intermediate',
  cert:['cka:services-and-networking','cka:cluster-architecture'],
  sum:'The plugin that gives each pod an IP and wires it to the network. Which one you have changes the failure modes.',
  syntax:'/etc/cni/net.d/*.conflist   ·   /opt/cni/bin/',
  flags:[['AWS VPC CNI','Pods get real VPC IPs from ENIs — security groups apply, and ENI limits cap pods per node'],
         ['Calico','Overlay or BGP, and a mature NetworkPolicy implementation'],
         ['Cilium','eBPF instead of iptables; can replace kube-proxy'],
         ['flannel','Simple VXLAN overlay — and it silently ignores NetworkPolicy'],
         ['Azure CNI','Pods on the VNet, same real-IP model as the AWS CNI']],
  ex:[['ls /etc/cni/net.d/','Which plugin is configured on this node'],
      ['kubectl get pods -n kube-system -l k8s-app=aws-node -o wide','The CNI daemonset — a node missing it cannot start pods'],
      ['kubectl describe node NODE | grep -i "pods:"','Max pods on this node, which on the AWS CNI is an ENI arithmetic result']],
  tip:'Know which CNI you are on before you debug anything, because the answers differ. On the AWS VPC CNI a pod has a real VPC address, so security groups and NACLs apply to pod traffic and `Insufficient IP addresses` is a subnet-sizing problem rather than a Kubernetes one. On flannel, NetworkPolicy manifests apply cleanly and do nothing at all — a security control that silently is not there.',
  related:['NetworkPolicy','veth pair','VPC & Subnets'] },

/* ── Cluster DNS and proxying ───────────────────────────────── */
{ name:'CoreDNS', track:'containers', cat:'dns', level:'intermediate',
  cert:['cka:services-and-networking','cka:troubleshooting'],
  sum:'Cluster DNS: the names pods resolve, and the search-path behaviour that surprises people.',
  syntax:'svc.namespace.svc.cluster.local   ·   /etc/resolv.conf in the pod',
  flags:[['api','Resolves inside the same namespace via the search path'],
         ['api.payments','Cross-namespace'],
         ['api.payments.svc.cluster.local','Fully qualified — always works, no search path needed'],
         ['ndots:5','Why unqualified external names take five failed lookups first'],
         ['dnsPolicy: ClusterFirst','The default; Default means "use the node\'s resolver"']],
  ex:[['kubectl exec api-7d9f -- cat /etc/resolv.conf','The search path and ndots the pod was given'],
      ['kubectl exec api-7d9f -- nslookup api.payments.svc.cluster.local','Resolve fully qualified, which isolates search-path problems'],
      ['kubectl -n kube-system logs -l k8s-app=kube-dns --tail=50','CoreDNS\'s own view'],
      ['kubectl get svc -n kube-system kube-dns','The DNS service IP, which should match resolv.conf']],
  tip:'`ndots:5` is the one to understand: any name with fewer than five dots is tried against every search-path suffix first, so `api.example.com` becomes four failed cluster lookups before the real one. That is why external DNS from inside a cluster is slower than expected and why a trailing dot — `api.example.com.` — is a legitimate fix. Test with the fully qualified name first: if that works and the short name does not, it is search path, not DNS.',
  related:['Service','kube-proxy','dig'] },

{ name:'kube-proxy', track:'containers', cat:'svc', level:'intermediate',
  cert:['cka:services-and-networking'],
  sum:'What actually turns a Service IP into a pod IP on every node.',
  syntax:'mode: iptables | ipvs | (replaced by eBPF)',
  flags:[['iptables mode','A chain per Service, random probability per endpoint — the default'],
         ['ipvs mode','Kernel load balancer; scales better past a few thousand Services'],
         ['ipvsadm -Ln','Read the IPVS table when in that mode'],
         ['externalTrafficPolicy: Local','Preserves the client IP but only routes to pods on the receiving node']],
  ex:[['kubectl -n kube-system get ds kube-proxy -o wide','It runs on every node; a node missing it has broken Services'],
      ['sudo iptables -t nat -L KUBE-SVC-XXXX -n','The rule chain for one Service'],
      ['sudo ipvsadm -Ln | head','The IPVS view, if that mode is in use']],
  tip:'A ClusterIP is not an interface and nothing listens on it — it is a NAT rule evaluated on the node making the call, which is why you cannot ping a Service IP and why the failure is per-node: if kube-proxy is down on one node, pods *on that node* cannot reach Services while everything else is fine. `externalTrafficPolicy: Local` is the usual cause of "some requests get a connection refused" — nodes with no local pod have nothing to forward to.',
  related:['Service','iptables','CNI'] },

/* ── The cloud network around it ────────────────────────────── */
{ name:'VPC & Subnets', track:'containers', cat:'vpc', level:'beginner',
  cert:['aws-saa:design-secure-architectures','az104:configure-virtual-networking'],
  sum:'The address space, how it is carved up, and what makes a subnet public.',
  syntax:'VPC CIDR → subnet per AZ → route table → gateway',
  flags:[['VPC CIDR','/16 is conventional; it cannot be shrunk later, only extended'],
         ['Subnet','Lives in exactly one AZ — this is why multi-AZ means multiple subnets'],
         ['Route table','What makes a subnet public: a 0.0.0.0/0 route to an internet gateway'],
         ['Reserved addresses','AWS takes 5 per subnet, Azure 5 — a /28 gives you 11 usable'],
         ['Secondary CIDR','How you grow when pods on the VPC CNI exhaust the space']],
  ex:[['aws ec2 describe-subnets --filters Name=vpc-id,Values=vpc-0a1b --query "Subnets[].{Id:SubnetId,AZ:AvailabilityZone,CIDR:CidrBlock,Free:AvailableIpAddressCount}" --output table','Where the addresses have gone'],
      ['aws ec2 describe-route-tables --filters Name=association.subnet-id,Values=subnet-0c2d','Public or private, decided by the 0.0.0.0/0 target']],
  tip:'"Public subnet" is not a setting — it is a subnet whose route table sends 0.0.0.0/0 to an internet gateway. Nothing else distinguishes it. Size subnets generously when pods take VPC addresses: a /24 gives 251 usable, and on the AWS VPC CNI each node reserves a block of them in advance, so `Insufficient IP addresses` arrives long before you expected it.',
  related:['Security Group vs NACL','NAT Gateway vs IGW','CNI'] },

{ name:'Security Group vs NACL', track:'containers', cat:'fw', level:'beginner',
  cert:['aws-saa:design-secure-architectures','sec+:security-architecture','az104:configure-virtual-networking'],
  sum:'Stateful instance firewall against stateless subnet filter — the single most reliable cloud interview question.',
  syntax:'SG: allow-only, stateful, on the ENI · NACL: allow+deny, stateless, on the subnet',
  flags:[['Security group','Stateful: the reply to an allowed flow is automatically allowed'],
         ['Security group','Allow rules only — there is no deny, so the default is deny-by-omission'],
         ['Security group','Can reference another security group as a source, which NACLs cannot'],
         ['NACL','Stateless: return traffic needs its own explicit rule, on ephemeral ports'],
         ['NACL','Numbered rules evaluated in order, first match wins, and deny exists'],
         ['Azure NSG','Stateful like a security group, but with priorities and explicit deny']],
  ex:[['aws ec2 describe-security-groups --group-ids sg-0a1b --query "SecurityGroups[].IpPermissions"','Inbound rules on one group'],
      ['aws ec2 describe-network-acls --filters Name=association.subnet-id,Values=subnet-0c2d','The subnet-level rules, both directions'],
      ['az network nsg rule list -g rg --nsg-name nsg1 -o table','The Azure equivalent']],
  tip:'Learn to say this cleanly: **a security group is stateful and allow-only, attached to the ENI; a NACL is stateless with explicit deny, attached to the subnet.** The practical consequence is the giveaway — if outbound works and the reply never arrives, suspect the NACL, because a stateless filter needs an inbound rule for ephemeral ports 1024-65535 that people forget. And SG-referencing-SG is how you express "the app tier may reach the database tier" without hardcoding any address.',
  related:['NetworkPolicy','conntrack','VPC & Subnets'] },

{ name:'NAT Gateway vs IGW', track:'containers', cat:'vpc', level:'intermediate',
  cert:['aws-saa:design-resilient-architectures','aws-saa:design-cost-optimized-architectures'],
  sum:'How private things reach out, how public things are reached, and where the surprise bill comes from.',
  syntax:'IGW: bidirectional, needs a public IP · NAT GW: outbound only, in a public subnet',
  flags:[['Internet gateway','Attached to the VPC; a resource also needs a public IP and a route'],
         ['NAT gateway','Lives in a *public* subnet, serves *private* subnets, outbound only'],
         ['One per AZ','A single NAT gateway is a single-AZ dependency and cross-AZ data charges'],
         ['VPC endpoint (Gateway)','S3 and DynamoDB, free, keeps that traffic off the NAT gateway'],
         ['VPC endpoint (Interface)','Most other services, an ENI in your subnet, hourly plus data']],
  ex:[['aws ec2 describe-nat-gateways --query "NatGateways[].{Id:NatGatewayId,Subnet:SubnetId,State:State}" --output table','Where they are and whether they are per-AZ'],
      ['aws ec2 describe-vpc-endpoints --query "VpcEndpoints[].{Svc:ServiceName,Type:VpcEndpointType}" --output table','What is already bypassing the NAT gateway']],
  tip:'Two things carry the answer. **Placement**: a NAT gateway sits in a public subnet and is routed to *from* private subnets — putting it in the private subnet is the classic mistake and produces silent timeouts. **Cost**: NAT gateways charge per hour and per GB, so an ECS or EKS cluster pulling images and writing to S3 through one is a line item people notice; a gateway VPC endpoint for S3 is free and removes most of it.',
  related:['VPC & Subnets','Security Group vs NACL','docker registry'] },

{ name:'ECS vs EKS vs AKS', track:'containers', cat:'ops', level:'intermediate',
  cert:['aws-saa:design-resilient-architectures','az104:deploy-and-manage-compute'],
  sum:'The managed options, what each actually manages, and how to choose without hand-waving.',
  syntax:'ECS (+Fargate) · EKS · AKS',
  flags:[['ECS','AWS-native orchestrator; task definitions, not manifests. No control plane to run'],
         ['Fargate','No nodes at all — you stop patching hosts and lose daemonsets and node access'],
         ['EKS','Managed Kubernetes control plane; you still own nodes, CNI, add-ons and upgrades'],
         ['AKS','The same shape on Azure, with a free control-plane tier'],
         ['Portability','Kubernetes manifests move between clouds; ECS task definitions do not']],
  ex:[['aws ecs describe-services --cluster prod --services api --query "services[].{Desired:desiredCount,Running:runningCount,Status:status}"','Whether an ECS service is actually at capacity'],
      ['aws ecs describe-tasks --cluster prod --tasks TASKARN --query "tasks[].stoppedReason"','Why a task stopped — the ECS equivalent of describe pod'],
      ['aws eks update-kubeconfig --name prod --region us-east-1','Point kubectl at an EKS cluster']],
  tip:'Answer the choice on operational burden rather than preference. ECS on Fargate is the least to run and the least to know, and it is genuinely the right answer for a small team with a handful of services. EKS or AKS earn their complexity when you need the ecosystem — operators, CRDs, a service mesh — or portability between clouds. Saying "Kubernetes, because it is standard" without naming what you would then own is the answer interviewers are probing for.',
  related:['Deployment','CNI','docker registry'] }

);
