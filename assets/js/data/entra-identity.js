/* Identity & M365 — the directory itself: users, groups, roles, PIM, licensing
   and the hybrid seam where on-premises Active Directory meets the cloud.    */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

/* ── Objects ────────────────────────────────────────────────── */
{ name:'Get-MgUser', track:'entra', cat:'entra', level:'beginner',
  cert:['ms-102:identity','az-104:identity'],
  sum:'One user or many — and the properties you need are not returned by default.',
  syntax:'Get-MgUser [-UserId UPN_OR_ID] [-Filter "…"] [-Property p1,p2] [-All]',
  flags:[['-Property','Most attributes are omitted unless named — this is the flag people miss'],
         ['-Filter "startsWith(displayName,\'Jo\')"','Server-side OData filtering'],
         ['-Search "displayName:jo" -ConsistencyLevel eventual','Search needs the eventual consistency header'],
         ['-All','Page through everything; without it you get the first page only'],
         ['-ExpandProperty manager','Pull a relationship in the same call']],
  ex:[['Get-MgUser -UserId jo@contoso.com -Property id,displayName,accountEnabled,onPremisesSyncEnabled,userType,signInActivity | Format-List','The fields a lifecycle question actually needs'],
      ['Get-MgUser -Filter "accountEnabled eq false" -All -Property displayName,userPrincipalName | Measure-Object','How many disabled accounts are still sitting there'],
      ['Get-MgUser -Filter "userType eq \'Guest\'" -All -Property displayName,createdDateTime,signInActivity','Guests, with when they last signed in']],
  tip:'The default property set is deliberately small, so `Get-MgUser` returning `$null` for an attribute usually means you did not ask for it rather than that it is empty — `-Property` is the fix and the single most common confusion with this SDK. Two fields worth knowing: `onPremisesSyncEnabled` tells you whether the object is mastered on-premises, which decides where a change has to be made; and `signInActivity` needs `AuditLog.Read.All` on top of the user scope, which is why it silently comes back empty for many people.',
  related:['Get-MgUserLicenseDetail','Get-MgGroupMember','Update-MgUser'] },

{ name:'Get-MgGroup', track:'entra', cat:'entra', level:'beginner',
  cert:['ms-102:identity','az-104:identity'],
  sum:'Groups, and the three kinds that behave completely differently.',
  syntax:'Get-MgGroup [-GroupId ID] [-Filter "…"] [-Property p1,p2] [-All]',
  flags:[['-Filter "groupTypes/any(c:c eq \'DynamicMembership\')"','Dynamic groups, whose membership you cannot edit'],
         ['-Property id,displayName,groupTypes,securityEnabled,mailEnabled,membershipRule','What kind of group it actually is'],
         ['-Filter "onPremisesSyncEnabled eq true"','Groups mastered in on-premises AD — cloud edits will be overwritten'],
         ['Get-MgGroupMember -GroupId ID -All','Members; a separate call, and paged']],
  ex:[['Get-MgGroup -Filter "displayName eq \'Sales\'" -Property id,groupTypes,membershipRule,onPremisesSyncEnabled | Format-List','What kind of Sales group is this, and where is it mastered'],
      ['Get-MgGroupMember -GroupId $id -All | ForEach-Object { $_.AdditionalProperties.userPrincipalName }','Members as UPNs rather than object ids'],
      ['Get-MgGroup -Filter "groupTypes/any(c:c eq \'DynamicMembership\')" -All -Property displayName,membershipRule','Every dynamic group and its rule']],
  tip:'Three kinds and they fail differently. A **security group** grants access and can be assigned to roles; a **Microsoft 365 group** also creates a mailbox, a SharePoint site and a Teams team, so deleting one deletes far more than a membership list; and a **dynamic** group computes membership from a rule, so `Add-MgGroupMember` against it fails and a user who "will not stay in the group" is being evicted by the rule every cycle. A group with `onPremisesSyncEnabled` is mastered on-premises and every cloud edit is transient.',
  related:['Get-MgUser','Get-MgGroupMember','Get-MgUserLicenseDetail'] },

{ name:'Update-MgUser', track:'entra', cat:'lifecycle', level:'intermediate',
  cert:['ms-102:identity'],
  sum:'Change a user — and the leaver process that actually revokes access.',
  syntax:'Update-MgUser -UserId UPN -BodyParameter @{ … }',
  flags:[['-AccountEnabled:$false','Disable; the first step of a leaver, and reversible'],
         ['Revoke-MgUserSignInSession -UserId UPN','Invalidate refresh tokens — without this, existing sessions keep working'],
         ['-BodyParameter @{department="Sales"}','Any writable attribute'],
         ['Remove-MgUser -UserId UPN','Soft-deletes for thirty days; recoverable from deletedItems']],
  ex:[['Update-MgUser -UserId jo@contoso.com -AccountEnabled:$false','Disable the account'],
      ['Revoke-MgUserSignInSession -UserId jo@contoso.com','Kill live sessions — the step most leaver runbooks omit'],
      ['Get-MgDirectoryDeletedItemAsUser -DirectoryObjectId $id','Confirm a deleted user is still recoverable']],
  tip:'Disabling an account does **not** end sessions that already exist: an issued access token stays valid until it expires, typically up to an hour, and a refresh token far longer. A real leaver process is disable, then `Revoke-MgUserSignInSession`, then reclaim licences, then convert or delegate the mailbox — in that order. If the user is synchronised from on-premises, disabling in the cloud is reverted at the next sync cycle; the change has to be made in Active Directory.',
  related:['Get-MgUser','Get-MgUserLicenseDetail','Get-MgDirectoryOnPremiseSynchronization'] },

/* ── Roles and PIM ──────────────────────────────────────────── */
{ name:'Revoke-MgUserSignInSession', track:'entra', cat:'lifecycle', level:'intermediate',
  cert:['sc-300:identities','ms-102:security'],
  sum:'Invalidate the refresh tokens. The step that turns a disable into an actual revocation.',
  syntax:'Revoke-MgUserSignInSession -UserId UPN',
  flags:[['-UserId','The user; there is no scope narrower than "all their sessions"'],
         ['Update-MgUser -AccountEnabled:$false','Stops new sign-ins — a different action, and both are needed'],
         ['Get-MgAuditLogSignIn … -Top 5','Verify: an error code of 50058 or 50057 confirms it took effect']],
  ex:[['Revoke-MgUserSignInSession -UserId jo@contoso.com','Every refresh token invalidated'],
      ['Update-MgUser -UserId jo@contoso.com -AccountEnabled:$false; Revoke-MgUserSignInSession -UserId jo@contoso.com','The pair, in the order a leaver or an incident needs'],
      ['Get-MgAuditLogSignIn -Filter "userPrincipalName eq \'jo@contoso.com\'" -Top 3','Confirm from the log rather than by asking']],
  tip:'Disabling an account stops **new** sign-ins and does nothing to sessions that already exist — an issued access token stays valid until it expires, typically up to an hour, and a refresh token far longer. This command invalidates the refresh tokens so the next renewal fails, which is what actually closes the window. It is the step most leaver runbooks omit and the first action in a suspected compromise, before the password reset rather than after it. Note what it does not touch: an OAuth consent grant holds its own token and survives this entirely.',
  related:['Update-MgUser','Get-MgAuditLogSignIn','Get-MgOauth2PermissionGrant'] },

{ name:'Get-MgDirectoryRole', track:'entra', cat:'rbac', level:'intermediate',
  cert:['ms-102:identity','sc-300:access','az-104:identity'],
  sum:'Who holds which directory role right now — the standing-privilege question.',
  syntax:'Get-MgDirectoryRole [-DirectoryRoleId ID] | Get-MgDirectoryRoleMember -DirectoryRoleId ID',
  flags:[['Get-MgDirectoryRole -All','Only roles that have been *activated* in the tenant appear here'],
         ['Get-MgDirectoryRoleTemplate','Every role that exists, activated or not'],
         ['Get-MgDirectoryRoleMember -DirectoryRoleId ID -All','The members of one role'],
         ['.AdditionalProperties.userPrincipalName','Members come back as directory objects, not users']],
  ex:[['Get-MgDirectoryRole -All | Where-Object DisplayName -eq "Global Administrator" | ForEach-Object { Get-MgDirectoryRoleMember -DirectoryRoleId $_.Id -All }','Every Global Administrator'],
      ['Get-MgDirectoryRole -All | ForEach-Object { [pscustomobject]@{ Role=$_.DisplayName; Count=(Get-MgDirectoryRoleMember -DirectoryRoleId $_.Id -All).Count } } | Sort-Object Count -Descending','Role assignment counts across the tenant'],
      ['Get-MgDirectoryRoleTemplate | Select-Object DisplayName,Description | Sort-Object DisplayName','What roles exist, when picking a least-privilege one']],
  tip:'Microsoft\'s guidance is fewer than five permanent Global Administrators, and counting them is the first number anyone asks for in an identity review. Two subtleties: `Get-MgDirectoryRole` lists only roles that have ever been activated in the tenant, so a role with no members may simply not appear; and with PIM in use this shows only **active** assignments, missing everyone who is merely eligible — which is most of the privilege in a well-run tenant.',
  related:['Get-MgRoleManagementDirectoryRoleEligibilitySchedule','Get-MgAuditLogDirectoryAudit'] },

{ name:'Get-MgRoleManagementDirectoryRoleEligibilitySchedule', track:'entra', cat:'rbac', level:'advanced',
  cert:['sc-300:access','ms-102:identity'],
  sum:'PIM eligibility — the privilege that is not visible in the role member list.',
  syntax:'Get-MgRoleManagementDirectoryRoleEligibilitySchedule -All [-Filter "…"]',
  flags:[['-ExpandProperty principal,roleDefinition','Otherwise you get object ids and have to resolve them yourself'],
         ['-Filter "roleDefinitionId eq \'62e90394-…\'"','That id is Global Administrator, and it is the same in every tenant'],
         ['Get-MgRoleManagementDirectoryRoleAssignmentScheduleInstance','Currently *activated* assignments, including PIM ones'],
         ['Get-MgRoleManagementDirectoryRoleAssignment','Permanent assignments made outside PIM']],
  ex:[['Get-MgRoleManagementDirectoryRoleEligibilitySchedule -All -ExpandProperty principal,roleDefinition | Select-Object @{n=\'Who\';e={$_.Principal.AdditionalProperties.displayName}},@{n=\'Role\';e={$_.RoleDefinition.DisplayName}}','Everyone who can elevate, and to what'],
      ['Get-MgRoleManagementDirectoryRoleAssignmentScheduleInstance -All -Filter "assignmentType eq \'Activated\'"','Who is elevated right now'],
      ['Get-MgAuditLogDirectoryAudit -Filter "activityDisplayName eq \'Add member to role completed (PIM activation)\'" -Top 20','Activation history, with justifications']],
  tip:'A tenant with PIM configured properly has almost nobody in the permanent role lists and a lot of people eligible, so an audit that reads only `Get-MgDirectoryRoleMember` concludes the tenant is beautifully locked down while missing all of the actual privilege. The controls that make eligibility safe rather than cosmetic are the ones to check: approval required, justification required, MFA on activation, and a short maximum duration. Eligibility with no approval and an eight-hour window is standing privilege with extra steps.',
  related:['Get-MgDirectoryRole','Get-MgAuditLogDirectoryAudit'] },

/* ── Licensing ──────────────────────────────────────────────── */
{ name:'Get-MgUserLicenseDetail', track:'entra', cat:'lic', level:'intermediate',
  cert:['ms-102:identity'],
  sum:'What a user is licensed for, and why a feature they should have is missing.',
  syntax:'Get-MgUserLicenseDetail -UserId UPN',
  flags:[['.SkuPartNumber','The licence, e.g. SPE_E3, ENTERPRISEPACK, EMS'],
         ['.ServicePlans','The individual services inside it, each with a provisioning status'],
         ['Get-MgSubscribedSku','Tenant-wide: what you own and how many are consumed'],
         ['(Get-MgUser -UserId … -Property licenseAssignmentStates).LicenseAssignmentStates','Direct versus group-based, and any error state']],
  ex:[['Get-MgUserLicenseDetail -UserId jo@contoso.com | Select-Object SkuPartNumber','What they hold'],
      ['(Get-MgUser -UserId jo@contoso.com -Property licenseAssignmentStates).LicenseAssignmentStates | Select-Object SkuId,AssignedByGroup,State,Error','Where each licence came from, and whether it failed'],
      ['Get-MgSubscribedSku | Select-Object SkuPartNumber,@{n=\'Used\';e={$_.ConsumedUnits}},@{n=\'Total\';e={$_.PrepaidUnits.Enabled}}','Consumption against entitlement']],
  tip:'`LicenseAssignmentStates` is the field that explains the confusing cases, because it shows whether a licence came directly or from a group and carries an `Error` when assignment failed. The two errors worth recognising: `CountViolation` means you have run out of that SKU, and `MutuallyExclusiveViolation` means two overlapping licences conflict — commonly a user in two group-licensing groups where both grant the same service plan. A licence assigned by group cannot be removed from the user directly; you remove them from the group.',
  related:['Get-MgGroup','Get-MgUser'] },

/* ── Hybrid ─────────────────────────────────────────────────── */
{ name:'Get-MgDirectoryOnPremiseSynchronization', track:'entra', cat:'hybrid', level:'advanced',
  cert:['ms-102:identity','az-104:identity'],
  sum:'Whether directory synchronisation is enabled, and how stale the last sync is.',
  syntax:'Get-MgDirectoryOnPremiseSynchronization',
  flags:[['.Features','Which sync features are on — password hash sync, writeback, seamless SSO'],
         ['.Configuration.AccidentalDeletionPrevention','The threshold that stops a bad sync deleting thousands of objects'],
         ['(Get-MgOrganization).OnPremisesLastSyncDateTime','The timestamp that matters most: when did anything last arrive'],
         ['(Get-MgUser -Property onPremisesSyncEnabled,onPremisesLastSyncDateTime …)','Per object, which is mastered where']],
  ex:[['(Get-MgOrganization).OnPremisesLastSyncDateTime','If this is hours old, the sync server is the problem'],
      ['Get-MgUser -UserId jo@contoso.com -Property onPremisesSyncEnabled,onPremisesImmutableId,onPremisesLastSyncDateTime | Format-List','Whether this object is mastered on-premises'],
      ['Get-MgDirectoryOnPremiseSynchronization | Select-Object -ExpandProperty Features','Password hash sync on or off, and writeback state']],
  tip:'The rule that explains most hybrid confusion is **source of authority**: an object with `onPremisesSyncEnabled` is mastered in Active Directory, so a cloud-side edit is either rejected outright or silently reverted at the next cycle — which by default is every thirty minutes. A user who "keeps getting re-enabled" is a leaver disabled in the cloud and still enabled on-premises. On the sync server itself, `Get-ADSyncScheduler` and `Start-ADSyncSyncCycle -PolicyType Delta` are the two commands to know, and the Synchronization Service Manager is where per-object errors are readable.',
  related:['Get-MgUser','Update-MgUser'] },

{ name:'Get-MgDomain', track:'entra', cat:'entra', level:'beginner',
  cert:['ms-102:identity'],
  sum:'Verified domains and their authentication type — federated or managed.',
  syntax:'Get-MgDomain [-DomainId contoso.com]',
  flags:[['.AuthenticationType','Managed means Entra authenticates; Federated means an external identity provider does'],
         ['.IsVerified / .IsDefault','Whether DNS verification completed, and which domain new users get'],
         ['Get-MgDomainServiceConfigurationRecord -DomainId D','The DNS records the domain needs'],
         ['.SupportedServices','Email, OfficeCommunicationsOnline, Intune — a domain can be verified for some and not others']],
  ex:[['Get-MgDomain | Select-Object Id,AuthenticationType,IsVerified,IsDefault','Every domain and how it authenticates'],
      ['Get-MgDomainServiceConfigurationRecord -DomainId contoso.com | Select-Object RecordType,Label,AdditionalProperties','What DNS still needs'],
      ['Get-MgDomain -DomainId contoso.com | Select-Object -ExpandProperty PasswordValidityPeriodInDays','Password expiry policy, per domain']],
  tip:'`AuthenticationType` decides where an authentication failure has to be debugged. A **federated** domain sends the user to AD FS or a third-party provider, so Entra never sees the password and the sign-in log records a federation error rather than a credential one — chasing Conditional Access there wastes an hour. **Managed** means Entra validates directly, using password hash sync or pass-through authentication. Converting between the two is a tenant-wide event that affects every user in the domain at once.',
  related:['Get-MgDirectoryOnPremiseSynchronization','Get-MgAuditLogSignIn'] }

);
