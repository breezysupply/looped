/* AWS track — storage, databases, and the audit trail that tells you what
   happened to them. The CloudTrail and Config entries are the ones that turn
   "nobody knows" into a name and a timestamp.                                */
window.LX = window.LX || { commands: [], scenarios: [], drills: [] };

LX.commands.push(

/* ── S3 ─────────────────────────────────────────────────────── */
{ name:'aws s3api get-bucket-policy', track:'aws', cat:'s3', level:'intermediate',
  cert:['aws-sec:data-protection','aws-saa:secure-architectures'],
  sum:'The resource policy — half of every S3 permission question, and the half people forget.',
  syntax:'aws s3api get-bucket-policy --bucket NAME',
  flags:[['--query Policy --output text | jq .','The response wraps the document as a JSON string'],
         ['get-bucket-policy-status --bucket NAME','Whether AWS considers the bucket public'],
         ['delete-bucket-policy --bucket NAME','Removes it entirely — read it into a file first']],
  ex:[['aws s3api get-bucket-policy --bucket artifacts --query Policy --output text | jq .','The document, readable'],
      ['aws s3api get-bucket-policy-status --bucket artifacts','PolicyStatus.IsPublic — the answer an auditor wants'],
      ['aws s3api get-bucket-acl --bucket artifacts','ACLs: legacy, usually disabled now, still occasionally the cause']],
  tip:'S3 access is a union of several inputs and the denial message never says which one lost: the identity policy, the bucket policy, Block Public Access, object ownership and ACLs, the VPC endpoint policy if the request came through one, and the KMS key policy if the object is encrypted with a customer-managed key. Work in that order and check the key policy early — an object you can `ListBucket` but not `GetObject` is very often a KMS denial wearing an S3 error message. Note that `NoSuchBucketPolicy` is a normal response meaning no policy exists, not a failure.',
  related:['aws s3api get-public-access-block','aws kms describe-key'] },

{ name:'aws s3api get-public-access-block', track:'aws', cat:'s3', level:'beginner',
  cert:['aws-sec:data-protection','aws-sysops:security'],
  sum:'The four switches that override everything else, and should be on everywhere.',
  syntax:'aws s3api get-public-access-block --bucket NAME',
  flags:[['BlockPublicAcls / IgnorePublicAcls','Kill public ACLs at creation, and ignore existing ones'],
         ['BlockPublicPolicy / RestrictPublicBuckets','Reject public bucket policies, and neutralise existing ones'],
         ['aws s3control get-public-access-block --account-id …','The account-level setting, which wins over the bucket one']],
  ex:[['aws s3api get-public-access-block --bucket artifacts --query PublicAccessBlockConfiguration','All four flags for one bucket'],
      ['aws s3control get-public-access-block --account-id 111122223333','The account-wide default — set this and most of the problem disappears'],
      ['for b in $(aws s3api list-buckets --query \'Buckets[].Name\' --output text); do echo -n "$b "; aws s3api get-public-access-block --bucket $b --query \'PublicAccessBlockConfiguration.BlockPublicPolicy\' 2>/dev/null || echo NONE; done','Sweep the account']],
  tip:'This is the control that would have prevented most public-bucket incidents, and it is now on by default for new buckets — which means the risk has moved to old buckets and to whoever deliberately turned it off. A missing configuration returns an error rather than "false", so a sweep must treat "no configuration" as unprotected. Setting it at the **account** level is the version worth doing: it applies to every bucket including ones created later by someone in a hurry.',
  related:['aws s3api get-bucket-policy','aws s3api list-object-versions'] },

{ name:'aws s3api list-object-versions', track:'aws', cat:'s3', level:'intermediate',
  cert:['aws-saa:resilient-architectures','aws-sysops:reliability'],
  sum:'What versioning kept after a delete — and why the bucket costs more than it should.',
  syntax:'aws s3api list-object-versions --bucket NAME [--prefix PREFIX]',
  flags:[['--prefix path/','Scope it; a versioned bucket can have millions of entries'],
         ['--query \'DeleteMarkers[].[Key,VersionId,LastModified]\'','What was "deleted" and can be undeleted'],
         ['--query \'Versions[?IsLatest==`false`].[Key,Size]\'','Noncurrent versions — invisible cost']],
  ex:[['aws s3api list-object-versions --bucket data --prefix reports/ --query \'DeleteMarkers[].[Key,VersionId]\' --output table','Find the delete marker hiding a file'],
      ['aws s3api delete-object --bucket data --key reports/q3.csv --version-id 3sL4kqtJlcpXroDTDm','Delete the marker, and the object is back'],
      ['aws s3api get-bucket-lifecycle-configuration --bucket data --query \'Rules[].[ID,Status,NoncurrentVersionExpiration.NoncurrentDays]\'','Whether anything ever cleans up old versions']],
  tip:'In a versioned bucket a delete does not delete: it writes a **delete marker** that becomes the latest version, so the object vanishes from `ls` while every byte is still billed. That is the recovery path — remove the marker by version id and the object returns — and it is also why a versioned bucket with no lifecycle rule grows forever. `MFA Delete` is the stronger control for anything a ransomware or insider scenario has to survive, since it requires the root user with an MFA token to permanently delete a version.',
  related:['aws s3api get-bucket-policy','aws backup list-backup-jobs'] },

{ name:'aws s3 sync', track:'aws', cat:'s3', level:'beginner',
  cert:['aws-saa:cost-optimized'],
  sum:'The everyday copy — and `--delete` is the flag that ruins afternoons.',
  syntax:'aws s3 sync SRC DST [--delete] [--exclude] [--include] [--dryrun]',
  flags:[['--dryrun','Prints what it would do. Use it every single time before --delete'],
         ['--delete','Removes destination objects missing from the source. Irreversible without versioning'],
         ['--exclude "*" --include "*.log"','Order matters — later patterns override earlier ones'],
         ['--storage-class GLACIER_IR','Set the class as you write, rather than paying to transition later'],
         ['--sse aws:kms --sse-kms-key-id alias/data','Encrypt with a key you control']],
  ex:[['aws s3 sync ./build s3://artifacts/app/ --dryrun','What would happen'],
      ['aws s3 sync s3://prod-data s3://dr-data --source-region us-east-1 --region us-west-2','Cross-region copy for DR'],
      ['aws s3 sync ./logs s3://archive/ --exclude "*" --include "*.gz" --storage-class GLACIER_IR','Only the archives, straight to cold storage']],
  tip:'`sync` compares size and modification time, not content, so a file that changed without changing size or timestamp is skipped silently — use `--exact-timestamps` when pulling down, or checksums if it matters. `--delete` with the source and destination the wrong way round is one of the classic self-inflicted outages, and the reason to enable versioning on anything you sync into. For very large transfers, tune `s3 configure set max_concurrent_requests` rather than assuming the network is the limit.',
  related:['aws s3api list-object-versions','aws s3api get-bucket-policy'] },

/* ── RDS ────────────────────────────────────────────────────── */
{ name:'aws rds describe-db-instances', track:'aws', cat:'rds', level:'beginner',
  cert:['aws-saa:resilient-architectures','aws-sysops:reliability'],
  sum:'State, endpoint, Multi-AZ, and whether it is reachable from where you think.',
  syntax:'aws rds describe-db-instances [--db-instance-identifier ID]',
  flags:[['--query \'DBInstances[].[DBInstanceIdentifier,DBInstanceStatus,Endpoint.Address,MultiAZ,PubliclyAccessible]\'','The five fields a connection problem needs'],
         ['--query \'DBInstances[].VpcSecurityGroups[].VpcSecurityGroupId\'','The security group to check against the app\'s'],
         ['--query \'DBInstances[].DBSubnetGroup.Subnets[].SubnetIdentifier\'','Which subnets it can live in']],
  ex:[['aws rds describe-db-instances --db-instance-identifier prod-db --query \'DBInstances[0].[DBInstanceStatus,Endpoint.Address,Endpoint.Port,MultiAZ]\' --output text','Status and endpoint'],
      ['aws rds describe-db-instances --query \'DBInstances[?PubliclyAccessible==`true`].DBInstanceIdentifier\'','Publicly accessible databases — a standing audit finding'],
      ['aws rds describe-db-instances --query \'DBInstances[?BackupRetentionPeriod==`0`].DBInstanceIdentifier\'','Instances with automated backups disabled, which also disables point-in-time recovery']],
  tip:'`PubliclyAccessible` controls whether the endpoint resolves to a public or a private address; it is not a firewall, and a "private" database with an open security group in a public subnet is still exposed. Multi-AZ is **availability, not read scaling** — the standby serves no traffic and failover is a DNS change taking a minute or two, so the application must reconnect rather than assume a stable IP. Read replicas are the scaling answer and they are asynchronous, so a read straight after a write can legitimately miss it.',
  related:['aws rds describe-events','aws ec2 describe-security-groups'] },

{ name:'aws rds describe-events', track:'aws', cat:'rds', level:'intermediate',
  cert:['aws-sysops:reliability'],
  sum:'What RDS did and when — failovers, restarts, storage autoscaling, backup windows.',
  syntax:'aws rds describe-events --source-identifier ID --source-type db-instance',
  flags:[['--duration 1440','Minutes to look back; the default is only sixty'],
         ['--event-categories failover maintenance','Narrow to the ones that explain an outage'],
         ['--query \'Events[].[Date,Message]\'','Timestamp and plain-English message']],
  ex:[['aws rds describe-events --source-identifier prod-db --source-type db-instance --duration 1440 --query \'Events[].[Date,Message]\' --output table','The last day of RDS activity'],
      ['aws rds describe-events --source-identifier prod-db --source-type db-instance --event-categories failover --duration 10080','Failovers in the last week'],
      ['aws rds describe-db-log-files --db-instance-identifier prod-db --query \'DescribeDBLogFiles[-3:].[LogFileName,Size]\' --output table','The database\'s own logs, which is where slow queries live']],
  tip:'A Multi-AZ failover appears here as a pair of events roughly a minute apart, and that is often the entire explanation for a burst of application errors that looked like a code problem — connection pools holding dead sockets across a failover fail in exactly that shape. `Storage-full` and `Storage autoscaling` events are the other high-value ones, because a database that hits its storage ceiling stops accepting writes and the application error says nothing about disk.',
  related:['aws rds describe-db-instances','aws cloudwatch describe-alarms'] },

{ name:'aws rds describe-db-snapshots', track:'aws', cat:'dr', level:'intermediate',
  cert:['aws-saa:resilient-architectures','aws-sysops:reliability'],
  sum:'Restore points, and the difference between a snapshot and point-in-time recovery.',
  syntax:'aws rds describe-db-snapshots --db-instance-identifier ID [--snapshot-type manual|automated]',
  flags:[['--snapshot-type automated','Deleted with the instance; manual snapshots outlive it'],
         ['--query \'sort_by(DBSnapshots,&SnapshotCreateTime)[-1].[DBSnapshotIdentifier,SnapshotCreateTime]\'','The newest'],
         ['restore-db-instance-to-point-in-time','Any second within the retention window, not just snapshot times']],
  ex:[['aws rds describe-db-snapshots --db-instance-identifier prod-db --query \'sort_by(DBSnapshots,&SnapshotCreateTime)[-3:].[DBSnapshotIdentifier,SnapshotCreateTime,Status]\' --output table','Recent restore points'],
      ['aws rds describe-db-instances --db-instance-identifier prod-db --query \'DBInstances[0].LatestRestorableTime\'','The most recent second you could restore to'],
      ['aws rds restore-db-instance-to-point-in-time --source-db-instance-identifier prod-db --target-db-instance-identifier prod-db-recover --restore-time 2026-08-27T11:15:00Z','Restore beside the original — never over it']],
  tip:'A restore always creates a **new instance**; it never overwrites the original, which is what makes recovery a safe thing to start early in an incident rather than a decision to agonise over. Two consequences catch people out: automated snapshots are deleted when the instance is deleted, so a final manual snapshot is the difference between an accident and a disaster; and the restored instance comes back with default parameter and security groups, so it is unreachable until you attach the real ones. Restoring a fifty-gigabyte instance is a tens-of-minutes operation — which is your RTO, whether or not anyone has measured it.',
  related:['aws rds describe-db-instances','aws backup list-backup-jobs'] },

/* ── The audit trail ────────────────────────────────────────── */
{ name:'aws cloudtrail lookup-events', track:'aws', cat:'obs', level:'intermediate',
  cert:['aws-sec:logging-and-monitoring','aws-sysops:monitoring'],
  sum:'Who did that, from where, and when. The answer to most incident postmortems.',
  syntax:'aws cloudtrail lookup-events --lookup-attributes AttributeKey=KEY,AttributeValue=VALUE',
  flags:[['AttributeKey=EventName,AttributeValue=TerminateInstances','Search by API call'],
         ['AttributeKey=ResourceName,AttributeValue=i-0a1b','Everything that touched one resource'],
         ['AttributeKey=Username,AttributeValue=alex','Everything one principal did'],
         ['--start-time / --end-time','Only ninety days are available through this API']],
  ex:[['aws cloudtrail lookup-events --lookup-attributes AttributeKey=EventName,AttributeValue=DeleteBucket --start-time 2026-08-20T00:00:00Z --query \'Events[].[EventTime,Username,Resources[0].ResourceName]\' --output table','Who deleted a bucket'],
      ['aws cloudtrail lookup-events --lookup-attributes AttributeKey=ResourceName,AttributeValue=sg-0a1b --query \'Events[].[EventTime,EventName,Username]\' --output table','The change history of a security group'],
      ['aws cloudtrail lookup-events --lookup-attributes AttributeKey=EventName,AttributeValue=ConsoleLogin --query \'Events[].CloudTrailEvent\' --output text | jq -r \'select(.responseElements.ConsoleLogin=="Failure")|[.eventTime,.sourceIPAddress]|@tsv\'','Failed console logins']],
  tip:'This API only reaches back **ninety days** and only covers management events — the control plane. Data events (`GetObject`, `Invoke`, `Decrypt`) are off by default, cost extra, and are the ones you need to answer "was the data actually read", so the time to enable them is before the incident. For anything older or larger, query the trail\'s S3 bucket with Athena instead; a saved Athena table over the trail is one of the highest-value things a small team can set up in an afternoon.',
  related:['aws configservice get-resource-config-history','aws logs start-query'] },

{ name:'aws configservice get-resource-config-history', track:'aws', cat:'obs', level:'advanced',
  cert:['aws-sec:logging-and-monitoring'],
  sum:'What a resource looked like before the change — the diff CloudTrail does not give you.',
  syntax:'aws configservice get-resource-config-history --resource-type TYPE --resource-id ID',
  flags:[['--resource-type AWS::EC2::SecurityGroup','The type has to be exact, including the namespace'],
         ['--limit 5','Newest first'],
         ['--query \'configurationItems[].[configurationItemCaptureTime,configurationItemStatus]\'','When each snapshot was taken']],
  ex:[['aws configservice get-resource-config-history --resource-type AWS::EC2::SecurityGroup --resource-id sg-0a1b --limit 2 --query \'configurationItems[].configuration\' --output text | jq .','Before and after, ready to diff'],
      ['aws configservice describe-compliance-by-config-rule --query \'ComplianceByConfigRules[?Compliance.ComplianceType==`NON_COMPLIANT`].ConfigRuleName\' --output text','Which rules are failing right now'],
      ['aws configservice get-compliance-details-by-config-rule --config-rule-name s3-bucket-public-read-prohibited --compliance-types NON_COMPLIANT --query \'EvaluationResults[].EvaluationResultIdentifier.EvaluationResultQualifier.ResourceId\'','The specific resources failing a rule']],
  tip:'CloudTrail tells you an API call happened; Config tells you what the resource looked like on either side of it. Together they answer "who changed it and what did they change", which is the question an incident review actually asks and the evidence a compliance auditor actually wants. Config also costs per configuration item recorded, so recording every resource type in every region is a real bill — scope the recorder deliberately, and keep the rules that map to controls you have to attest to.',
  related:['aws cloudtrail lookup-events','aws cloudwatch describe-alarms'] },

{ name:'aws logs start-query', track:'aws', cat:'obs', level:'intermediate',
  cert:['aws-sysops:monitoring','aws-sec:logging-and-monitoring'],
  sum:'CloudWatch Logs Insights — searching log groups without downloading them.',
  syntax:'aws logs start-query --log-group-name NAME --start-time EPOCH --end-time EPOCH --query-string QUERY',
  flags:[['--query-string \'fields @timestamp, @message | filter @message like /ERROR/ | sort @timestamp desc | limit 20\'','The query language: fields, filter, stats, sort, limit'],
         ['get-query-results --query-id ID','Results arrive asynchronously — start then fetch'],
         ['--log-group-names','Several groups at once, which is how you correlate two services']],
  ex:[['aws logs start-query --log-group-name /aws/lambda/api --start-time $(date -d \'1 hour ago\' +%s) --end-time $(date +%s) --query-string \'fields @timestamp,@message | filter @message like /Task timed out/ | limit 20\' --query queryId --output text','Start it'],
      ['aws logs get-query-results --query-id abc-123 --query \'results[].[value]\' --output text','Fetch when Status is Complete'],
      ['aws logs tail /aws/lambda/api --follow --since 10m','The shortcut when you just want to watch it live']],
  tip:'`aws logs tail --follow` is the command to reach for during an incident and `start-query` is the one for analysis — trying to use Insights interactively through the CLI is slower than the console. Insights scans by volume of data, so it bills by gigabytes scanned: narrow the time window before you narrow the filter, since the window is what determines cost. A `filter` before a `stats` is both faster and cheaper than the reverse.',
  related:['aws cloudtrail lookup-events','aws cloudwatch describe-alarms'] },

{ name:'aws cloudwatch describe-alarms', track:'aws', cat:'obs', level:'beginner',
  cert:['aws-sysops:monitoring'],
  sum:'What is alarming now, and what is quietly stuck in INSUFFICIENT_DATA.',
  syntax:'aws cloudwatch describe-alarms [--state-value ALARM|OK|INSUFFICIENT_DATA]',
  flags:[['--state-value ALARM','What is firing'],
         ['--state-value INSUFFICIENT_DATA','Alarms that are not watching anything — worse than no alarm, because they look like coverage'],
         ['describe-alarm-history --alarm-name NAME','When it flapped, and how often']],
  ex:[['aws cloudwatch describe-alarms --state-value ALARM --query \'MetricAlarms[].[AlarmName,StateReason]\' --output table','Everything currently firing'],
      ['aws cloudwatch describe-alarms --state-value INSUFFICIENT_DATA --query \'MetricAlarms[].AlarmName\' --output text','Alarms with no data — usually a deleted resource or a wrong dimension'],
      ['aws cloudwatch describe-alarm-history --alarm-name app-5xx --max-records 10 --query \'AlarmHistoryItems[].[Timestamp,HistorySummary]\' --output table','The flap history behind an alert nobody trusts']],
  tip:'`INSUFFICIENT_DATA` is the state to audit for, because it is indistinguishable from health on a dashboard and usually means the dimension is wrong or the resource is gone — the alarm will never fire again and nobody will notice. Set `TreatMissingData` deliberately rather than by default: `missing` for a metric that legitimately goes quiet, `breaching` for a heartbeat where absence is itself the failure. And an alarm with no action attached is a widget, not an alert.',
  related:['aws logs start-query','aws rds describe-events'] },

{ name:'aws backup list-backup-jobs', track:'aws', cat:'dr', level:'intermediate',
  cert:['aws-saa:resilient-architectures','aws-sysops:reliability'],
  sum:'Whether the backups you believe in actually ran.',
  syntax:'aws backup list-backup-jobs [--by-state COMPLETED|FAILED] [--by-resource-arn ARN]',
  flags:[['--by-state FAILED','The only state worth a standing check'],
         ['--by-created-after 2026-08-01T00:00:00Z','Scope the window'],
         ['list-recovery-points-by-backup-vault --backup-vault-name NAME','What you could actually restore from']],
  ex:[['aws backup list-backup-jobs --by-state FAILED --by-created-after 2026-08-20T00:00:00Z --query \'BackupJobs[].[ResourceArn,StatusMessage]\' --output table','Failed jobs and why'],
      ['aws backup list-recovery-points-by-backup-vault --backup-vault-name prod --query \'RecoveryPoints[].[ResourceArn,CreationDate,Status]\' --output table','Restore points in the vault'],
      ['aws backup list-protected-resources --query \'Results[].[ResourceArn,LastBackupTime]\' --output table','What is covered by a plan at all — and what is not']],
  tip:'The two questions an auditor and a bad day both ask are "is every resource covered by a plan" and "has a restore ever been tested". `list-protected-resources` answers the first and nothing but an actual restore answers the second. A vault lock in compliance mode makes recovery points immutable even to an account administrator, which is the control that survives a compromised credential — and it cannot be undone, so it is a decision to make deliberately rather than during an incident.',
  related:['aws rds describe-db-snapshots','aws ec2 describe-snapshots'] }

);
